{"program": 1, "context": "File permission initializer for secure log files", "noncompliant": "// ------ Non-Compliant Program (001_nc.cpp)\n// Context: File permission initializer for secure log files\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_001 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {0600, 0640, 0644, 0755};  // NC: octal file permissions\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 0600, 0640, 0644, 0755\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_001::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (001_c.cpp)\n// Context: File permission initializer for secure log files\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_001 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {384, 416, 420, 493};  // C: decimal equivalents\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 384, 416, 420, 493\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_001::execute();\n    return 0;\n}\n"}
{"program": 2, "context": "Robot sensor configuration file generator", "noncompliant": "// ------ Non-Compliant Program (002_nc.cpp)\n// Context: Robot sensor configuration file generator\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_002 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {0600, 0640, 0644, 0755};  // NC: octal file permissions\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 0600, 0640, 0644, 0755\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_002::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (002_c.cpp)\n// Context: Robot sensor configuration file generator\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_002 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {384, 416, 420, 493};  // C: decimal equivalents\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 384, 416, 420, 493\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_002::execute();\n    return 0;\n}\n"}
{"program": 3, "context": "Data logger mode setup for industrial controller", "noncompliant": "// ------ Non-Compliant Program (003_nc.cpp)\n// Context: Data logger mode setup for industrial controller\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_003 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_003::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (003_c.cpp)\n// Context: Data logger mode setup for industrial controller\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_003 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_003::execute();\n    return 0;\n}\n"}
{"program": 4, "context": "Embedded flight recorder calibration data", "noncompliant": "// ------ Non-Compliant Program (004_nc.cpp)\n// Context: Embedded flight recorder calibration data\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_004 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_004::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (004_c.cpp)\n// Context: Embedded flight recorder calibration data\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_004 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_004::execute();\n    return 0;\n}\n"}
{"program": 5, "context": "LED diagnostic blink pattern controller", "noncompliant": "// ------ Non-Compliant Program (005_nc.cpp)\n// Context: LED diagnostic blink pattern controller\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_005 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_005::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (005_c.cpp)\n// Context: LED diagnostic blink pattern controller\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_005 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_005::execute();\n    return 0;\n}\n"}
{"program": 6, "context": "Drone telemetry file output", "noncompliant": "// ------ Non-Compliant Program (006_nc.cpp)\n// Context: Drone telemetry file output\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_006 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {0600, 0640, 0644, 0755};  // NC: octal file permissions\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 0600, 0640, 0644, 0755\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_006::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (006_c.cpp)\n// Context: Drone telemetry file output\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_006 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {384, 416, 420, 493};  // C: decimal equivalents\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 384, 416, 420, 493\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_006::execute();\n    return 0;\n}\n"}
{"program": 7, "context": "Audio signal analyzer sample counter", "noncompliant": "// ------ Non-Compliant Program (007_nc.cpp)\n// Context: Audio signal analyzer sample counter\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_007 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_007::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (007_c.cpp)\n// Context: Audio signal analyzer sample counter\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_007 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_007::execute();\n    return 0;\n}\n"}
{"program": 8, "context": "Smart parking meter configuration updater", "noncompliant": "// ------ Non-Compliant Program (008_nc.cpp)\n// Context: Smart parking meter configuration updater\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_008 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_008::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (008_c.cpp)\n// Context: Smart parking meter configuration updater\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_008 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_008::execute();\n    return 0;\n}\n"}
{"program": 9, "context": "Automotive ECU diagnostic log saver", "noncompliant": "// ------ Non-Compliant Program (009_nc.cpp)\n// Context: Automotive ECU diagnostic log saver\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_009 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_009::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (009_c.cpp)\n// Context: Automotive ECU diagnostic log saver\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_009 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_009::execute();\n    return 0;\n}\n"}
{"program": 10, "context": "Surveillance camera configuration writer", "noncompliant": "// ------ Non-Compliant Program (010_nc.cpp)\n// Context: Surveillance camera configuration writer\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_010 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_010::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (010_c.cpp)\n// Context: Surveillance camera configuration writer\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_010 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_010::execute();\n    return 0;\n}\n"}
{"program": 11, "context": "Automated warehouse conveyor speed tuner", "noncompliant": "// ------ Non-Compliant Program (011_nc.cpp)\n// Context: Automated warehouse conveyor speed tuner\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_011 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_011::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (011_c.cpp)\n// Context: Automated warehouse conveyor speed tuner\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_011 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_011::execute();\n    return 0;\n}\n"}
{"program": 12, "context": "Flight control sensor data dumper", "noncompliant": "// ------ Non-Compliant Program (012_nc.cpp)\n// Context: Flight control sensor data dumper\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_012 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_012::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (012_c.cpp)\n// Context: Flight control sensor data dumper\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_012 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_012::execute();\n    return 0;\n}\n"}
{"program": 13, "context": "Autonomous car brake response calibrator", "noncompliant": "// ------ Non-Compliant Program (013_nc.cpp)\n// Context: Autonomous car brake response calibrator\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_013 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_013::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (013_c.cpp)\n// Context: Autonomous car brake response calibrator\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_013 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_013::execute();\n    return 0;\n}\n"}
{"program": 14, "context": "Wind turbine blade sensor encoder", "noncompliant": "// ------ Non-Compliant Program (014_nc.cpp)\n// Context: Wind turbine blade sensor encoder\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_014 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_014::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (014_c.cpp)\n// Context: Wind turbine blade sensor encoder\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_014 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_014::execute();\n    return 0;\n}\n"}
{"program": 15, "context": "Medical ventilator airflow regulator", "noncompliant": "// ------ Non-Compliant Program (015_nc.cpp)\n// Context: Medical ventilator airflow regulator\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_015 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_015::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (015_c.cpp)\n// Context: Medical ventilator airflow regulator\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_015 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_015::execute();\n    return 0;\n}\n"}
{"program": 16, "context": "Maritime sonar ping signal generator", "noncompliant": "// ------ Non-Compliant Program (016_nc.cpp)\n// Context: Maritime sonar ping signal generator\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_016 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_016::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (016_c.cpp)\n// Context: Maritime sonar ping signal generator\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_016 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_016::execute();\n    return 0;\n}\n"}
{"program": 17, "context": "Security door keypad tone generator", "noncompliant": "// ------ Non-Compliant Program (017_nc.cpp)\n// Context: Security door keypad tone generator\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_017 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_017::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (017_c.cpp)\n// Context: Security door keypad tone generator\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_017 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_017::execute();\n    return 0;\n}\n"}
{"program": 18, "context": "Spacecraft temperature calibration constants", "noncompliant": "// ------ Non-Compliant Program (018_nc.cpp)\n// Context: Spacecraft temperature calibration constants\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_018 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_018::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (018_c.cpp)\n// Context: Spacecraft temperature calibration constants\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_018 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_018::execute();\n    return 0;\n}\n"}
{"program": 19, "context": "Nuclear plant pressure sensor diagnostics", "noncompliant": "// ------ Non-Compliant Program (019_nc.cpp)\n// Context: Nuclear plant pressure sensor diagnostics\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_019 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_019::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (019_c.cpp)\n// Context: Nuclear plant pressure sensor diagnostics\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_019 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_019::execute();\n    return 0;\n}\n"}
{"program": 20, "context": "Automated irrigation timer sequence", "noncompliant": "// ------ Non-Compliant Program (020_nc.cpp)\n// Context: Automated irrigation timer sequence\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_020 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_020::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (020_c.cpp)\n// Context: Automated irrigation timer sequence\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_020 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_020::execute();\n    return 0;\n}\n"}
{"program": 21, "context": "Space rover motor current logger", "noncompliant": "// ------ Non-Compliant Program (021_nc.cpp)\n// Context: Space rover motor current logger\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_021 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_021::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (021_c.cpp)\n// Context: Space rover motor current logger\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_021 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_021::execute();\n    return 0;\n}\n"}
{"program": 22, "context": "Traffic monitoring system frame counter", "noncompliant": "// ------ Non-Compliant Program (022_nc.cpp)\n// Context: Traffic monitoring system frame counter\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_022 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_022::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (022_c.cpp)\n// Context: Traffic monitoring system frame counter\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_022 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_022::execute();\n    return 0;\n}\n"}
{"program": 23, "context": "Automotive radar pulse generator", "noncompliant": "// ------ Non-Compliant Program (023_nc.cpp)\n// Context: Automotive radar pulse generator\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_023 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_023::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (023_c.cpp)\n// Context: Automotive radar pulse generator\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_023 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_023::execute();\n    return 0;\n}\n"}
{"program": 24, "context": "Server log file permission handler", "noncompliant": "// ------ Non-Compliant Program (024_nc.cpp)\n// Context: Server log file permission handler\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_024 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {0600, 0640, 0644, 0755};  // NC: octal file permissions\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 0600, 0640, 0644, 0755\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_024::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (024_c.cpp)\n// Context: Server log file permission handler\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_024 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {384, 416, 420, 493};  // C: decimal equivalents\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 384, 416, 420, 493\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_024::execute();\n    return 0;\n}\n"}
{"program": 25, "context": "Secure service PID file creation", "noncompliant": "// ------ Non-Compliant Program (025_nc.cpp)\n// Context: Secure service PID file creation\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_025 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {0600, 0640, 0644, 0755};  // NC: octal file permissions\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 0600, 0640, 0644, 0755\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_025::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (025_c.cpp)\n// Context: Secure service PID file creation\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_025 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {384, 416, 420, 493};  // C: decimal equivalents\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 384, 416, 420, 493\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_025::execute();\n    return 0;\n}\n"}
{"program": 26, "context": "Telemetry CSV writer with separator", "noncompliant": "// ------ Non-Compliant Program (026_nc.cpp)\n// Context: Telemetry CSV writer with separator\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_026 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_026::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (026_c.cpp)\n// Context: Telemetry CSV writer with separator\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_026 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_026::execute();\n    return 0;\n}\n"}
{"program": 27, "context": "Bitmask parser for device flags", "noncompliant": "// ------ Non-Compliant Program (027_nc.cpp)\n// Context: Bitmask parser for device flags\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_027 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_027::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (027_c.cpp)\n// Context: Bitmask parser for device flags\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_027 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_027::execute();\n    return 0;\n}\n"}
{"program": 28, "context": "Camera firmware version fields", "noncompliant": "// ------ Non-Compliant Program (028_nc.cpp)\n// Context: Camera firmware version fields\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_028 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_028::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (028_c.cpp)\n// Context: Camera firmware version fields\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_028 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_028::execute();\n    return 0;\n}\n"}
{"program": 29, "context": "Network daemon umask initializer", "noncompliant": "// ------ Non-Compliant Program (029_nc.cpp)\n// Context: Network daemon umask initializer\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_029 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_029::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (029_c.cpp)\n// Context: Network daemon umask initializer\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_029 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_029::execute();\n    return 0;\n}\n"}
{"program": 30, "context": "Log writer with escaped header chars", "noncompliant": "// ------ Non-Compliant Program (030_nc.cpp)\n// Context: Log writer with escaped header chars\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_030 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {'\\011', '\\012', '\\015', '\\033'};  // NC: octal escape sequences\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: '\\011', '\\012', '\\015', '\\033'\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_030::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (030_c.cpp)\n// Context: Log writer with escaped header chars\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_030 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {'\\t', '\\n', '\\r', '\\x1B'};  // C: standard/hex escapes\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: '\\t', '\\n', '\\r', '\\x1B'\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_030::execute();\n    return 0;\n}\n"}
{"program": 31, "context": "Readonly results file via fchmod", "noncompliant": "// ------ Non-Compliant Program (031_nc.cpp)\n// Context: Readonly results file via fchmod\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_031 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {0600, 0640, 0644, 0755};  // NC: octal file permissions\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 0600, 0640, 0644, 0755\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_031::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (031_c.cpp)\n// Context: Readonly results file via fchmod\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_031 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {384, 416, 420, 493};  // C: decimal equivalents\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 384, 416, 420, 493\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_031::execute();\n    return 0;\n}\n"}
{"program": 32, "context": "PLC schedule minute markers", "noncompliant": "// ------ Non-Compliant Program (032_nc.cpp)\n// Context: PLC schedule minute markers\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_032 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_032::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (032_c.cpp)\n// Context: PLC schedule minute markers\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_032 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_032::execute();\n    return 0;\n}\n"}
{"program": 33, "context": "POS terminal log file permissions", "noncompliant": "// ------ Non-Compliant Program (033_nc.cpp)\n// Context: POS terminal log file permissions\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_033 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {0600, 0640, 0644, 0755};  // NC: octal file permissions\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 0600, 0640, 0644, 0755\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_033::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (033_c.cpp)\n// Context: POS terminal log file permissions\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_033 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {384, 416, 420, 493};  // C: decimal equivalents\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 384, 416, 420, 493\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_033::execute();\n    return 0;\n}\n"}
{"program": 34, "context": "Marine sonar depth calibration writer", "noncompliant": "// ------ Non-Compliant Program (034_nc.cpp)\n// Context: Marine sonar depth calibration writer\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_034 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_034::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (034_c.cpp)\n// Context: Marine sonar depth calibration writer\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_034 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_034::execute();\n    return 0;\n}\n"}
{"program": 35, "context": "Rail crossing light pattern configuration", "noncompliant": "// ------ Non-Compliant Program (035_nc.cpp)\n// Context: Rail crossing light pattern configuration\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_035 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_035::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (035_c.cpp)\n// Context: Rail crossing light pattern configuration\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_035 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_035::execute();\n    return 0;\n}\n"}
{"program": 36, "context": "Air traffic beacon identifier printer", "noncompliant": "// ------ Non-Compliant Program (036_nc.cpp)\n// Context: Air traffic beacon identifier printer\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_036 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_036::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (036_c.cpp)\n// Context: Air traffic beacon identifier printer\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_036 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_036::execute();\n    return 0;\n}\n"}
{"program": 37, "context": "Factory temperature sensor threshold", "noncompliant": "// ------ Non-Compliant Program (037_nc.cpp)\n// Context: Factory temperature sensor threshold\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_037 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_037::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (037_c.cpp)\n// Context: Factory temperature sensor threshold\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_037 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_037::execute();\n    return 0;\n}\n"}
{"program": 38, "context": "Space station environmental log writer", "noncompliant": "// ------ Non-Compliant Program (038_nc.cpp)\n// Context: Space station environmental log writer\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_038 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_038::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (038_c.cpp)\n// Context: Space station environmental log writer\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_038 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_038::execute();\n    return 0;\n}\n"}
{"program": 39, "context": "Submarine sonar ping rate configuration", "noncompliant": "// ------ Non-Compliant Program (039_nc.cpp)\n// Context: Submarine sonar ping rate configuration\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_039 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_039::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (039_c.cpp)\n// Context: Submarine sonar ping rate configuration\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_039 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_039::execute();\n    return 0;\n}\n"}
{"program": 40, "context": "Power grid relay control mask", "noncompliant": "// ------ Non-Compliant Program (040_nc.cpp)\n// Context: Power grid relay control mask\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_040 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_040::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (040_c.cpp)\n// Context: Power grid relay control mask\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_040 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_040::execute();\n    return 0;\n}\n"}
{"program": 41, "context": "Data center fan control threshold", "noncompliant": "// ------ Non-Compliant Program (041_nc.cpp)\n// Context: Data center fan control threshold\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_041 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_041::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (041_c.cpp)\n// Context: Data center fan control threshold\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_041 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_041::execute();\n    return 0;\n}\n"}
{"program": 42, "context": "Air traffic radar beacon power test", "noncompliant": "// ------ Non-Compliant Program (042_nc.cpp)\n// Context: Air traffic radar beacon power test\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_042 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_042::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (042_c.cpp)\n// Context: Air traffic radar beacon power test\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_042 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_042::execute();\n    return 0;\n}\n"}
{"program": 43, "context": "Weather station sensor calibration", "noncompliant": "// ------ Non-Compliant Program (043_nc.cpp)\n// Context: Weather station sensor calibration\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_043 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_043::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (043_c.cpp)\n// Context: Weather station sensor calibration\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_043 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_043::execute();\n    return 0;\n}\n"}
{"program": 44, "context": "Airport runway ID display generator", "noncompliant": "// ------ Non-Compliant Program (044_nc.cpp)\n// Context: Airport runway ID display generator\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_044 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_044::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (044_c.cpp)\n// Context: Airport runway ID display generator\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_044 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_044::execute();\n    return 0;\n}\n"}
{"program": 45, "context": "Solar panel inverter efficiency reader", "noncompliant": "// ------ Non-Compliant Program (045_nc.cpp)\n// Context: Solar panel inverter efficiency reader\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_045 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_045::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (045_c.cpp)\n// Context: Solar panel inverter efficiency reader\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_045 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_045::execute();\n    return 0;\n}\n"}
{"program": 46, "context": "Weather radar pulse count analyzer", "noncompliant": "// ------ Non-Compliant Program (046_nc.cpp)\n// Context: Weather radar pulse count analyzer\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_046 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_046::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (046_c.cpp)\n// Context: Weather radar pulse count analyzer\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_046 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_046::execute();\n    return 0;\n}\n"}
{"program": 47, "context": "Blockchain transaction nonce formatter", "noncompliant": "// ------ Non-Compliant Program (047_nc.cpp)\n// Context: Blockchain transaction nonce formatter\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_047 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_047::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (047_c.cpp)\n// Context: Blockchain transaction nonce formatter\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_047 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_047::execute();\n    return 0;\n}\n"}
{"program": 48, "context": "Machine learning batch size configurator", "noncompliant": "// ------ Non-Compliant Program (048_nc.cpp)\n// Context: Machine learning batch size configurator\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_048 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_048::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (048_c.cpp)\n// Context: Machine learning batch size configurator\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_048 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_048::execute();\n    return 0;\n}\n"}
{"program": 49, "context": "Underwater sensor beacon address list", "noncompliant": "// ------ Non-Compliant Program (049_nc.cpp)\n// Context: Underwater sensor beacon address list\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_049 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_049::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (049_c.cpp)\n// Context: Underwater sensor beacon address list\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_049 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_049::execute();\n    return 0;\n}\n"}
{"program": 50, "context": "Quantum qubit state serializer", "noncompliant": "// ------ Non-Compliant Program (050_nc.cpp)\n// Context: Quantum qubit state serializer\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_050 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_050::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (050_c.cpp)\n// Context: Quantum qubit state serializer\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_050 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_050::execute();\n    return 0;\n}\n"}
{"program": 51, "context": "5G beamforming phase shift encoder", "noncompliant": "// ------ Non-Compliant Program (051_nc.cpp)\n// Context: 5G beamforming phase shift encoder\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_051 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_051::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (051_c.cpp)\n// Context: 5G beamforming phase shift encoder\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_051 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_051::execute();\n    return 0;\n}\n"}
{"program": 52, "context": "Telecom signal field separator", "noncompliant": "// ------ Non-Compliant Program (052_nc.cpp)\n// Context: Telecom signal field separator\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_052 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_052::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (052_c.cpp)\n// Context: Telecom signal field separator\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_052 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_052::execute();\n    return 0;\n}\n"}
{"program": 53, "context": "Cryptocurrency wallet address checksum", "noncompliant": "// ------ Non-Compliant Program (053_nc.cpp)\n// Context: Cryptocurrency wallet address checksum\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_053 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_053::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (053_c.cpp)\n// Context: Cryptocurrency wallet address checksum\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_053 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_053::execute();\n    return 0;\n}\n"}
{"program": 54, "context": "Elevator floor mapping table", "noncompliant": "// ------ Non-Compliant Program (054_nc.cpp)\n// Context: Elevator floor mapping table\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_054 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_054::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (054_c.cpp)\n// Context: Elevator floor mapping table\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_054 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_054::execute();\n    return 0;\n}\n"}
{"program": 55, "context": "Neural network layer size array", "noncompliant": "// ------ Non-Compliant Program (055_nc.cpp)\n// Context: Neural network layer size array\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_055 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_055::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (055_c.cpp)\n// Context: Neural network layer size array\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_055 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_055::execute();\n    return 0;\n}\n"}
{"program": 56, "context": "Security system passcode encoder", "noncompliant": "// ------ Non-Compliant Program (056_nc.cpp)\n// Context: Security system passcode encoder\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_056 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_056::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (056_c.cpp)\n// Context: Security system passcode encoder\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_056 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_056::execute();\n    return 0;\n}\n"}
{"program": 57, "context": "Container orchestration port mapper", "noncompliant": "// ------ Non-Compliant Program (057_nc.cpp)\n// Context: Container orchestration port mapper\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_057 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_057::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (057_c.cpp)\n// Context: Container orchestration port mapper\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_057 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_057::execute();\n    return 0;\n}\n"}
{"program": 58, "context": "Pipeline sensor mode register mask", "noncompliant": "// ------ Non-Compliant Program (058_nc.cpp)\n// Context: Pipeline sensor mode register mask\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_058 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_058::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (058_c.cpp)\n// Context: Pipeline sensor mode register mask\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_058 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_058::execute();\n    return 0;\n}\n"}
{"program": 59, "context": "LIDAR point cloud resolution setter", "noncompliant": "// ------ Non-Compliant Program (059_nc.cpp)\n// Context: LIDAR point cloud resolution setter\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_059 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_059::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (059_c.cpp)\n// Context: LIDAR point cloud resolution setter\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_059 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_059::execute();\n    return 0;\n}\n"}
{"program": 60, "context": "Augmented reality marker ID generator", "noncompliant": "// ------ Non-Compliant Program (060_nc.cpp)\n// Context: Augmented reality marker ID generator\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_060 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_060::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (060_c.cpp)\n// Context: Augmented reality marker ID generator\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_060 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_060::execute();\n    return 0;\n}\n"}
{"program": 61, "context": "Power grid substation status formatter", "noncompliant": "// ------ Non-Compliant Program (061_nc.cpp)\n// Context: Power grid substation status formatter\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_061 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_061::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (061_c.cpp)\n// Context: Power grid substation status formatter\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_061 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_061::execute();\n    return 0;\n}\n"}
{"program": 62, "context": "Holographic display pixel offset calculator", "noncompliant": "// ------ Non-Compliant Program (062_nc.cpp)\n// Context: Holographic display pixel offset calculator\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_062 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_062::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (062_c.cpp)\n// Context: Holographic display pixel offset calculator\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_062 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_062::execute();\n    return 0;\n}\n"}
{"program": 63, "context": "VR headset orientation quaternion serializer", "noncompliant": "// ------ Non-Compliant Program (063_nc.cpp)\n// Context: VR headset orientation quaternion serializer\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_063 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_063::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (063_c.cpp)\n// Context: VR headset orientation quaternion serializer\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_063 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_063::execute();\n    return 0;\n}\n"}
{"program": 64, "context": "Biometric fingerprint matcher threshold", "noncompliant": "// ------ Non-Compliant Program (064_nc.cpp)\n// Context: Biometric fingerprint matcher threshold\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_064 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_064::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (064_c.cpp)\n// Context: Biometric fingerprint matcher threshold\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_064 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_064::execute();\n    return 0;\n}\n"}
{"program": 65, "context": "Genomic sequencer read length configurator", "noncompliant": "// ------ Non-Compliant Program (065_nc.cpp)\n// Context: Genomic sequencer read length configurator\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_065 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_065::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (065_c.cpp)\n// Context: Genomic sequencer read length configurator\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_065 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_065::execute();\n    return 0;\n}\n"}
{"program": 66, "context": "Voting machine ballot counter", "noncompliant": "// ------ Non-Compliant Program (066_nc.cpp)\n// Context: Voting machine ballot counter\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_066 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_066::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (066_c.cpp)\n// Context: Voting machine ballot counter\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_066 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_066::execute();\n    return 0;\n}\n"}
{"program": 67, "context": "Lottery ticket validator", "noncompliant": "// ------ Non-Compliant Program (067_nc.cpp)\n// Context: Lottery ticket validator\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_067 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_067::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (067_c.cpp)\n// Context: Lottery ticket validator\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_067 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_067::execute();\n    return 0;\n}\n"}
{"program": 68, "context": "Aquarium pH monitor", "noncompliant": "// ------ Non-Compliant Program (068_nc.cpp)\n// Context: Aquarium pH monitor\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_068 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_068::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (068_c.cpp)\n// Context: Aquarium pH monitor\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_068 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_068::execute();\n    return 0;\n}\n"}
{"program": 69, "context": "Car wash cycle timer", "noncompliant": "// ------ Non-Compliant Program (069_nc.cpp)\n// Context: Car wash cycle timer\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_069 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_069::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (069_c.cpp)\n// Context: Car wash cycle timer\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_069 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_069::execute();\n    return 0;\n}\n"}
{"program": 70, "context": "Vending machine coin acceptor", "noncompliant": "// ------ Non-Compliant Program (070_nc.cpp)\n// Context: Vending machine coin acceptor\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_070 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_070::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (070_c.cpp)\n// Context: Vending machine coin acceptor\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_070 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_070::execute();\n    return 0;\n}\n"}
{"program": 71, "context": "Chocolate tempering controller", "noncompliant": "// ------ Non-Compliant Program (071_nc.cpp)\n// Context: Chocolate tempering controller\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_071 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_071::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (071_c.cpp)\n// Context: Chocolate tempering controller\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_071 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_071::execute();\n    return 0;\n}\n"}
{"program": 72, "context": "Passport scanner page counter", "noncompliant": "// ------ Non-Compliant Program (072_nc.cpp)\n// Context: Passport scanner page counter\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_072 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_072::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (072_c.cpp)\n// Context: Passport scanner page counter\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_072 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_072::execute();\n    return 0;\n}\n"}
{"program": 73, "context": "Slot machine reel position", "noncompliant": "// ------ Non-Compliant Program (073_nc.cpp)\n// Context: Slot machine reel position\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_073 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_073::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (073_c.cpp)\n// Context: Slot machine reel position\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_073 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_073::execute();\n    return 0;\n}\n"}
{"program": 74, "context": "Planetarium star brightness", "noncompliant": "// ------ Non-Compliant Program (074_nc.cpp)\n// Context: Planetarium star brightness\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_074 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_074::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (074_c.cpp)\n// Context: Planetarium star brightness\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_074 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_074::execute();\n    return 0;\n}\n"}
{"program": 75, "context": "Kiln firing zone monitor", "noncompliant": "// ------ Non-Compliant Program (075_nc.cpp)\n// Context: Kiln firing zone monitor\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_075 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_075::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (075_c.cpp)\n// Context: Kiln firing zone monitor\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_075 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_075::execute();\n    return 0;\n}\n"}
{"program": 76, "context": "Telescope focal reducer", "noncompliant": "// ------ Non-Compliant Program (076_nc.cpp)\n// Context: Telescope focal reducer\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_076 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_076::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (076_c.cpp)\n// Context: Telescope focal reducer\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_076 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_076::execute();\n    return 0;\n}\n"}
{"program": 77, "context": "Dialysis treatment time tracker", "noncompliant": "// ------ Non-Compliant Program (077_nc.cpp)\n// Context: Dialysis treatment time tracker\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_077 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_077::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (077_c.cpp)\n// Context: Dialysis treatment time tracker\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_077 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_077::execute();\n    return 0;\n}\n"}
{"program": 78, "context": "DNA base pair counter", "noncompliant": "// ------ Non-Compliant Program (078_nc.cpp)\n// Context: DNA base pair counter\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_078 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_078::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (078_c.cpp)\n// Context: DNA base pair counter\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_078 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_078::execute();\n    return 0;\n}\n"}
{"program": 79, "context": "Insulin pump dose calculator", "noncompliant": "// ------ Non-Compliant Program (079_nc.cpp)\n// Context: Insulin pump dose calculator\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_079 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_079::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (079_c.cpp)\n// Context: Insulin pump dose calculator\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_079 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_079::execute();\n    return 0;\n}\n"}
{"program": 80, "context": "Seismograph magnitude calculator", "noncompliant": "// ------ Non-Compliant Program (080_nc.cpp)\n// Context: Seismograph magnitude calculator\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_080 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_080::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (080_c.cpp)\n// Context: Seismograph magnitude calculator\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_080 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_080::execute();\n    return 0;\n}\n"}
{"program": 81, "context": "Container ship draft sensor", "noncompliant": "// ------ Non-Compliant Program (081_nc.cpp)\n// Context: Container ship draft sensor\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_081 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_081::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (081_c.cpp)\n// Context: Container ship draft sensor\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_081 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_081::execute();\n    return 0;\n}\n"}
{"program": 82, "context": "Particle counter array sizer", "noncompliant": "// ------ Non-Compliant Program (082_nc.cpp)\n// Context: Particle counter array sizer\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_082 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_082::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (082_c.cpp)\n// Context: Particle counter array sizer\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_082 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_082::execute();\n    return 0;\n}\n"}
{"program": 83, "context": "Cyclotron beam intensity", "noncompliant": "// ------ Non-Compliant Program (083_nc.cpp)\n// Context: Cyclotron beam intensity\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_083 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_083::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (083_c.cpp)\n// Context: Cyclotron beam intensity\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_083 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_083::execute();\n    return 0;\n}\n"}
{"program": 84, "context": "Geothermal well depth gauge", "noncompliant": "// ------ Non-Compliant Program (084_nc.cpp)\n// Context: Geothermal well depth gauge\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_084 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_084::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (084_c.cpp)\n// Context: Geothermal well depth gauge\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_084 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_084::execute();\n    return 0;\n}\n"}
{"program": 85, "context": "Desalination membrane pressure", "noncompliant": "// ------ Non-Compliant Program (085_nc.cpp)\n// Context: Desalination membrane pressure\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_085 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_085::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (085_c.cpp)\n// Context: Desalination membrane pressure\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_085 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_085::execute();\n    return 0;\n}\n"}
{"program": 86, "context": "Electron microscope magnification", "noncompliant": "// ------ Non-Compliant Program (086_nc.cpp)\n// Context: Electron microscope magnification\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_086 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_086::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (086_c.cpp)\n// Context: Electron microscope magnification\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_086 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_086::execute();\n    return 0;\n}\n"}
{"program": 87, "context": "Photovoltaic panel string count", "noncompliant": "// ------ Non-Compliant Program (087_nc.cpp)\n// Context: Photovoltaic panel string count\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_087 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_087::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (087_c.cpp)\n// Context: Photovoltaic panel string count\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_087 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_087::execute();\n    return 0;\n}\n"}
{"program": 88, "context": "CNC toolchanger position", "noncompliant": "// ------ Non-Compliant Program (088_nc.cpp)\n// Context: CNC toolchanger position\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_088 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_088::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (088_c.cpp)\n// Context: CNC toolchanger position\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_088 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_088::execute();\n    return 0;\n}\n"}
{"program": 89, "context": "Tsunami buoy sampling interval", "noncompliant": "// ------ Non-Compliant Program (089_nc.cpp)\n// Context: Tsunami buoy sampling interval\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_089 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_089::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (089_c.cpp)\n// Context: Tsunami buoy sampling interval\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_089 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_089::execute();\n    return 0;\n}\n"}
{"program": 90, "context": "Tokamak magnetic field coils", "noncompliant": "// ------ Non-Compliant Program (090_nc.cpp)\n// Context: Tokamak magnetic field coils\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_090 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_090::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (090_c.cpp)\n// Context: Tokamak magnetic field coils\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_090 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_090::execute();\n    return 0;\n}\n"}
{"program": 91, "context": "Bioreactor vessel capacity", "noncompliant": "// ------ Non-Compliant Program (091_nc.cpp)\n// Context: Bioreactor vessel capacity\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_091 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_091::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (091_c.cpp)\n// Context: Bioreactor vessel capacity\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_091 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_091::execute();\n    return 0;\n}\n"}
{"program": 92, "context": "Brewery batch identifier", "noncompliant": "// ------ Non-Compliant Program (092_nc.cpp)\n// Context: Brewery batch identifier\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_092 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_092::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (092_c.cpp)\n// Context: Brewery batch identifier\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_092 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_092::execute();\n    return 0;\n}\n"}
{"program": 93, "context": "Espresso grinder step count", "noncompliant": "// ------ Non-Compliant Program (093_nc.cpp)\n// Context: Espresso grinder step count\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_093 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_093::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (093_c.cpp)\n// Context: Espresso grinder step count\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_093 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_093::execute();\n    return 0;\n}\n"}
{"program": 94, "context": "Plasma cutter arc voltage", "noncompliant": "// ------ Non-Compliant Program (094_nc.cpp)\n// Context: Plasma cutter arc voltage\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_094 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_094::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (094_c.cpp)\n// Context: Plasma cutter arc voltage\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_094 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_094::execute();\n    return 0;\n}\n"}
{"program": 95, "context": "Wind tunnel airspeed calibrator", "noncompliant": "// ------ Non-Compliant Program (095_nc.cpp)\n// Context: Wind tunnel airspeed calibrator\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_095 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_095::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (095_c.cpp)\n// Context: Wind tunnel airspeed calibrator\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_095 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_095::execute();\n    return 0;\n}\n"}
{"program": 96, "context": "Inkjet printer nozzle controller", "noncompliant": "// ------ Non-Compliant Program (096_nc.cpp)\n// Context: Inkjet printer nozzle controller\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_096 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_096::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (096_c.cpp)\n// Context: Inkjet printer nozzle controller\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_096 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_096::execute();\n    return 0;\n}\n"}
{"program": 97, "context": "Snowmaking machine temperature sensor", "noncompliant": "// ------ Non-Compliant Program (097_nc.cpp)\n// Context: Snowmaking machine temperature sensor\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_097 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_097::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (097_c.cpp)\n// Context: Snowmaking machine temperature sensor\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_097 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_097::execute();\n    return 0;\n}\n"}
{"program": 98, "context": "Fire sprinkler zone activator", "noncompliant": "// ------ Non-Compliant Program (098_nc.cpp)\n// Context: Fire sprinkler zone activator\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_098 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_098::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (098_c.cpp)\n// Context: Fire sprinkler zone activator\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_098 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_098::execute();\n    return 0;\n}\n"}
{"program": 99, "context": "Arcade game score multiplier", "noncompliant": "// ------ Non-Compliant Program (099_nc.cpp)\n// Context: Arcade game score multiplier\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_099 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_099::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (099_c.cpp)\n// Context: Arcade game score multiplier\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_099 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_099::execute();\n    return 0;\n}\n"}
{"program": 100, "context": "Piano key pressure sensor", "noncompliant": "// ------ Non-Compliant Program (100_nc.cpp)\n// Context: Piano key pressure sensor\n// MISRA C++ Rule: Octal constants/escapes not allowed\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_100 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {010, 020, 0100, 0200};  // NC: octal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 010, 020, 0100, 0200\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_100::execute();\n    return 0;\n}\n", "compliant": "// ------ Compliant Program (100_c.cpp)\n// Context: Piano key pressure sensor\n// MISRA C++ Compliant: Decimal/hex constants only\n#include <iostream>\n#include <iomanip>\n#include <array>\n#include <fstream>\n\nnamespace oct_100 {\n    struct DataPoint {\n        int id;\n        int value;\n        bool processed;\n    };\n    \n    static int processValue(int input, int factor) {\n        return input * factor + (input / 2);\n    }\n    \n    static bool validateRange(int val, int min, int max) {\n        return (val >= min) && (val <= max);\n    }\n    \n    static void logDataPoint(const DataPoint& dp) {\n        std::cout << \"[DATA] id=\" << dp.id \n                 << \" value=\" << dp.value\n                 << \" processed=\" << (dp.processed ? \"yes\" : \"no\")\n                 << std::endl;\n    }\n    \n    void execute() {\n        int configValues[] = {8, 16, 64, 128};  // C: decimal constants\n        \n        std::array<DataPoint, 12> dataset;\n        unsigned successCount = 0U;\n        unsigned failCount = 0U;\n        int totalProcessed = 0;\n        \n        std::cout << \"Starting data processing...\" << std::endl;\n        std::cout << \"Configuration values: 8, 16, 64, 128\" << std::endl;\n        std::cout << std::endl;\n        \n        for (size_t i = 0; i < dataset.size(); ++i) {\n            int baseValue = configValues[i % 4];\n            int processed = processValue(baseValue, 2);\n            \n            dataset[i].id = static_cast<int>(i);\n            dataset[i].value = processed;\n            dataset[i].processed = validateRange(processed, 0, 2000);\n            \n            if (dataset[i].processed) {\n                successCount++;\n                totalProcessed += processed;\n            } else {\n                failCount++;\n            }\n            \n            if ((i % 3) == 0) {\n                logDataPoint(dataset[i]);\n            }\n        }\n        \n        double average = (successCount > 0) ? \n                        (static_cast<double>(totalProcessed) / successCount) : 0.0;\n        \n        std::cout << \"\\nProcessing Summary:\" << std::endl;\n        std::cout << \"  Successful:  \" << successCount << std::endl;\n        std::cout << \"  Failed:      \" << failCount << std::endl;\n        std::cout << \"  Total:       \" << dataset.size() << std::endl;\n        std::cout << \"  Average val: \" << std::fixed << std::setprecision(2) << average << std::endl;\n        \n        std::ofstream logFile(\"processing.log\");\n        if (logFile.is_open()) {\n            logFile << \"Processing complete: \" << successCount << \" success, \" \n                   << failCount << \" failed\" << std::endl;\n            logFile.close();\n            std::cout << \"Log file written successfully\" << std::endl;\n        }\n    }\n}\n\nint main() {\n    oct_100::execute();\n    return 0;\n}\n"}
