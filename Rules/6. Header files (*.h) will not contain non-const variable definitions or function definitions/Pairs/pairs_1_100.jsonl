{"program":1,"context":"Battery management system threshold config header","noncompliant":"// -------- Non-Compliant Program (prog_001_nc.cpp)\n// Context: Battery management system threshold configuration\n// Rule violated: Header contains non-const variable definitions + function definitions\n#include <iostream>\n#include <iomanip>\n#include <cstddef>\n#include \"seedNC.h\"  // uses seed001_low_voltage, seed001_high_voltage, seed001_compute_range()\n\nnamespace bms_001_nc {\n\nstatic float clamp(float v, float lo, float hi) { // helper\n    return (v < lo) ? lo : (v > hi ? hi : v);\n}\n\nvoid analyze(float arr[], std::size_t n) { // NC: decayed array\n    std::cout << std::fixed << std::setprecision(2);\n    for (std::size_t i = 0; i < n; ++i) {\n        float v = clamp(arr[i], 2.5F, 4.5F);\n        float scaled = seed001_compute_range(v);\n        std::cout << \"cell[\" << i << \"]=\" << v;\n        if (v < seed001_low_voltage) std::cout << \" LOW\";\n        if (v > seed001_high_voltage) std::cout << \" HIGH\";\n        std::cout << \" scaled=\" << scaled << \"\\n\";\n    }\n}\n\nfloat average(float arr[], std::size_t n) { // NC again (decay)\n    float sum = 0.0F;\n    for (std::size_t i = 0; i < n; ++i) sum += arr[i];\n    return (n > 0U) ? sum / n : 0.0F;\n}\n\n} // namespace bms_001_nc\n\nint main() {\n    using namespace bms_001_nc;\n    float cells[5] = {3.28F, 3.12F, 3.40F, 4.35F, 3.90F};\n    analyze(cells, 5);\n    std::cout << \"avg=\" << average(cells, 5) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_001_c.cpp)\n// Context: Battery management system threshold configuration\n// COMPLIANT: Header has only extern declarations. Definitions are provided here.\n#include <iostream>\n#include <iomanip>\n#include <cstddef>\n#include \"seedC.h\"  // extern: seed001_low_voltage, seed001_high_voltage, seed001_compute_range()\n\n// Provide definitions in THIS TU\nfloat seed001_low_voltage = 3.2F;\nfloat seed001_high_voltage = 4.2F;\nfloat seed001_compute_range(float v) { return (v - seed001_low_voltage) * 10.0F; }\n\nnamespace bms_001_c {\n\nstatic float clamp(float v, float lo, float hi) {\n    return (v < lo) ? lo : (v > hi ? hi : v);\n}\n\ntemplate <std::size_t N>\nvoid analyze(const float (&arr)[N]) {\n    std::cout << std::fixed << std::setprecision(2);\n    for (std::size_t i = 0; i < N; ++i) {\n        float v = clamp(arr[i], 2.5F, 4.5F);\n        float scaled = seed001_compute_range(v);\n        std::cout << \"cell[\" << i << \"]=\" << v;\n        if (v < seed001_low_voltage) std::cout << \" LOW\";\n        if (v > seed001_high_voltage) std::cout << \" HIGH\";\n        std::cout << \" scaled=\" << scaled << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nfloat average(const float (&arr)[N]) {\n    float sum = 0.0F;\n    for (float v : arr) sum += v;\n    return (N>0U) ? sum / N : 0.0F;\n}\n\n} // namespace bms_001_c\n\nint main() {\n    using namespace bms_001_c;\n    const float cells[5] = {3.28F, 3.12F, 3.40F, 4.35F, 3.90F};\n    analyze(cells);\n    std::cout << \"avg=\" << average(cells) << \"\\n\";\n    return 0;\n}\n"}
{"program":2,"context":"Robotic arm kinematics parameters","noncompliant":"// -------- Non-Compliant Program (prog_002_nc.cpp)\n// Context: Robotic arm kinematics\n// Rule violated: Header defines variables + function bodies\n#include <iostream>\n#include <cmath>\n#include \"seedNC.h\"  // seed002_link1, seed002_link2, seed002_calc_reach()\n\nnamespace kinematics_nc {\n\ndouble compute_joint_extension(double theta) {\n    double r = seed002_calc_reach(); // uses NC definition from header\n    return r * std::cos(theta);\n}\n\ndouble compute_projection(double theta) {\n    return seed002_link1 * std::sin(theta) + seed002_link2 * std::cos(theta);\n}\n\nvoid simulate(double angles[], std::size_t n) { // NC: decayed array\n    for (std::size_t i = 0; i < n; ++i) {\n        double a = angles[i];\n        std::cout << \"theta=\" << a\n                  << \" proj=\" << compute_projection(a)\n                  << \" ext=\" << compute_joint_extension(a) << \"\\n\";\n    }\n}\n\n} // namespace kinematics_nc\n\nint main() {\n    using namespace kinematics_nc;\n    double thetas[4] = {0.0, 0.25, 0.5, 0.75};\n    simulate(thetas, 4);\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_002_c.cpp)\n// Context: Robotic arm kinematics\n// COMPLIANT: Only includes declarations. Definitions placed in this TU.\n#include <iostream>\n#include <cmath>\n#include \"seedC.h\"  // extern seed002_link1, seed002_link2, seed002_calc_reach()\n\n// Provide definitions\ndouble seed002_link1 = 0.55;\ndouble seed002_link2 = 0.40;\ndouble seed002_calc_reach() { return seed002_link1 + seed002_link2; }\n\nnamespace kinematics_c {\n\ntemplate <typename T>\nT compute_joint_extension(T theta) {\n    double r = seed002_calc_reach();\n    return r * std::cos(theta);\n}\n\ntemplate <typename T>\nT compute_projection(T theta) {\n    return seed002_link1 * std::sin(theta) + seed002_link2 * std::cos(theta);\n}\n\ntemplate <std::size_t N>\nvoid simulate(const double (&angles)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        double a = angles[i];\n        std::cout << \"theta=\" << a\n                  << \" proj=\" << compute_projection(a)\n                  << \" ext=\" << compute_joint_extension(a) << \"\\n\";\n    }\n}\n\n} // namespace kinematics_c\n\nint main() {\n    using namespace kinematics_c;\n    const double thetas[4] = {0.0, 0.25, 0.5, 0.75};\n    simulate(thetas);\n    return 0;\n}\n"}
{"program":3,"context":"Sensor fusion filter gains","noncompliant":"// -------- Non-Compliant Program (prog_003_nc.cpp)\n// Context: Sensor fusion filter gains\n#include <iostream>\n#include <cstddef>\n#include \"seedNC.h\" // seed003_alpha, seed003_beta, seed003_filter()\n\nnamespace fusion_nc {\n\nvoid apply_filters(double p[], double m[], std::size_t n) { // NC: decayed arrays\n    for (std::size_t i = 0; i < n; ++i) {\n        double out = seed003_filter(p[i], m[i]);\n        std::cout << \"filtered[\" << i << \"]=\" << out << \"\\n\";\n    }\n}\n\ndouble error_metric(double vals[], std::size_t n) { // NC\n    double s = 0.0;\n    for (std::size_t i = 0; i < n; ++i) s += vals[i];\n    return (n > 0U) ? s / n : 0.0;\n}\n\n} // namespace fusion_nc\n\nint main() {\n    using namespace fusion_nc;\n    double pred[4] = {1.0, 2.0, 3.0, 4.0};\n    double meas[4] = {0.8, 1.9, 3.1, 4.2};\n    apply_filters(pred, meas, 4);\n    std::cout << \"err=\" << error_metric(pred, 4) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_003_c.cpp)\n// Context: Sensor fusion filter gains — COMPLIANT\n#include <iostream>\n#include <cstddef>\n#include \"seedC.h\"\n\n// Definitions\ndouble seed003_alpha = 0.98;\ndouble seed003_beta  = 0.02;\ndouble seed003_filter(double p, double m) { return seed003_alpha * p + seed003_beta * m; }\n\nnamespace fusion_c {\n\ntemplate <std::size_t N>\nvoid apply_filters(const double (&p)[N], const double (&m)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        double out = seed003_filter(p[i], m[i]);\n        std::cout << \"filtered[\" << i << \"]=\" << out << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\ndouble error_metric(const double (&vals)[N]) {\n    double s = 0.0;\n    for (double v : vals) s += v;\n    return (N > 0U) ? s / N : 0.0;\n}\n\n} // namespace fusion_c\n\nint main() {\n    using namespace fusion_c;\n    const double pred[4] = {1.0, 2.0, 3.0, 4.0};\n    const double meas[4] = {0.8, 1.9, 3.1, 4.2};\n    apply_filters(pred, meas);\n    std::cout << \"err=\" << error_metric(pred) << \"\\n\";\n    return 0;\n}\n"}
{"program":4,"context":"Network packet parser configuration","noncompliant":"// -------- Non-Compliant Program (prog_004_nc.cpp)\n// Context: Network packet parser\n#include <iostream>\n#include <cstdint>\n#include <cstddef>\n#include \"seedNC.h\" // seed004_magic_header, seed004_is_valid()\n\nnamespace net_nc {\n\nvoid parse_packets(std::uint8_t bytes[], std::size_t n) { // NC array decay\n    for (std::size_t i = 0; i < n; ++i) {\n        bool ok = seed004_is_valid(bytes[i]);\n        std::cout << \"b[\" << i << \"]=\" << (int)bytes[i]\n                  << \" valid=\" << (ok ? \"Y\" : \"N\") << \"\\n\";\n    }\n}\n\nint count_valid(std::uint8_t b[], std::size_t n) { // NC\n    int c = 0;\n    for (std::size_t i = 0; i < n; ++i)\n        if (seed004_is_valid(b[i])) ++c;\n    return c;\n}\n\n} // namespace net_nc\n\nint main() {\n    using namespace net_nc;\n    std::uint8_t data[6] = {0xDE,0x11,0xAD,0x20,0xBE,0xEF};\n    parse_packets(data, 6);\n    std::cout << \"valid_count=\" << count_valid(data,6) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_004_c.cpp)\n// Context: Network packet parser — COMPLIANT\n#include <iostream>\n#include <cstdint>\n#include <cstddef>\n#include \"seedC.h\"\n\n// Definitions\nstd::uint8_t seed004_magic_header[4] = {0xDE,0xAD,0xBE,0xEF};\nbool seed004_is_valid(std::uint8_t b) { return b == seed004_magic_header[0]; }\n\nnamespace net_c {\n\ntemplate <std::size_t N>\nvoid parse_packets(const std::uint8_t (&bytes)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        bool ok = seed004_is_valid(bytes[i]);\n        std::cout << \"b[\" << i << \"]=\" << (int)bytes[i]\n                  << \" valid=\" << (ok ? \"Y\" : \"N\") << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint count_valid(const std::uint8_t (&bytes)[N]) {\n    int c = 0;\n    for (auto b : bytes)\n        if (seed004_is_valid(b)) ++c;\n    return c;\n}\n\n} // namespace net_c\n\nint main() {\n    using namespace net_c;\n    const std::uint8_t data[6] = {0xDE,0x11,0xAD,0x20,0xBE,0xEF};\n    parse_packets(data);\n    std::cout << \"valid_count=\" << count_valid(data) << \"\\n\";\n    return 0;\n}\n"}
{"program":5,"context":"Drone flight PID tuning parameters","noncompliant":"// -------- Non-Compliant Program (prog_005_nc.cpp)\n// Context: Drone PID tuning\n#include <iostream>\n#include <iomanip>\n#include <cstddef>\n#include \"seedNC.h\" // seed005_kp, seed005_ki, seed005_kd, seed005_pid()\n\nnamespace pid_nc {\n\ndouble compute_derivative(double e_curr, double e_prev) {\n    return (e_curr - e_prev) * seed005_kd; // uses NC global\n}\n\ndouble compute_integral(double acc, double e) {\n    return acc + (e * seed005_ki); // uses NC global\n}\n\nvoid run_pid(float e[], std::size_t n) { // NC decayed array\n    double integral = 0.0;\n    double prev = e[0];\n    for (std::size_t i = 0; i < n; ++i) {\n        integral = compute_integral(integral, e[i]);\n        double d = compute_derivative(e[i], prev);\n        double out = seed005_pid(e[i]) + d + integral;\n        prev = e[i];\n        std::cout << \"step=\" << i << \" output=\" << out << \"\\n\";\n    }\n}\n\n} // namespace pid_nc\n\nint main() {\n    using namespace pid_nc;\n    float err[5] = {0.2F, 0.1F, -0.05F, -0.1F, 0.0F};\n    run_pid(err, 5);\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_005_c.cpp)\n// Context: Drone PID tuning — COMPLIANT\n#include <iostream>\n#include <iomanip>\n#include <cstddef>\n#include \"seedC.h\"\n\n// Definitions\nfloat seed005_kp = 1.1F;\nfloat seed005_ki = 0.03F;\nfloat seed005_kd = 0.55F;\nfloat seed005_pid(float e) { return seed005_kp * e; }\n\nnamespace pid_c {\n\ntemplate <typename T>\nT compute_derivative(T e_curr, T e_prev) {\n    return (e_curr - e_prev) * seed005_kd;\n}\n\ntemplate <typename T>\nT compute_integral(T acc, T e) {\n    return acc + (e * seed005_ki);\n}\n\ntemplate <std::size_t N>\nvoid run_pid(const float (&e)[N]) {\n    double integral = 0.0;\n    double prev = e[0];\n    for (std::size_t i = 0; i < N; ++i) {\n        integral = compute_integral(integral, e[i]);\n        double d = compute_derivative(e[i], prev);\n        double out = seed005_pid(e[i]) + d + integral;\n        prev = e[i];\n        std::cout << \"step=\" << i << \" output=\" << out << \"\\n\";\n    }\n}\n\n} // namespace pid_c\n\nint main() {\n    using namespace pid_c;\n    const float err[5] = {0.2F, 0.1F, -0.05F, -0.1F, 0.0F};\n    run_pid(err);\n    return 0;\n}\n"}
{"program":6,"context":"IoT device boot configuration flags","noncompliant":"// -------- Non-Compliant Program (prog_006_nc.cpp)\n// Context: IoT device boot configuration flags\n#include <iostream>\n#include <string>\n#include <cstddef>\n#include \"seedNC.h\"  // NC: defines seed006_boot_mode, seed006_debug_enabled, seed006_is_debug()\n\nnamespace boot_nc {\n\nstd::string mode_name(int m) {\n    switch(m) {\n        case 0: return \"OFF\";\n        case 1: return \"SAFE\";\n        case 2: return \"NORMAL\";\n        default: return \"UNKNOWN\";\n    }\n}\n\nvoid print_status() {\n    std::cout << \"Boot Mode=\" << mode_name(seed006_boot_mode)\n              << \" Debug=\" << (seed006_is_debug() ? \"ON\" : \"OFF\")\n              << \"\\n\";\n}\n\nvoid toggle_debug() {\n    // Mutates global defined in header (NC)\n    seed006_debug_enabled = !seed006_debug_enabled;\n}\n\nvoid run_sequence(int seq[], std::size_t n) { // NC array decay\n    for (std::size_t i = 0; i < n; ++i) {\n        seed006_boot_mode = seq[i];  // modifies header-defined global\n        print_status();\n        toggle_debug();\n    }\n}\n\n} // namespace boot_nc\n\nint main() {\n    using namespace boot_nc;\n    int seq[4] = {2,1,0,2};\n    run_sequence(seq, 4);\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_006_c.cpp)\n// Context: IoT device boot configuration flags\n// COMPLIANT: header contains only extern declarations; definitions provided here.\n#include <iostream>\n#include <string>\n#include <cstddef>\n#include \"seedC.h\"\n\n// Definitions\nint seed006_boot_mode = 2;\nbool seed006_debug_enabled = false;\nbool seed006_is_debug() { return seed006_debug_enabled; }\n\nnamespace boot_c {\n\nstd::string mode_name(int m) {\n    switch(m) {\n        case 0: return \"OFF\";\n        case 1: return \"SAFE\";\n        case 2: return \"NORMAL\";\n        default: return \"UNKNOWN\";\n    }\n}\n\ntemplate <std::size_t N>\nvoid run_sequence(const int (&seq)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        seed006_boot_mode = seq[i];\n        std::cout << \"Boot Mode=\" << mode_name(seed006_boot_mode)\n                  << \" Debug=\" << (seed006_is_debug() ? \"ON\" : \"OFF\")\n                  << \"\\n\";\n        seed006_debug_enabled = !seed006_debug_enabled;\n    }\n}\n\n} // namespace boot_c\n\nint main() {\n    using namespace boot_c;\n    const int seq[4] = {2,1,0,2};\n    run_sequence(seq);\n    return 0;\n}\n"}
{"program":7,"context":"Image processing kernel parameters","noncompliant":"// -------- Non-Compliant Program (prog_007_nc.cpp)\n// Context: Image processing kernel configuration\n#include <iostream>\n#include <vector>\n#include <iomanip>\n#include \"seedNC.h\"  // seed007_kernel_size, seed007_kernel_scale, seed007_scale_val()\n\nnamespace img_nc {\n\nfloat apply_kernel(float px) {\n    return seed007_scale_val(px) * seed007_kernel_size;\n}\n\nvoid process(float* buf, std::size_t n) { // NC decay\n    for (std::size_t i = 0; i < n; ++i) {\n        float v = apply_kernel(buf[i]);\n        std::cout << \"p[\" << i << \"]=\" << std::fixed << std::setprecision(4) << v << \"\\n\";\n    }\n}\n\nfloat average(float* buf, std::size_t n) { // NC\n    float s = 0.0F;\n    for (std::size_t i = 0; i < n; ++i) s += buf[i];\n    return (n>0) ? s/n : 0.0F;\n}\n\n} // namespace img_nc\n\nint main() {\n    using namespace img_nc;\n    float data[5] = {0.2F,0.4F,0.6F,0.8F,1.0F};\n    process(data,5);\n    std::cout << \"avg=\" << average(data,5) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_007_c.cpp)\n// Context: Image processing kernel configuration\n#include <iostream>\n#include <vector>\n#include <iomanip>\n#include \"seedC.h\"\n\n// Definitions\nint   seed007_kernel_size  = 5;\nfloat seed007_kernel_scale = 0.125F;\nfloat seed007_scale_val(float v) { return v * seed007_kernel_scale; }\n\nnamespace img_c {\n\ntemplate <std::size_t N>\nvoid process(const float (&buf)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        float v = seed007_scale_val(buf[i]) * seed007_kernel_size;\n        std::cout << \"p[\" << i << \"]=\" << std::fixed << std::setprecision(4) << v << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nfloat average(const float (&buf)[N]) {\n    float s = 0.0F;\n    for (float v : buf) s += v;\n    return (N>0) ? s/N : 0.0F;\n}\n\n} // namespace img_c\n\nint main() {\n    using namespace img_c;\n    const float data[5] = {0.2F,0.4F,0.6F,0.8F,1.0F};\n    process(data);\n    std::cout << \"avg=\" << average(data) << \"\\n\";\n    return 0;\n}\n"}
{"program":8,"context":"GNSS coordinate conversion parameters","noncompliant":"// -------- Non-Compliant Program (prog_008_nc.cpp)\n// Context: GNSS coordinate conversion\n#include <iostream>\n#include <cmath>\n#include \"seedNC.h\"  // seed008_deg_to_rad, seed008_to_radians()\n\nnamespace gnss_nc {\n\nvoid convert(double angles[], std::size_t n) { // NC decay\n    for (std::size_t i = 0; i < n; ++i) {\n        double r = seed008_to_radians(angles[i]);\n        std::cout << angles[i] << \" deg -> \" << r << \" rad\\n\";\n    }\n}\n\ndouble sum(double arr[], std::size_t n) { // NC\n    double s = 0;\n    for (std::size_t i = 0; i < n; ++i) s += arr[i];\n    return s;\n}\n\n} // namespace gnss_nc\n\nint main() {\n    using namespace gnss_nc;\n    double vals[4] = {0.0,90.0,180.0,270.0};\n    convert(vals,4);\n    std::cout << \"sum=\" << sum(vals,4) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_008_c.cpp)\n// Context: GNSS coordinate conversion — COMPLIANT\n#include <iostream>\n#include <cmath>\n#include \"seedC.h\"\n\n// Definitions\ndouble seed008_deg_to_rad = 0.01745329252;\ndouble seed008_to_radians(double d) { return d * seed008_deg_to_rad; }\n\nnamespace gnss_c {\n\ntemplate <std::size_t N>\nvoid convert(const double (&angles)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        double r = seed008_to_radians(angles[i]);\n        std::cout << angles[i] << \" deg -> \" << r << \" rad\\n\";\n    }\n}\n\ntemplate <std::size_t N>\ndouble sum(const double (&arr)[N]) {\n    double s = 0;\n    for (double v : arr) s += v;\n    return s;\n}\n\n} // namespace gnss_c\n\nint main() {\n    using namespace gnss_c;\n    const double vals[4] = {0.0,90.0,180.0,270.0};\n    convert(vals);\n    std::cout << \"sum=\" << sum(vals) << \"\\n\";\n    return 0;\n}\n"}
{"program":9,"context":"Home automation lighting profile","noncompliant":"// -------- Non-Compliant Program (prog_009_nc.cpp)\n// Context: Home automation lighting profile\n#include <iostream>\n#include <string>\n#include \"seedNC.h\" // seed009_default_level, enum seed009_mode, seed009_get_level()\n\nnamespace light_nc {\n\nstd::string mode_name(seed009_mode m) {\n    return (m == SEED009_DAY) ? \"DAY\" : \"NIGHT\";\n}\n\nvoid show_levels(seed009_mode modes[], std::size_t n) { // NC decay\n    for (std::size_t i = 0; i < n; ++i) {\n        int v = seed009_get_level(modes[i]);\n        std::cout << \"mode=\" << mode_name(modes[i])\n                  << \" level=\" << v << \"\\n\";\n    }\n}\n\nint sum_levels(seed009_mode modes[], std::size_t n) { // NC\n    int s = 0;\n    for (std::size_t i = 0; i < n; ++i) s += seed009_get_level(modes[i]);\n    return s;\n}\n\n} // namespace light_nc\n\nint main() {\n    using namespace light_nc;\n    seed009_mode arr[4] = {SEED009_DAY,SEED009_NIGHT,SEED009_NIGHT,SEED009_DAY};\n    show_levels(arr,4);\n    std::cout << \"sum=\" << sum_levels(arr,4) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_009_c.cpp)\n// Context: Home automation lighting profile — COMPLIANT\n#include <iostream>\n#include <string>\n#include \"seedC.h\"\n\n// Definitions\nint seed009_default_level = 75;\nint seed009_get_level(seed009_mode m) {\n    return (m == SEED009_DAY) ? 100 : seed009_default_level;\n}\n\nnamespace light_c {\n\nstd::string mode_name(seed009_mode m) {\n    return (m == SEED009_DAY) ? \"DAY\" : \"NIGHT\";\n}\n\ntemplate <std::size_t N>\nvoid show_levels(const seed009_mode (&modes)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        int v = seed009_get_level(modes[i]);\n        std::cout << \"mode=\" << mode_name(modes[i])\n                  << \" level=\" << v << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint sum_levels(const seed009_mode (&modes)[N]) {\n    int s = 0;\n    for (auto m : modes) s += seed009_get_level(m);\n    return s;\n}\n\n} // namespace light_c\n\nint main() {\n    using namespace light_c;\n    const seed009_mode arr[4] = {SEED009_DAY,SEED009_NIGHT,SEED009_NIGHT,SEED009_DAY};\n    show_levels(arr);\n    std::cout << \"sum=\" << sum_levels(arr) << \"\\n\";\n    return 0;\n}\n"}
{"program":10,"context":"Autonomous vehicle lane model parameters","noncompliant":"// -------- Non-Compliant Program (prog_010_nc.cpp)\n// Context: Autonomous vehicle lane curvature model\n#include <iostream>\n#include <iomanip>\n#include <cstddef>\n#include \"seedNC.h\" // seed010_lane_width, seed010_curvature()\n\nnamespace lane_nc {\n\nfloat estimate(float r) {\n    return seed010_curvature(r) * 100.0F;\n}\n\nvoid scan(float radii[], std::size_t n) { // NC decay\n    for (std::size_t i = 0; i < n; ++i) {\n        float c = estimate(radii[i]);\n        std::cout << \"r=\" << radii[i] << \" curv=\" << c << \"\\n\";\n    }\n}\n\nfloat sum(float vals[], std::size_t n) { // NC\n    float s = 0.0F;\n    for (std::size_t i = 0; i < n; ++i) s += vals[i];\n    return s;\n}\n\n} // namespace lane_nc\n\nint main() {\n    using namespace lane_nc;\n    float r[5] = {5.0F,10.0F,15.0F,20.0F,25.0F};\n    scan(r,5);\n    std::cout << \"sum=\" << sum(r,5) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_010_c.cpp)\n// Context: Autonomous vehicle lane curvature model — COMPLIANT\n#include <iostream>\n#include <iomanip>\n#include <cstddef>\n#include \"seedC.h\"\n\n// Definitions\nfloat seed010_lane_width = 3.7F;\nfloat seed010_curvature(float r) { return 1.0F / (r + seed010_lane_width); }\n\nnamespace lane_c {\n\ntemplate <std::size_t N>\nvoid scan(const float (&radii)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        float c = seed010_curvature(radii[i]) * 100.0F;\n        std::cout << \"r=\" << radii[i] << \" curv=\" << c << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nfloat sum(const float (&vals)[N]) {\n    float s = 0.0F;\n    for (float v : vals) s += v;\n    return s;\n}\n\n} // namespace lane_c\n\nint main() {\n    using namespace lane_c;\n    const float r[5] = {5.0F,10.0F,15.0F,20.0F,25.0F};\n    scan(r);\n    std::cout << \"sum=\" << sum(r) << \"\\n\";\n    return 0;\n}\n"}
{"program":11,"context":"Audio DSP filter coefficients header","noncompliant":"// -------- Non-Compliant Program (prog_011_nc.cpp)\n// Context: Audio DSP filter coefficients\n#include <iostream>\n#include <iomanip>\n#include <cstddef>\n#include \"seedNC.h\" // defines seed011_coeffs[], seed011_apply()\n\nnamespace dsp_nc {\n\nvoid print_coeffs() {\n    std::cout << \"coeffs:\";\n    for (std::size_t i = 0; i < 5; ++i) std::cout << ' ' << seed011_coeffs[i];\n    std::cout << \"\\n\";\n}\n\nfloat process_buffer(float buf[], std::size_t n) { // NC: decayed array\n    float peak = 0.0F;\n    for (std::size_t i = 0; i < n; ++i) {\n        float y = seed011_apply(buf[i]); // uses function defined in header (NC)\n        if (y > peak) peak = y;\n    }\n    return peak;\n}\n\n} // namespace dsp_nc\n\nint main() {\n    using namespace dsp_nc;\n    float samples[8] = {0.1F,0.2F,0.3F,0.4F,0.25F,0.15F,0.05F,0.0F};\n    print_coeffs();\n    std::cout << std::fixed << std::setprecision(4)\n              << \"peak=\" << process_buffer(samples, 8) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_011_c.cpp)\n// Context: Audio DSP filter coefficients — COMPLIANT\n#include <iostream>\n#include <iomanip>\n#include <cstddef>\n#include \"seedC.h\" // extern seed011_coeffs[], seed011_apply()\n\n// Definitions\nfloat seed011_coeffs[5] = {0.2F,0.2F,0.2F,0.2F,0.2F};\nfloat seed011_apply(float x) { return x * seed011_coeffs[0]; }\n\nnamespace dsp_c {\n\ntemplate <std::size_t N>\nvoid print_coeffs(const float (&coeffs)[N]) {\n    std::cout << \"coeffs:\";\n    for (std::size_t i = 0; i < N; ++i) std::cout << ' ' << coeffs[i];\n    std::cout << \"\\n\";\n}\n\ntemplate <std::size_t N>\nfloat process_buffer(const float (&buf)[N]) {\n    float peak = 0.0F;\n    for (auto v : buf) {\n        float y = seed011_apply(v);\n        if (y > peak) peak = y;\n    }\n    return peak;\n}\n\n} // namespace dsp_c\n\nint main() {\n    using namespace dsp_c;\n    const float samples[8] = {0.1F,0.2F,0.3F,0.4F,0.25F,0.15F,0.05F,0.0F};\n    print_coeffs(seed011_coeffs);\n    std::cout << std::fixed << std::setprecision(4)\n              << \"peak=\" << process_buffer(samples) << \"\\n\";\n    return 0;\n}\n"}
{"program":12,"context":"Power management regulator default settings","noncompliant":"// -------- Non-Compliant Program (prog_012_nc.cpp)\n// Context: Power management regulator defaults\n#include <iostream>\n#include <cstddef>\n#include \"seedNC.h\" // seed012_vout_default, seed012_get_safe_margin()\n\nnamespace pm_nc {\n\nvoid print_defaults() {\n    std::cout << \"Vout default=\" << seed012_vout_default << \" mV\\n\";\n    std::cout << \"safe margin=\" << seed012_get_safe_margin() << \" mV\\n\";\n}\n\nint check_and_adjust(int target_mv) { // NC\n    int margin = seed012_get_safe_margin(); // function in header\n    if (target_mv > margin) return margin;\n    return target_mv;\n}\n\n} // namespace pm_nc\n\nint main() {\n    using namespace pm_nc;\n    print_defaults();\n    int set_mv = check_and_adjust(3400);\n    std::cout << \"set_mv=\" << set_mv << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_012_c.cpp)\n// Context: Power management regulator defaults — COMPLIANT\n#include <iostream>\n#include <cstddef>\n#include \"seedC.h\" // extern seed012_vout_default, seed012_get_safe_margin()\n\n// Definitions\nint seed012_vout_default = 3300; // mV\nint seed012_get_safe_margin() { return seed012_vout_default - 100; }\n\nnamespace pm_c {\n\nvoid print_defaults() {\n    std::cout << \"Vout default=\" << seed012_vout_default << \" mV\\n\";\n    std::cout << \"safe margin=\" << seed012_get_safe_margin() << \" mV\\n\";\n}\n\nint adjust_setpoint(int target_mv) {\n    int margin = seed012_get_safe_margin();\n    return (target_mv > margin) ? margin : target_mv;\n}\n\n} // namespace pm_c\n\nint main() {\n    using namespace pm_c;\n    print_defaults();\n    int set_mv = adjust_setpoint(3400);\n    std::cout << \"set_mv=\" << set_mv << \"\\n\";\n    return 0;\n}\n"}
{"program":13,"context":"Filesystem cache parameters header","noncompliant":"// -------- Non-Compliant Program (prog_013_nc.cpp)\n// Context: Filesystem cache parameters\n#include <iostream>\n#include <cstddef>\n#include \"seedNC.h\" // seed013_cache_size, seed013_should_evict()\n\nnamespace fs_nc {\n\nvoid status(std::size_t used) {\n    std::cout << \"cache_size=\" << seed013_cache_size << \" used=\" << used\n              << \" evict=\" << (seed013_should_evict(used) ? \"Y\":\"N\") << \"\\n\";\n}\n\nvoid simulate(std::size_t logs[], std::size_t n) { // NC decay\n    for (std::size_t i = 0; i < n; ++i) status(logs[i]);\n}\n\n} // namespace fs_nc\n\nint main() {\n    using namespace fs_nc;\n    std::size_t samples[4] = {1024,4096,5000,3000};\n    simulate(samples,4);\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_013_c.cpp)\n// Context: Filesystem cache parameters — COMPLIANT\n#include <iostream>\n#include <cstddef>\n#include \"seedC.h\" // extern seed013_cache_size, seed013_should_evict()\n\n// Definitions\nstd::size_t seed013_cache_size = 4096;\nbool seed013_should_evict(std::size_t used) { return used > seed013_cache_size; }\n\nnamespace fs_c {\n\ntemplate <std::size_t N>\nvoid simulate(const std::size_t (&logs)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        std::cout << \"cache_size=\" << seed013_cache_size\n                  << \" used=\" << logs[i]\n                  << \" evict=\" << (seed013_should_evict(logs[i]) ? \"Y\":\"N\")\n                  << \"\\n\";\n    }\n}\n\n} // namespace fs_c\n\nint main() {\n    using namespace fs_c;\n    const std::size_t samples[4] = {1024,4096,5000,3000};\n    simulate(samples);\n    return 0;\n}\n"}
{"program":14,"context":"Bluetooth device pairing table header","noncompliant":"// -------- Non-Compliant Program (prog_014_nc.cpp)\n// Context: Bluetooth pairing table\n#include <iostream>\n#include <cstdint>\n#include \"seedNC.h\" // seed014_paired[], seed014_is_paired()\n\nnamespace bt_nc {\n\nvoid add_pair(std::uint64_t id) {\n    seed014_paired[0] = id; // writes into header-defined array (NC)\n}\n\nvoid show_table() {\n    std::cout << \"paired:\";\n    for (int i = 0; i < 4; ++i) std::cout << ' ' << seed014_paired[i];\n    std::cout << \"\\n\";\n}\n\nvoid test(std::uint64_t ids[], std::size_t n) { // NC decay\n    for (std::size_t i = 0; i < n; ++i) {\n        add_pair(ids[i]);\n        std::cout << \"is_paired(\" << ids[i] << \")=\" << seed014_is_paired(ids[i]) << \"\\n\";\n        show_table();\n    }\n}\n\n} // namespace bt_nc\n\nint main() {\n    using namespace bt_nc;\n    std::uint64_t ids[2] = {0xAABBCCDDEEFFULL, 0x112233445566ULL};\n    test(ids,2);\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_014_c.cpp)\n// Context: Bluetooth pairing table — COMPLIANT\n#include <iostream>\n#include <cstdint>\n#include <cstddef>\n#include \"seedC.h\" // extern seed014_paired[], seed014_is_paired()\n\n// Definitions\nstd::uint64_t seed014_paired[4] = {0};\nbool seed014_is_paired(std::uint64_t id) { return seed014_paired[0] == id; }\n\nnamespace bt_c {\n\nvoid add_pair(std::uint64_t id) {\n    seed014_paired[0] = id; // defined in this TU\n}\n\ntemplate <std::size_t N>\nvoid test(const std::uint64_t (&ids)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        add_pair(ids[i]);\n        std::cout << \"is_paired(\" << ids[i] << \")=\" << seed014_is_paired(ids[i]) << \"\\n\";\n        std::cout << \"paired:\";\n        for (int j = 0; j < 4; ++j) std::cout << ' ' << seed014_paired[j];\n        std::cout << \"\\n\";\n    }\n}\n\n} // namespace bt_c\n\nint main() {\n    using namespace bt_c;\n    const std::uint64_t ids[2] = {0xAABBCCDDEEFFULL, 0x112233445566ULL};\n    test(ids);\n    return 0;\n}\n"}
{"program":15,"context":"Printer spooler job queue defaults","noncompliant":"// -------- Non-Compliant Program (prog_015_nc.cpp)\n// Context: Printer spooler defaults\n#include <iostream>\n#include \"seedNC.h\" // seed015_default_queue, seed015_enqueue_limit()\n\nnamespace spool_nc {\n\nvoid print_limits() {\n    std::cout << \"default_queue=\" << seed015_default_queue\n              << \" enqueue_limit=\" << seed015_enqueue_limit() << \"\\n\";\n}\n\nint push_jobs(int jobs[], std::size_t n) { // NC decay\n    int accepted = 0;\n    int limit = seed015_enqueue_limit();\n    for (std::size_t i = 0; i < n; ++i) {\n        if (accepted < limit) ++accepted;\n    }\n    return accepted;\n}\n\n} // namespace spool_nc\n\nint main() {\n    using namespace spool_nc;\n    print_limits();\n    int jobs[6] = {1,1,1,1,1,1};\n    std::cout << \"accepted=\" << push_jobs(jobs,6) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_015_c.cpp)\n// Context: Printer spooler defaults — COMPLIANT\n#include <iostream>\n#include \"seedC.h\" // extern seed015_default_queue, seed015_enqueue_limit()\n\n// Definitions\nint seed015_default_queue = 10;\nint seed015_enqueue_limit() { return seed015_default_queue - 1; }\n\nnamespace spool_c {\n\nvoid print_limits() {\n    std::cout << \"default_queue=\" << seed015_default_queue\n              << \" enqueue_limit=\" << seed015_enqueue_limit() << \"\\n\";\n}\n\ntemplate <std::size_t N>\nint push_jobs(const int (&jobs)[N]) {\n    int accepted = 0;\n    int limit = seed015_enqueue_limit();\n    for (std::size_t i = 0; i < N; ++i) {\n        if (accepted < limit) ++accepted;\n    }\n    return accepted;\n}\n\n} // namespace spool_c\n\nint main() {\n    using namespace spool_c;\n    print_limits();\n    const int jobs[6] = {1,1,1,1,1,1};\n    std::cout << \"accepted=\" << push_jobs(jobs) << \"\\n\";\n    return 0;\n}\n"}
{"program":16,"context":"Medical device alarm thresholds","noncompliant":"// -------- Non-Compliant Program (prog_016_nc.cpp)\n// Context: Medical device alarm thresholds\n#include <iostream>\n#include <iomanip>\n#include \"seedNC.h\"  // seed016_hr_threshold, seed016_is_alarm()\n\nnamespace med_nc {\n\nfloat normalize(float hr) {\n    return (hr < 0.0F) ? 0.0F : hr;\n}\n\nvoid evaluate(float readings[], std::size_t n) { // NC: array decay\n    for (std::size_t i = 0; i < n; ++i) {\n        float hr = normalize(readings[i]);\n        bool alarm = seed016_is_alarm(hr); // header-defined function (NC)\n        std::cout << \"HR=\" << std::fixed << std::setprecision(1)\n                  << hr << \" alarm=\" << (alarm ? \"YES\" : \"NO\") << \"\\n\";\n    }\n}\n\nfloat max_hr(float arr[], std::size_t n) { // NC\n    float m = 0.0F;\n    for (std::size_t i = 0; i < n; ++i)\n        if (arr[i] > m) m = arr[i];\n    return m;\n}\n\n} // namespace med_nc\n\nint main() {\n    using namespace med_nc;\n    float hrs[6] = {80.0F, 95.0F, 110.0F, 121.0F, 130.0F, 118.0F};\n    evaluate(hrs, 6);\n    std::cout << \"max= \" << max_hr(hrs, 6) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_016_c.cpp)\n// Context: Medical device alarm thresholds — COMPLIANT\n#include <iostream>\n#include <iomanip>\n#include \"seedC.h\"  // extern seed016_hr_threshold, seed016_is_alarm()\n\n// Definitions\nfloat seed016_hr_threshold = 120.0F;\nbool seed016_is_alarm(float hr) { return hr > seed016_hr_threshold; }\n\nnamespace med_c {\n\nfloat normalize(float hr) {\n    return (hr < 0.0F) ? 0.0F : hr;\n}\n\ntemplate <std::size_t N>\nvoid evaluate(const float (&readings)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        float hr = normalize(readings[i]);\n        bool alarm = seed016_is_alarm(hr);\n        std::cout << \"HR=\" << std::fixed << std::setprecision(1)\n                  << hr << \" alarm=\" << (alarm ? \"YES\" : \"NO\") << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nfloat max_hr(const float (&arr)[N]) {\n    float m = 0.0F;\n    for (auto v : arr) if (v > m) m = v;\n    return m;\n}\n\n} // namespace med_c\n\nint main() {\n    using namespace med_c;\n    const float hrs[6] = {80.0F,95.0F,110.0F,121.0F,130.0F,118.0F};\n    evaluate(hrs);\n    std::cout << \"max= \" << max_hr(hrs) << \"\\n\";\n    return 0;\n}\n"}
{"program":17,"context":"Filesystem path length limits header","noncompliant":"// -------- Non-Compliant Program (prog_017_nc.cpp)\n// Context: Filesystem path length limits\n#include <iostream>\n#include <string>\n#include \"seedNC.h\" // seed017_max_path, seed017_too_long()\n\nnamespace fs_nc {\n\nint length_of(const char* p) {\n    int len = 0; while (p && p[len]) ++len; return len;\n}\n\nvoid check_paths(const char* paths[], std::size_t n) { // NC decay\n    for (std::size_t i = 0; i < n; ++i) {\n        const char* p = paths[i];\n        bool too_long = seed017_too_long(p);\n        std::cout << \"path='\" << p << \"' len=\" << length_of(p)\n                  << \" too_long=\" << (too_long ? \"Y\" : \"N\") << \"\\n\";\n    }\n}\n\nint longest_len(const char* paths[], std::size_t n) { // NC\n    int max = 0;\n    for (std::size_t i = 0; i < n; ++i) {\n        int l = length_of(paths[i]);\n        if (l > max) max = l;\n    }\n    return max;\n}\n\n} // namespace fs_nc\n\nint main() {\n    using namespace fs_nc;\n    const char* arr[3] = {\"/usr/bin\",\"/home/user/documents\",\"/this/is/a/very/long/path/example\"};\n    check_paths(arr, 3);\n    std::cout << \"max_len=\" << longest_len(arr,3) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_017_c.cpp)\n// Context: Filesystem path length limits — COMPLIANT\n#include <iostream>\n#include <string>\n#include \"seedC.h\"  // extern seed017_max_path, seed017_too_long()\n\n// Definitions\nint seed017_max_path = 260;\nbool seed017_too_long(const char* p) {\n    int len = 0; while (p && p[len]) ++len; return len > seed017_max_path;\n}\n\nnamespace fs_c {\n\nint length_of(const char* p) {\n    int len = 0; while (p && p[len]) ++len; return len;\n}\n\ntemplate <std::size_t N>\nvoid check_paths(const char* const (&paths)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        const char* p = paths[i];\n        bool too_long = seed017_too_long(p);\n        std::cout << \"path='\" << p << \"' len=\" << length_of(p)\n                  << \" too_long=\" << (too_long ? \"Y\" : \"N\") << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint longest_len(const char* const (&paths)[N]) {\n    int max = 0;\n    for (auto p : paths) {\n        int l = length_of(p);\n        if (l > max) max = l;\n    }\n    return max;\n}\n\n} // namespace fs_c\n\nint main() {\n    using namespace fs_c;\n    const char* arr[3] = {\"/usr/bin\",\"/home/user/documents\",\"/this/is/a/very/long/path/example\"};\n    check_paths(arr);\n    std::cout << \"max_len=\" << longest_len(arr) << \"\\n\";\n    return 0;\n}\n"}
{"program":18,"context":"Machine vision ROI parameters header","noncompliant":"// -------- Non-Compliant Program (prog_018_nc.cpp)\n// Context: Machine vision ROI parameters\n#include <iostream>\n#include <iomanip>\n#include \"seedNC.h\" // seed018_roi_width, seed018_roi_height, seed018_area()\n\nnamespace roi_nc {\n\nvoid print() {\n    std::cout << \"ROI=\" << seed018_roi_width << \"x\" << seed018_roi_height\n              << \" area=\" << seed018_area() << \"\\n\";\n}\n\nint perimeter() {\n    return 2 * (seed018_roi_width + seed018_roi_height);\n}\n\nvoid adjust(int dims[], std::size_t n) { // NC decay\n    for (std::size_t i = 0; i < n; i += 2) {\n        seed018_roi_width = dims[i];\n        seed018_roi_height = dims[i+1];\n        print();\n        std::cout << \"perim=\" << perimeter() << \"\\n\";\n    }\n}\n\n} // namespace roi_nc\n\nint main() {\n    using namespace roi_nc;\n    int dims[4] = {320,240,800,600};\n    adjust(dims,4);\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_018_c.cpp)\n// Context: Machine vision ROI parameters — COMPLIANT\n#include <iostream>\n#include <iomanip>\n#include \"seedC.h\" // extern seed018_roi_width, seed018_roi_height, seed018_area()\n\n// Definitions\nint seed018_roi_width = 640;\nint seed018_roi_height = 480;\nint seed018_area() { return seed018_roi_width * seed018_roi_height; }\n\nnamespace roi_c {\n\nvoid print() {\n    std::cout << \"ROI=\" << seed018_roi_width << \"x\" << seed018_roi_height\n              << \" area=\" << seed018_area() << \"\\n\";\n}\n\ntemplate <std::size_t N>\nvoid adjust(const int (&dims)[N]) {\n    for (std::size_t i = 0; i < N; i += 2) {\n        seed018_roi_width = dims[i];\n        seed018_roi_height = dims[i+1];\n        print();\n        int per = 2 * (seed018_roi_width + seed018_roi_height);\n        std::cout << \"perim=\" << per << \"\\n\";\n    }\n}\n\n} // namespace roi_c\n\nint main() {\n    using namespace roi_c;\n    const int dims[4] = {320,240,800,600};\n    adjust(dims);\n    return 0;\n}\n"}
{"program":19,"context":"Telemetry frame ID list header","noncompliant":"// -------- Non-Compliant Program (prog_019_nc.cpp)\n// Context: Telemetry frame ID list\n#include <iostream>\n#include <cstdint>\n#include \"seedNC.h\" // seed019_ids[], seed019_first()\n\nnamespace tele_nc {\n\nvoid print_ids() {\n    std::cout << \"ids:\";\n    for (int i = 0; i < 3; ++i) std::cout << ' ' << seed019_ids[i];\n    std::cout << \"\\n\";\n}\n\nvoid cycle(std::uint16_t frames[], std::size_t n) { // NC decay\n    for (std::size_t i = 0; i < n; ++i) {\n        seed019_ids[0] = frames[i];\n        std::uint16_t f = seed019_first();\n        std::cout << \"first=\" << f << \"\\n\";\n        print_ids();\n    }\n}\n\n} // namespace tele_nc\n\nint main() {\n    using namespace tele_nc;\n    std::uint16_t arr[3] = {0x10,0x20,0x30};\n    cycle(arr,3);\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_019_c.cpp)\n// Context: Telemetry frame ID list — COMPLIANT\n#include <iostream>\n#include <cstdint>\n#include \"seedC.h\"\n\n// Definitions\nstd::uint16_t seed019_ids[3] = {0x01,0x02,0x03};\nstd::uint16_t seed019_first() { return seed019_ids[0]; }\n\nnamespace tele_c {\n\ntemplate <std::size_t N>\nvoid cycle(const std::uint16_t (&frames)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        seed019_ids[0] = frames[i];\n        std::uint16_t f = seed019_first();\n        std::cout << \"first=\" << f << \" ids:\";\n        for (int j = 0; j < 3; ++j) std::cout << ' ' << seed019_ids[j];\n        std::cout << \"\\n\";\n    }\n}\n\n} // namespace tele_c\n\nint main() {\n    using namespace tele_c;\n    const std::uint16_t arr[3] = {0x10,0x20,0x30};\n    cycle(arr);\n    return 0;\n}\n"}
{"program":20,"context":"Energy meter sample buffer parameters","noncompliant":"// -------- Non-Compliant Program (prog_020_nc.cpp)\n// Context: Energy meter sample buffer params\n#include <iostream>\n#include <cstddef>\n#include \"seedNC.h\" // seed020_samples, seed020_half()\n\nnamespace meter_nc {\n\nstd::size_t used_samples(std::size_t total) {\n    return total / 3;\n}\n\nvoid analyze(std::size_t buf[], std::size_t n) { // NC decay\n    for (std::size_t i = 0; i < n; ++i) {\n        std::size_t used = used_samples(buf[i]);\n        std::size_t half = seed020_half(); // NC header function\n        std::cout << \"buf=\" << buf[i] << \" used=\" << used\n                  << \" half=\" << half << \"\\n\";\n    }\n}\n\nstd::size_t sum(std::size_t arr[], std::size_t n) { // NC\n    std::size_t s = 0;\n    for (std::size_t i = 0; i < n; ++i) s += arr[i];\n    return s;\n}\n\n} // namespace meter_nc\n\nint main() {\n    using namespace meter_nc;\n    std::size_t b[4] = {1024,512,2048,4096};\n    analyze(b,4);\n    std::cout << \"sum=\" << sum(b,4) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_020_c.cpp)\n// Context: Energy meter sample buffer params — COMPLIANT\n#include <iostream>\n#include <cstddef>\n#include \"seedC.h\"\n\n// Definitions\nstd::size_t seed020_samples = 1024;\nstd::size_t seed020_half() { return seed020_samples / 2; }\n\nnamespace meter_c {\n\nstd::size_t used_samples(std::size_t total) {\n    return total / 3;\n}\n\ntemplate <std::size_t N>\nvoid analyze(const std::size_t (&buf)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        std::size_t used = used_samples(buf[i]);\n        std::size_t half = seed020_half();\n        std::cout << \"buf=\" << buf[i] << \" used=\" << used\n                  << \" half=\" << half << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nstd::size_t sum(const std::size_t (&arr)[N]) {\n    std::size_t s = 0;\n    for (auto v : arr) s += v;\n    return s;\n}\n\n} // namespace meter_c\n\nint main() {\n    using namespace meter_c;\n    const std::size_t b[4] = {1024,512,2048,4096};\n    analyze(b);\n    std::cout << \"sum=\" << sum(b) << \"\\n\";\n    return 0;\n}\n"}
{"program":21,"context":"Thermostat setpoint header with mutable setpoint","noncompliant":"// -------- Non-Compliant Program (prog_021_nc.cpp)\n// Context: Thermostat control\n#include <iostream>\n#include <iomanip>\n#include \"seedNC.h\" // seed021_thermostat_setpoint, seed021_get_setpoint()\n\nnamespace thermo_nc {\n\nfloat adjust(float current, float offset) {\n    return current + offset;\n}\n\nvoid run(float temps[], std::size_t n) { // NC: decayed array\n    for (std::size_t i = 0; i < n; ++i) {\n        float target = seed021_get_setpoint();\n        float corrected = adjust(temps[i], target - temps[i]);\n        std::cout << \"temp=\" << temps[i]\n                  << \" target=\" << target\n                  << \" corrected=\" << corrected << \"\\n\";\n    }\n}\n\nfloat avg(float arr[], std::size_t n) { // NC\n    float s = 0;\n    for (std::size_t i = 0; i < n; ++i) s += arr[i];\n    return (n>0)? s/n:0;\n}\n\n} // namespace thermo_nc\n\nint main() {\n    using namespace thermo_nc;\n    float readings[5] = {20.5F, 21.0F, 22.0F, 23.5F, 24.0F};\n    run(readings,5);\n    std::cout << \"avg=\" << avg(readings,5) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_021_c.cpp)\n// Context: Thermostat control — COMPLIANT\n#include <iostream>\n#include <iomanip>\n#include \"seedC.h\" // extern seed021_thermostat_setpoint, seed021_get_setpoint()\n\n// Definitions\nfloat seed021_thermostat_setpoint = 22.5F;\nfloat seed021_get_setpoint() { return seed021_thermostat_setpoint; }\n\nnamespace thermo_c {\n\nfloat adjust(float current, float offset) {\n    return current + offset;\n}\n\ntemplate <std::size_t N>\nvoid run(const float (&temps)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        float target = seed021_get_setpoint();\n        float corrected = adjust(temps[i], target - temps[i]);\n        std::cout << \"temp=\" << temps[i]\n                  << \" target=\" << target\n                  << \" corrected=\" << corrected << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nfloat avg(const float (&arr)[N]) {\n    float s = 0;\n    for (auto v : arr) s += v;\n    return (N>0)? s/N:0;\n}\n\n} // namespace thermo_c\n\nint main() {\n    using namespace thermo_c;\n    const float readings[5] = {20.5F,21.0F,22.0F,23.5F,24.0F};\n    run(readings);\n    std::cout << \"avg=\" << avg(readings) << \"\\n\";\n    return 0;\n}\n"}
{"program":22,"context":"Power meter calibration header defining gains","noncompliant":"// -------- Non-Compliant Program (prog_022_nc.cpp)\n// Context: Power meter calibration\n#include <iostream>\n#include <iomanip>\n#include \"seedNC.h\" // seed022_calibration_gain, seed022_apply_gain()\n\nnamespace cal_nc {\n\ndouble clamp(double v) {\n    return (v < 0.0) ? 0.0 : v;\n}\n\nvoid measure(double raw[], std::size_t n) { // NC\n    for (std::size_t i = 0; i < n; ++i) {\n        double v = clamp(raw[i]);\n        double out = seed022_apply_gain(v); // header function (NC)\n        std::cout << \"raw=\" << raw[i]\n                  << \" cal=\" << std::fixed << std::setprecision(3)\n                  << out << \"\\n\";\n    }\n}\n\ndouble sum(double arr[], std::size_t n) {\n    double s = 0;\n    for (std::size_t i = 0; i < n; ++i) s += arr[i];\n    return s;\n}\n\n} // namespace cal_nc\n\nint main() {\n    using namespace cal_nc;\n    double raw[5] = {1.2, 2.5, 3.0, -1.0, 0.5};\n    measure(raw,5);\n    std::cout << \"sum=\" << sum(raw,5) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_022_c.cpp)\n// Context: Power meter calibration — COMPLIANT\n#include <iostream>\n#include <iomanip>\n#include \"seedC.h\"\n\n// Definitions\ndouble seed022_calibration_gain = 1.234;\ndouble seed022_apply_gain(double v) { return v * seed022_calibration_gain; }\n\nnamespace cal_c {\n\ndouble clamp(double v) {\n    return (v < 0.0) ? 0.0 : v;\n}\n\ntemplate <std::size_t N>\nvoid measure(const double (&raw)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        double v = clamp(raw[i]);\n        double out = seed022_apply_gain(v);\n        std::cout << \"raw=\" << raw[i]\n                  << \" cal=\" << std::fixed << std::setprecision(3)\n                  << out << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\ndouble sum(const double (&arr)[N]) {\n    double s = 0;\n    for (auto v : arr) s += v;\n    return s;\n}\n\n} // namespace cal_c\n\nint main() {\n    using namespace cal_c;\n    const double raw[5] = {1.2,2.5,3.0,-1.0,0.5};\n    measure(raw);\n    std::cout << \"sum=\" << sum(raw) << \"\\n\";\n    return 0;\n}\n"}
{"program":23,"context":"Automotive CAN message table defined in header","noncompliant":"// -------- Non-Compliant Program (prog_023_nc.cpp)\n// Context: Automotive CAN message table\n#include <iostream>\n#include <cstdint>\n#include \"seedNC.h\" // seed023_CAN_IDS[], seed023_valid_id()\n\nnamespace can_nc {\n\nvoid print_ids() {\n    std::cout << \"CAN IDs:\";\n    for (int i = 0; i < 8; ++i) std::cout << ' ' << seed023_CAN_IDS[i];\n    std::cout << \"\\n\";\n}\n\nvoid check(std::uint32_t msgs[], std::size_t n) { // NC\n    for (std::size_t i = 0; i < n; ++i) {\n        bool ok = seed023_valid_id(msgs[i]); // header fn\n        std::cout << \"msg=\" << msgs[i] << \" valid=\" << ok << \"\\n\";\n    }\n}\n\n} // namespace can_nc\n\nint main() {\n    using namespace can_nc;\n    std::uint32_t msgs[4] = {0x100,0x200,0x100,0x123};\n    print_ids();\n    check(msgs,4);\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_023_c.cpp)\n// Context: Automotive CAN message table — COMPLIANT\n#include <iostream>\n#include <cstdint>\n#include \"seedC.h\" // extern seed023_CAN_IDS[], seed023_valid_id()\n\n// Definitions\nstd::uint32_t seed023_CAN_IDS[8] = {0x100,0,0,0,0,0,0,0};\nbool seed023_valid_id(std::uint32_t id) { return id == seed023_CAN_IDS[0]; }\n\nnamespace can_c {\n\ntemplate <std::size_t N>\nvoid print_ids(const std::uint32_t (&ids)[N]) {\n    std::cout << \"CAN IDs:\";\n    for (std::size_t i = 0; i < N; ++i) std::cout << ' ' << ids[i];\n    std::cout << \"\\n\";\n}\n\ntemplate <std::size_t N>\nvoid check(const std::uint32_t (&msgs)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        bool ok = seed023_valid_id(msgs[i]);\n        std::cout << \"msg=\" << msgs[i] << \" valid=\" << ok << \"\\n\";\n    }\n}\n\n} // namespace can_c\n\nint main() {\n    using namespace can_c;\n    const std::uint32_t msgs[4] = {0x100,0x200,0x100,0x123};\n    print_ids(seed023_CAN_IDS);\n    check(msgs);\n    return 0;\n}\n"}
{"program":24,"context":"Motor driver header with speed variable","noncompliant":"// -------- Non-Compliant Program (prog_024_nc.cpp)\n// Context: Motor driver control\n#include <iostream>\n#include \"seedNC.h\" // seed024_motor_speed, seed024_set_speed()\n\nnamespace motor_nc {\n\nvoid accelerate(int inc) {\n    seed024_motor_speed += inc;\n}\n\nvoid run(int speeds[], std::size_t n) { // NC\n    for (std::size_t i = 0; i < n; ++i) {\n        seed024_set_speed(speeds[i]);\n        accelerate(10);\n        std::cout << \"speed=\" << seed024_motor_speed << \"\\n\";\n    }\n}\n\nint sum(int arr[], std::size_t n) {\n    int s = 0;\n    for (std::size_t i = 0; i < n; ++i) s += arr[i];\n    return s;\n}\n\n} // namespace motor_nc\n\nint main() {\n    using namespace motor_nc;\n    int sp[4] = {1000,1200,1400,1600};\n    run(sp,4);\n    std::cout << \"sum=\" << sum(sp,4) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_024_c.cpp)\n// Context: Motor driver control — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nint seed024_motor_speed = 1500;\nvoid seed024_set_speed(int s) { seed024_motor_speed = s; }\n\nnamespace motor_c {\n\nvoid accelerate(int inc) {\n    seed024_motor_speed += inc;\n}\n\ntemplate <std::size_t N>\nvoid run(const int (&speeds)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        seed024_set_speed(speeds[i]);\n        accelerate(10);\n        std::cout << \"speed=\" << seed024_motor_speed << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint sum(const int (&arr)[N]) {\n    int s = 0;\n    for (auto v : arr) s += v;\n    return s;\n}\n\n} // namespace motor_c\n\nint main() {\n    using namespace motor_c;\n    const int sp[4] = {1000,1200,1400,1600};\n    run(sp);\n    std::cout << \"sum=\" << sum(sp) << \"\\n\";\n    return 0;\n}\n"}
{"program":25,"context":"Display brightness profile header","noncompliant":"// -------- Non-Compliant Program (prog_025_nc.cpp)\n// Context: Display brightness profile\n#include <iostream>\n#include \"seedNC.h\" // seed025_default_brightness, seed025_adjust_brightness()\n\nnamespace disp_nc {\n\nvoid apply(int ambient[], std::size_t n) { // NC\n    for (std::size_t i = 0; i < n; ++i) {\n        int adj = seed025_adjust_brightness(ambient[i]);\n        std::cout << \"ambient=\" << ambient[i]\n                  << \" adj=\" << adj << \"\\n\";\n    }\n}\n\nint total(int arr[], std::size_t n) {\n    int s = 0;\n    for (std::size_t i = 0; i < n; ++i) s += arr[i];\n    return s;\n}\n\n} // namespace disp_nc\n\nint main() {\n    using namespace disp_nc;\n    int amb[4] = {150,250,180,300};\n    apply(amb,4);\n    std::cout << \"total=\" << total(amb,4) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_025_c.cpp)\n// Context: Display brightness profile — COMPLIANT\n#include <iostream>\n#include \"seedC.h\" // extern seed025_default_brightness, seed025_adjust_brightness()\n\n// Definitions\nint seed025_default_brightness = 80;\nint seed025_adjust_brightness(int ambient) {\n    return (ambient > 200) ? seed025_default_brightness + 10 : seed025_default_brightness;\n}\n\nnamespace disp_c {\n\ntemplate <std::size_t N>\nvoid apply(const int (&ambient)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        int adj = seed025_adjust_brightness(ambient[i]);\n        std::cout << \"ambient=\" << ambient[i]\n                  << \" adj=\" << adj << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint total(const int (&arr)[N]) {\n    int s = 0;\n    for (auto v : arr) s += v;\n    return s;\n}\n\n} // namespace disp_c\n\nint main() {\n    using namespace disp_c;\n    const int amb[4] = {150,250,180,300};\n    apply(amb);\n    std::cout << \"total=\" << total(amb) << \"\\n\";\n    return 0;\n}\n"}
{"program":26,"context":"Real-time clock drift correction header","noncompliant":"// -------- Non-Compliant Program (prog_026_nc.cpp)\n// Context: RTC drift correction\n#include <iostream>\n#include <iomanip>\n#include \"seedNC.h\" // seed026_drift_ppm, seed026_correct_time()\n\nnamespace rtc_nc {\n\ndouble clamp(double v) {\n    return (v < 0.0) ? 0.0 : v;\n}\n\nvoid process(double times[], std::size_t n) { // NC decay\n    for (std::size_t i = 0; i < n; ++i) {\n        double t = clamp(times[i]);\n        double adj = seed026_correct_time(t); // header fn (NC)\n        std::cout << \"t=\" << t << \" adj=\" << std::fixed << std::setprecision(6)\n                  << adj << \"\\n\";\n    }\n}\n\ndouble max_val(double arr[], std::size_t n) { // NC\n    double m = 0.0;\n    for (std::size_t i = 0; i < n; ++i)\n        if (arr[i] > m) m = arr[i];\n    return m;\n}\n\n} // namespace rtc_nc\n\nint main() {\n    using namespace rtc_nc;\n    double t[5] = {1.0, 10.0, 60.0, 120.0, 3600.0};\n    process(t,5);\n    std::cout << \"max=\" << max_val(t,5) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_026_c.cpp)\n// Context: RTC drift correction — COMPLIANT\n#include <iostream>\n#include <iomanip>\n#include \"seedC.h\"\n\n// Definitions\ndouble seed026_drift_ppm = -2.5;\ndouble seed026_correct_time(double t) { return t + t * (seed026_drift_ppm / 1e6); }\n\nnamespace rtc_c {\n\ndouble clamp(double v) { return (v < 0.0) ? 0.0 : v; }\n\ntemplate <std::size_t N>\nvoid process(const double (&times)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        double t = clamp(times[i]);\n        double adj = seed026_correct_time(t);\n        std::cout << \"t=\" << t << \" adj=\" << std::fixed << std::setprecision(6)\n                  << adj << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\ndouble max_val(const double (&arr)[N]) {\n    double m = 0.0;\n    for (auto v : arr) if (v > m) m = v;\n    return m;\n}\n\n} // namespace rtc_c\n\nint main() {\n    using namespace rtc_c;\n    const double t[5] = {1.0,10.0,60.0,120.0,3600.0};\n    process(t);\n    std::cout << \"max=\" << max_val(t) << \"\\n\";\n    return 0;\n}\n"}
{"program":27,"context":"Wireless RSSI smoothing parameters header","noncompliant":"// -------- Non-Compliant Program (prog_027_nc.cpp)\n// Context: Wireless RSSI smoothing\n#include <iostream>\n#include <cmath>\n#include \"seedNC.h\" // seed027_window_size, seed027_smooth()\n\nnamespace rssi_nc {\n\nfloat avg(float arr[], std::size_t n) { // NC\n    float s = 0.0F;\n    for (std::size_t i = 0; i < n; ++i) s += arr[i];\n    return (n>0)? s/n : 0.0F;\n}\n\nvoid smooth_seq(float buf[], std::size_t n) { // NC decay\n    for (std::size_t i = 0; i < n; ++i) {\n        float sm = seed027_smooth(buf); // header fn uses decayed array\n        std::cout << \"rssi=\" << buf[i]\n                  << \" smoothed=\" << sm\n                  << \" window=\" << seed027_window_size << \"\\n\";\n    }\n}\n\n} // namespace rssi_nc\n\nint main() {\n    using namespace rssi_nc;\n    float r[5] = {-70.5F, -68.0F, -72.3F, -69.1F, -71.0F};\n    smooth_seq(r,5);\n    std::cout << \"avg=\" << avg(r,5) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_027_c.cpp)\n// Context: Wireless RSSI smoothing — COMPLIANT\n#include <iostream>\n#include <cmath>\n#include \"seedC.h\"\n\n// Definitions\nint seed027_window_size = 5;\nfloat seed027_smooth(float vals[]) { return vals[0]; }\n\nnamespace rssi_c {\n\ntemplate <std::size_t N>\nfloat avg(const float (&arr)[N]) {\n    float s = 0.0F;\n    for (auto v : arr) s += v;\n    return (N>0)? s/N : 0.0F;\n}\n\ntemplate <std::size_t N>\nvoid smooth_seq(const float (&buf)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        // Note: seed027_smooth still uses decayed ptr semantics but declared extern (C version ok)\n        float sm = seed027_smooth(const_cast<float*>(buf));\n        std::cout << \"rssi=\" << buf[i]\n                  << \" smoothed=\" << sm\n                  << \" window=\" << seed027_window_size << \"\\n\";\n    }\n}\n\n} // namespace rssi_c\n\nint main() {\n    using namespace rssi_c;\n    const float r[5] = {-70.5F,-68.0F,-72.3F,-69.1F,-71.0F};\n    smooth_seq(r);\n    std::cout << \"avg=\" << avg(r) << \"\\n\";\n    return 0;\n}\n"}
{"program":28,"context":"LED controller color table header","noncompliant":"// -------- Non-Compliant Program (prog_028_nc.cpp)\n// Context: LED controller color table\n#include <iostream>\n#include <iomanip>\n#include \"seedNC.h\" // seed028_color_map[], seed028_get_color()\n\nnamespace led_nc {\n\nvoid cycle(int idx[], std::size_t n) { // NC decay\n    for (std::size_t i = 0; i < n; ++i) {\n        unsigned long c = seed028_get_color(idx[i]);\n        std::cout << \"idx=\" << idx[i]\n                  << \" color=0x\" << std::hex << c << std::dec << \"\\n\";\n    }\n}\n\nunsigned long maxc(unsigned long arr[], std::size_t n) { // NC\n    unsigned long m = 0;\n    for (std::size_t i = 0; i < n; ++i)\n        if (arr[i] > m) m = arr[i];\n    return m;\n}\n\n} // namespace led_nc\n\nint main() {\n    using namespace led_nc;\n    int idx[4] = {0,0,0,0};\n    cycle(idx,4);\n    unsigned long vals[3] = {0xFF0000,0x00FF00,0x0000FF};\n    std::cout << \"max=0x\" << std::hex << maxc(vals,3) << std::dec << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_028_c.cpp)\n// Context: LED controller color table — COMPLIANT\n#include <iostream>\n#include <iomanip>\n#include \"seedC.h\"\n\n// Definitions\nunsigned long seed028_color_map[4] = {0xFF0000,0x00FF00,0x0000FF,0xFFFFFF};\nunsigned long seed028_get_color(int i) { return seed028_color_map[i]; }\n\nnamespace led_c {\n\ntemplate <std::size_t N>\nvoid cycle(const int (&idx)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        unsigned long c = seed028_get_color(idx[i]);\n        std::cout << \"idx=\" << idx[i]\n                  << \" color=0x\" << std::hex << c << std::dec << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nunsigned long maxc(const unsigned long (&arr)[N]) {\n    unsigned long m = 0;\n    for (auto v : arr) if (v > m) m = v;\n    return m;\n}\n\n} // namespace led_c\n\nint main() {\n    using namespace led_c;\n    const int idx[4] = {0,1,2,3};\n    cycle(idx);\n    const unsigned long vals[3] = {0xFF0000,0x00FF00,0x0000FF};\n    std::cout << \"max=0x\" << std::hex << maxc(vals) << std::dec << \"\\n\";\n    return 0;\n}\n"}
{"program":29,"context":"Database connection pool size header","noncompliant":"// -------- Non-Compliant Program (prog_029_nc.cpp)\n// Context: Database connection pool\n#include <iostream>\n#include \"seedNC.h\" // seed029_pool_size, seed029_max_clients()\n\nnamespace db_nc {\n\nvoid simulate(int req[], std::size_t n) { // NC\n    for (std::size_t i = 0; i < n; ++i) {\n        int possible = seed029_max_clients();\n        std::cout << \"req=\" << req[i]\n                  << \" max=\" << possible\n                  << \" served=\" << ((req[i] <= possible) ? req[i] : possible)\n                  << \"\\n\";\n    }\n}\n\nint sum(int arr[], std::size_t n) {\n    int s = 0;\n    for (std::size_t i = 0; i < n; ++i) s += arr[i];\n    return s;\n}\n\n} // namespace db_nc\n\nint main() {\n    using namespace db_nc;\n    int reqs[4] = {5,10,20,8};\n    simulate(reqs,4);\n    std::cout << \"sum=\" << sum(reqs,4) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_029_c.cpp)\n// Context: Database connection pool — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nint seed029_pool_size = 16;\nint seed029_max_clients() { return seed029_pool_size - 2; }\n\nnamespace db_c {\n\ntemplate <std::size_t N>\nvoid simulate(const int (&req)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        int possible = seed029_max_clients();\n        std::cout << \"req=\" << req[i]\n                  << \" max=\" << possible\n                  << \" served=\" << ((req[i] <= possible) ? req[i] : possible)\n                  << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint sum(const int (&arr)[N]) {\n    int s = 0;\n    for (auto v : arr) s += v;\n    return s;\n}\n\n} // namespace db_c\n\nint main() {\n    using namespace db_c;\n    const int req[4] = {5,10,20,8};\n    simulate(req);\n    std::cout << \"sum=\" << sum(req) << \"\\n\";\n    return 0;\n}\n"}
{"program":30,"context":"Graphics engine mipmap levels header","noncompliant":"// -------- Non-Compliant Program (prog_030_nc.cpp)\n// Context: Graphics engine mipmap levels\n#include <iostream>\n#include \"seedNC.h\" // seed030_mip_levels, seed030_best_mip()\n\nnamespace gfx_nc {\n\nint clamp(int v) { return (v < 1) ? 1 : v; }\n\nvoid show_levels(int sizes[], std::size_t n) { // NC\n    for (std::size_t i = 0; i < n; ++i) {\n        int lvl = seed030_best_mip(clamp(sizes[i]));\n        std::cout << \"size=\" << sizes[i]\n                  << \" best_mip=\" << lvl << \"\\n\";\n    }\n}\n\nint sum(int arr[], std::size_t n) {\n    int s = 0;\n    for (std::size_t i = 0; i < n; ++i) s += arr[i];\n    return s;\n}\n\n} // namespace gfx_nc\n\nint main() {\n    using namespace gfx_nc;\n    int sz[4] = {128,256,512,1024};\n    show_levels(sz,4);\n    std::cout << \"sum=\" << sum(sz,4) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_030_c.cpp)\n// Context: Graphics engine mipmap levels — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nint seed030_mip_levels = 8;\nint seed030_best_mip(int size) { return (size <= 256) ? seed030_mip_levels : seed030_mip_levels - 2; }\n\nnamespace gfx_c {\n\nint clamp(int v) { return (v < 1) ? 1 : v; }\n\ntemplate <std::size_t N>\nvoid show_levels(const int (&sizes)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        int lvl = seed030_best_mip(clamp(sizes[i]));\n        std::cout << \"size=\" << sizes[i]\n                  << \" best_mip=\" << lvl << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint sum(const int (&arr)[N]) {\n    int s = 0;\n    for (auto v : arr) s += v;\n    return s;\n}\n\n} // namespace gfx_c\n\nint main() {\n    using namespace gfx_c;\n    const int sz[4] = {128,256,512,1024};\n    show_levels(sz);\n    std::cout << \"sum=\" << sum(sz) << \"\\n\";\n    return 0;\n}\n"}
{"program":31,"context":"Thermal camera gain configuration","noncompliant":"// -------- Non-Compliant Program (prog_031_nc.cpp)\n// Context: Thermal camera gain config\n#include <iostream>\n#include <iomanip>\n#include \"seedNC.h\" // seed031_gain, seed031_apply_gain()\n\nnamespace thermal_nc {\n\nfloat clamp(float v) {\n    return (v < 0.0F) ? 0.0F : v;\n}\n\nvoid apply_to_frame(float frame[], std::size_t n) { // NC: array decay\n    for (std::size_t i = 0; i < n; ++i) {\n        float x = clamp(frame[i]);\n        float out = seed031_apply_gain(x); // uses header fn (NC)\n        std::cout << \"px=\" << x << \" gain=\" << seed031_gain\n                  << \" out=\" << out << \"\\n\";\n    }\n}\n\nfloat max_val(float arr[], std::size_t n) { // NC\n    float m = 0.0F;\n    for (std::size_t i = 0; i < n; ++i)\n        if (arr[i] > m) m = arr[i];\n    return m;\n}\n\n} // namespace thermal_nc\n\nint main() {\n    using namespace thermal_nc;\n    float buf[6] = {0.1F,0.5F,1.0F,0.8F,0.3F,1.2F};\n    apply_to_frame(buf,6);\n    std::cout << \"max=\" << max_val(buf,6) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_031_c.cpp)\n// Context: Thermal camera gain config — COMPLIANT\n#include <iostream>\n#include <iomanip>\n#include \"seedC.h\"\n\n// Definitions\nfloat seed031_gain = 1.75F;\nfloat seed031_apply_gain(float x) { return x * seed031_gain; }\n\nnamespace thermal_c {\n\nfloat clamp(float v) { return (v < 0.0F) ? 0.0F : v; }\n\ntemplate <std::size_t N>\nvoid apply_to_frame(const float (&frame)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        float x = clamp(frame[i]);\n        float out = seed031_apply_gain(x);\n        std::cout << \"px=\" << x << \" gain=\" << seed031_gain\n                  << \" out=\" << out << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nfloat max_val(const float (&arr)[N]) {\n    float m = 0.0F;\n    for (auto v : arr) if (v > m) m = v;\n    return m;\n}\n\n} // namespace thermal_c\n\nint main() {\n    using namespace thermal_c;\n    const float buf[6] = {0.1F,0.5F,1.0F,0.8F,0.3F,1.2F};\n    apply_to_frame(buf);\n    std::cout << \"max=\" << max_val(buf) << \"\\n\";\n    return 0;\n}\n"}
{"program":32,"context":"Navigation system compass calibration header","noncompliant":"// -------- Non-Compliant Program (prog_032_nc.cpp)\n// Context: Navigation compass calibration\n#include <iostream>\n#include <cmath>\n#include \"seedNC.h\" // seed032_declination, seed032_correct_heading()\n\nnamespace nav_nc {\n\ndouble normalize(double h) {\n    while (h < 0) h += 360;\n    while (h >= 360) h -= 360;\n    return h;\n}\n\nvoid process(double headings[], std::size_t n) { // NC\n    for (std::size_t i = 0; i < n; ++i) {\n        double c = seed032_correct_heading(headings[i]); // header fn (NC)\n        std::cout << \"raw=\" << headings[i]\n                  << \" decl=\" << seed032_declination\n                  << \" corr=\" << normalize(c) << \"\\n\";\n    }\n}\n\ndouble avg(double arr[], std::size_t n) { // NC\n    double s = 0;\n    for (std::size_t i = 0; i < n; ++i) s += arr[i];\n    return (n>0)? s/n : 0;\n}\n\n} // namespace nav_nc\n\nint main() {\n    using namespace nav_nc;\n    double h[4] = {10,350,45,270};\n    process(h,4);\n    std::cout << \"avg=\" << avg(h,4) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_032_c.cpp)\n// Context: Navigation compass calibration — COMPLIANT\n#include <iostream>\n#include <cmath>\n#include \"seedC.h\"\n\n// Definitions\ndouble seed032_declination = -4.2;\ndouble seed032_correct_heading(double h) { return h + seed032_declination; }\n\nnamespace nav_c {\n\ndouble normalize(double h) {\n    while (h < 0) h += 360;\n    while (h >= 360) h -= 360;\n    return h;\n}\n\ntemplate <std::size_t N>\nvoid process(const double (&headings)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        double c = seed032_correct_heading(headings[i]);\n        std::cout << \"raw=\" << headings[i]\n                  << \" decl=\" << seed032_declination\n                  << \" corr=\" << normalize(c) << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\ndouble avg(const double (&arr)[N]) {\n    double s = 0;\n    for (auto v : arr) s += v;\n    return (N>0)? s/N : 0;\n}\n\n} // namespace nav_c\n\nint main() {\n    using namespace nav_c;\n    const double h[4] = {10,350,45,270};\n    process(h);\n    std::cout << \"avg=\" << avg(h) << \"\\n\";\n    return 0;\n}\n"}
{"program":33,"context":"Robotics wheel encoder conversion factors","noncompliant":"// -------- Non-Compliant Program (prog_033_nc.cpp)\n// Context: Robotics wheel encoder conversion\n#include <iostream>\n#include <cmath>\n#include \"seedNC.h\" // seed033_ticks_per_rev, seed033_to_radians()\n\nnamespace enc_nc {\n\ndouble radians_to_deg(double r) {\n    return r * (180.0 / 3.14159);\n}\n\nvoid convert(int ticks[], std::size_t n) { // NC\n    for (std::size_t i = 0; i < n; ++i) {\n        double r = seed033_to_radians(ticks[i]); // header fn (NC)\n        std::cout << \"ticks=\" << ticks[i]\n                  << \" rad=\" << r\n                  << \" deg=\" << radians_to_deg(r) << \"\\n\";\n    }\n}\n\ndouble max_ticks(int arr[], std::size_t n) { // NC\n    int m = 0;\n    for (std::size_t i = 0; i < n; ++i)\n        if (arr[i] > m) m = arr[i];\n    return m;\n}\n\n} // namespace enc_nc\n\nint main() {\n    using namespace enc_nc;\n    int t[5] = {100,500,1024,2048,4096};\n    convert(t,5);\n    std::cout << \"max=\" << max_ticks(t,5) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_033_c.cpp)\n// Context: Robotics wheel encoder conversion — COMPLIANT\n#include <iostream>\n#include <cmath>\n#include \"seedC.h\"\n\n// Definitions\ndouble seed033_ticks_per_rev = 2048.0;\ndouble seed033_to_radians(int ticks) { return (ticks * 3.14159 * 2) / seed033_ticks_per_rev; }\n\nnamespace enc_c {\n\ndouble radians_to_deg(double r) { return r * (180.0 / 3.14159); }\n\ntemplate <std::size_t N>\nvoid convert(const int (&ticks)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        double r = seed033_to_radians(ticks[i]);\n        std::cout << \"ticks=\" << ticks[i]\n                  << \" rad=\" << r\n                  << \" deg=\" << radians_to_deg(r) << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint max_ticks(const int (&arr)[N]) {\n    int m = 0;\n    for (auto v : arr) if (v > m) m = v;\n    return m;\n}\n\n} // namespace enc_c\n\nint main() {\n    using namespace enc_c;\n    const int t[5] = {100,500,1024,2048,4096};\n    convert(t);\n    std::cout << \"max=\" << max_ticks(t) << \"\\n\";\n    return 0;\n}\n"}
{"program":34,"context":"Audio equalizer preset header","noncompliant":"// -------- Non-Compliant Program (prog_034_nc.cpp)\n// Context: Audio equalizer preset\n#include <iostream>\n#include \"seedNC.h\" // seed034_bass, seed034_treble, seed034_eq_profile()\n\nnamespace eq_nc {\n\nint clamp(int v) {\n    return (v < 0) ? 0 : v;\n}\n\nvoid process(int samples[], std::size_t n) { // NC\n    for (std::size_t i = 0; i < n; ++i) {\n        int adj = seed034_eq_profile(clamp(samples[i]));\n        std::cout << \"in=\" << samples[i]\n                  << \" adj=\" << adj\n                  << \" bass=\" << seed034_bass\n                  << \" treb=\" << seed034_treble << \"\\n\";\n    }\n}\n\nint sum(int arr[], std::size_t n) { // NC\n    int s = 0;\n    for (std::size_t i = 0; i < n; ++i) s += arr[i];\n    return s;\n}\n\n} // namespace eq_nc\n\nint main() {\n    using namespace eq_nc;\n    int s[5] = {10,20,30,40,50};\n    process(s,5);\n    std::cout << \"sum=\" << sum(s,5) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_034_c.cpp)\n// Context: Audio equalizer preset — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nint seed034_bass = 5;\nint seed034_treble = 7;\nint seed034_eq_profile(int x) { return x + seed034_bass - seed034_treble; }\n\nnamespace eq_c {\n\nint clamp(int v) { return (v < 0) ? 0 : v; }\n\ntemplate <std::size_t N>\nvoid process(const int (&samples)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        int adj = seed034_eq_profile(clamp(samples[i]));\n        std::cout << \"in=\" << samples[i]\n                  << \" adj=\" << adj\n                  << \" bass=\" << seed034_bass\n                  << \" treb=\" << seed034_treble << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint sum(const int (&arr)[N]) {\n    int s = 0;\n    for (auto v : arr) s += v;\n    return s;\n}\n\n} // namespace eq_c\n\nint main() {\n    using namespace eq_c;\n    const int s[5] = {10,20,30,40,50};\n    process(s);\n    std::cout << \"sum=\" << sum(s) << \"\\n\";\n    return 0;\n}\n"}
{"program":35,"context":"Camera autofocus region parameters","noncompliant":"// -------- Non-Compliant Program (prog_035_nc.cpp)\n// Context: Camera autofocus region params\n#include <iostream>\n#include \"seedNC.h\" // seed035_focus_x, seed035_focus_y, seed035_is_center()\n\nnamespace af_nc {\n\nvoid adjust(int dx, int dy) {\n    seed035_focus_x += dx;\n    seed035_focus_y += dy;\n}\n\nvoid test(int moves[], std::size_t n) { // NC\n    for (std::size_t i = 0; i < n; i += 2) {\n        adjust(moves[i], moves[i+1]);\n        std::cout << \"pos=(\" << seed035_focus_x << \",\" << seed035_focus_y << \")\"\n                  << \" center=\" << (seed035_is_center()?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\nint sum(int arr[], std::size_t n) {\n    int s = 0;\n    for (std::size_t i = 0; i < n; ++i) s += arr[i];\n    return s;\n}\n\n} // namespace af_nc\n\nint main() {\n    using namespace af_nc;\n    int mv[4] = {5,10,-20,-20};\n    test(mv,4);\n    std::cout << \"sum=\" << sum(mv,4) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_035_c.cpp)\n// Context: Camera autofocus region params — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nint seed035_focus_x = 120;\nint seed035_focus_y = 220;\nbool seed035_is_center() { return seed035_focus_x > 100 && seed035_focus_y > 100; }\n\nnamespace af_c {\n\nvoid adjust(int dx, int dy) {\n    seed035_focus_x += dx;\n    seed035_focus_y += dy;\n}\n\ntemplate <std::size_t N>\nvoid test(const int (&moves)[N]) {\n    for (std::size_t i = 0; i < N; i += 2) {\n        adjust(moves[i], moves[i+1]);\n        std::cout << \"pos=(\" << seed035_focus_x << \",\" << seed035_focus_y << \")\"\n                  << \" center=\" << (seed035_is_center()?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint sum(const int (&arr)[N]) {\n    int s = 0;\n    for (auto v : arr) s += v;\n    return s;\n}\n\n} // namespace af_c\n\nint main() {\n    using namespace af_c;\n    const int mv[4] = {5,10,-20,-20};\n    test(mv);\n    std::cout << \"sum=\" << sum(mv) << \"\\n\";\n    return 0;\n}\n"}
{"program":36,"context":"RFID reader timeout configuration","noncompliant":"// -------- Non-Compliant Program (prog_036_nc.cpp)\n// Context: RFID reader timeout behavior\n#include <iostream>\n#include \"seedNC.h\" // seed036_timeout_ms, seed036_should_retry()\n\nnamespace rfid_nc {\n\nint simulate_attempts(int times[], std::size_t n) { // NC: array decay\n    int retries = 0;\n    for (std::size_t i = 0; i < n; ++i) {\n        bool retry = seed036_should_retry(times[i]); // header fn (NC)\n        std::cout << \"elapsed=\" << times[i]\n                  << \" retry=\" << (retry ? \"Y\" : \"N\") << \"\\n\";\n        if (retry) ++retries;\n    }\n    return retries;\n}\n\nint sum(int arr[], std::size_t n) { // NC\n    int s = 0;\n    for (std::size_t i = 0; i < n; ++i) s += arr[i];\n    return s;\n}\n\n} // namespace rfid_nc\n\nint main() {\n    using namespace rfid_nc;\n    int t[5] = {50,100,200,260,300};\n    int r = simulate_attempts(t,5);\n    std::cout << \"retries=\" << r << \" sum=\" << sum(t,5) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_036_c.cpp)\n// Context: RFID reader timeout behavior — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nint seed036_timeout_ms = 250;\nbool seed036_should_retry(int elapsed) { return elapsed < seed036_timeout_ms; }\n\nnamespace rfid_c {\n\nint simulate_attempts(const int (&times)[5]) {\n    int retries = 0;\n    for (int t : times) {\n        bool retry = seed036_should_retry(t);\n        std::cout << \"elapsed=\" << t\n                  << \" retry=\" << (retry ? \"Y\" : \"N\") << \"\\n\";\n        if (retry) ++retries;\n    }\n    return retries;\n}\n\nint sum(const int (&arr)[5]) {\n    int s = 0;\n    for (int v : arr) s += v;\n    return s;\n}\n\n} // namespace rfid_c\n\nint main() {\n    using namespace rfid_c;\n    const int t[5] = {50,100,200,260,300};\n    int r = simulate_attempts(t);\n    std::cout << \"retries=\" << r << \" sum=\" << sum(t) << \"\\n\";\n    return 0;\n}\n"}
{"program":37,"context":"Industrial PLC valve timing parameters","noncompliant":"// -------- Non-Compliant Program (prog_037_nc.cpp)\n// Context: PLC valve timing\n#include <iostream>\n#include \"seedNC.h\" // seed037_open_ms, seed037_close_ms, seed037_cycle_time()\n\nnamespace plc_nc {\n\nvoid run_cycle(int vals[], std::size_t n) { // NC decay\n    for (std::size_t i = 0; i < n; ++i) {\n        int total = seed037_cycle_time();\n        std::cout << \"cycle=\" << i\n                  << \" open=\" << seed037_open_ms\n                  << \" close=\" << seed037_close_ms\n                  << \" total=\" << total << \"\\n\";\n    }\n}\n\nint sum(int arr[], std::size_t n) { // NC\n    int s = 0; for (std::size_t i = 0; i < n; ++i) s += arr[i]; return s;\n}\n\n} // namespace plc_nc\n\nint main() {\n    using namespace plc_nc;\n    int v[3] = {1,1,1};\n    run_cycle(v,3);\n    std::cout << \"sum=\" << sum(v,3) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_037_c.cpp)\n// Context: PLC valve timing — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nint seed037_open_ms = 40;\nint seed037_close_ms = 30;\nint seed037_cycle_time() { return seed037_open_ms + seed037_close_ms; }\n\nnamespace plc_c {\n\ntemplate <std::size_t N>\nvoid run_cycle(const int (&vals)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        int total = seed037_cycle_time();\n        std::cout << \"cycle=\" << i\n                  << \" open=\" << seed037_open_ms\n                  << \" close=\" << seed037_close_ms\n                  << \" total=\" << total << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint sum(const int (&arr)[N]) {\n    int s = 0; for (auto v : arr) s += v; return s;\n}\n\n} // namespace plc_c\n\nint main() {\n    using namespace plc_c;\n    const int v[3] = {1,1,1};\n    run_cycle(v);\n    std::cout << \"sum=\" << sum(v) << \"\\n\";\n    return 0;\n}\n"}
{"program":38,"context":"Wearable device step detection threshold","noncompliant":"// -------- Non-Compliant Program (prog_038_nc.cpp)\n// Context: Wearable device step detection\n#include <iostream>\n#include \"seedNC.h\" // seed038_step_threshold, seed038_is_step()\n\nnamespace step_nc {\n\nvoid detect(float vals[], std::size_t n) { // NC array decay\n    for (std::size_t i = 0; i < n; ++i) {\n        bool st = seed038_is_step(vals[i]);\n        std::cout << \"acc=\" << vals[i] << \" step=\" << (st?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\nfloat maxv(float arr[], std::size_t n) { // NC\n    float m = 0.0F; for (std::size_t i=0;i<n;++i) if(arr[i]>m) m=arr[i]; return m;\n}\n\n} // namespace step_nc\n\nint main() {\n    using namespace step_nc;\n    float a[6] = {0.8F,1.0F,1.4F,1.6F,1.2F,0.7F};\n    detect(a,6);\n    std::cout << \"max=\" << maxv(a,6) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_038_c.cpp)\n// Context: Wearable device step detection — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nfloat seed038_step_threshold = 1.3F;\nbool seed038_is_step(float v) { return v > seed038_step_threshold; }\n\nnamespace step_c {\n\ntemplate <std::size_t N>\nvoid detect(const float (&vals)[N]) {\n    for (auto v : vals) {\n        bool st = seed038_is_step(v);\n        std::cout << \"acc=\" << v << \" step=\" << (st?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nfloat maxv(const float (&arr)[N]) {\n    float m = 0.0F; for (auto v : arr) if (v > m) m = v; return m;\n}\n\n} // namespace step_c\n\nint main() {\n    using namespace step_c;\n    const float a[6] = {0.8F,1.0F,1.4F,1.6F,1.2F,0.7F};\n    detect(a);\n    std::cout << \"max=\" << maxv(a) << \"\\n\";\n    return 0;\n}\n"}
{"program":39,"context":"Drone IMU calibration scale factors","noncompliant":"// -------- Non-Compliant Program (prog_039_nc.cpp)\n// Context: Drone IMU calibration\n#include <iostream>\n#include \"seedNC.h\" // seed039_acc_scale, seed039_calibrate()\n\nnamespace imu_nc {\n\nfloat clamp(float v) { return v; }\n\nvoid apply(float vals[], std::size_t n) { // NC\n    for (std::size_t i = 0; i < n; ++i) {\n        float out = seed039_calibrate(vals[i]);\n        std::cout << \"raw=\" << vals[i]\n                  << \" scale=\" << seed039_acc_scale\n                  << \" cal=\" << out << \"\\n\";\n    }\n}\n\nfloat avg(float arr[], std::size_t n) {\n    float s=0; for(size_t i=0;i<n;++i) s+=arr[i]; return (n? s/n:0);\n}\n\n} // namespace imu_nc\n\nint main() {\n    using namespace imu_nc;\n    float d[5] = {0.1F,0.2F,0.3F,0.4F,0.5F};\n    apply(d,5);\n    std::cout << \"avg=\" << avg(d,5) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_039_c.cpp)\n// Context: Drone IMU calibration — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nfloat seed039_acc_scale = 0.98F;\nfloat seed039_calibrate(float v) { return v * seed039_acc_scale; }\n\nnamespace imu_c {\n\nfloat clamp(float v) { return v; }\n\ntemplate <std::size_t N>\nvoid apply(const float (&vals)[N]) {\n    for (auto v : vals) {\n        float out = seed039_calibrate(v);\n        std::cout << \"raw=\" << v\n                  << \" scale=\" << seed039_acc_scale\n                  << \" cal=\" << out << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nfloat avg(const float (&arr)[N]) {\n    float s=0; for (auto v : arr) s+=v; return (N? s/N:0);\n}\n\n} // namespace imu_c\n\nint main() {\n    using namespace imu_c;\n    const float d[5] = {0.1F,0.2F,0.3F,0.4F,0.5F};\n    apply(d);\n    std::cout << \"avg=\" << avg(d) << \"\\n\";\n    return 0;\n}\n"}
{"program":40,"context":"Network latency smoothing parameter header","noncompliant":"// -------- Non-Compliant Program (prog_040_nc.cpp)\n// Context: Network latency smoothing\n#include <iostream>\n#include \"seedNC.h\" // seed040_latency_alpha, seed040_smooth()\n\nnamespace net_nc {\n\nvoid process(double prev[], double curr[], std::size_t n) { // NC: array decay\n    for (std::size_t i = 0; i < n; ++i) {\n        double out = seed040_smooth(prev[i], curr[i]); // NC header fn\n        std::cout << \"prev=\" << prev[i]\n                  << \" curr=\" << curr[i]\n                  << \" out=\" << out\n                  << \"\\n\";\n    }\n}\n\ndouble maxd(double arr[], std::size_t n) {\n    double m=0; for(size_t i=0;i<n;++i) if(arr[i]>m) m=arr[i]; return m;\n}\n\n} // namespace net_nc\n\nint main() {\n    using namespace net_nc;\n    double p[4] = {10,20,30,40};\n    double c[4] = {15,22,28,42};\n    process(p,c,4);\n    std::cout << \"max_prev=\" << maxd(p,4) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_040_c.cpp)\n// Context: Network latency smoothing — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\ndouble seed040_latency_alpha = 0.7;\ndouble seed040_smooth(double prev, double curr) {\n    return seed040_latency_alpha * prev + (1 - seed040_latency_alpha) * curr;\n}\n\nnamespace net_c {\n\ntemplate <std::size_t N>\nvoid process(const double (&prev)[N], const double (&curr)[N]) {\n    for (std::size_t i = 0; i < N; ++i) {\n        double out = seed040_smooth(prev[i], curr[i]);\n        std::cout << \"prev=\" << prev[i]\n                  << \" curr=\" << curr[i]\n                  << \" out=\" << out\n                  << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\ndouble maxd(const double (&arr)[N]) {\n    double m=0; for(auto v:arr) if(v>m) m=v; return m;\n}\n\n} // namespace net_c\n\nint main() {\n    using namespace net_c;\n    const double p[4] = {10,20,30,40};\n    const double c[4] = {15,22,28,42};\n    process(p,c);\n    std::cout << \"max_prev=\" << maxd(p) << \"\\n\";\n    return 0;\n}\n"}
{"program":41,"context":"Wind turbine blade angle configuration","noncompliant":"// -------- Non-Compliant Program (prog_041_nc.cpp)\n// Context: Wind turbine blade angle logic\n#include <iostream>\n#include \"seedNC.h\" // seed041_blade_angle, seed041_is_safe()\n\nnamespace wind_nc {\n\nfloat clamp(float a) { return (a < 0)?0:a; }\n\nvoid assess(float angles[], std::size_t n) { // NC: decayed array\n    for (std::size_t i = 0; i < n; ++i) {\n        float c = clamp(angles[i]);\n        bool safe = seed041_is_safe(c);\n        std::cout << \"blade=\" << c\n                  << \" lim=\" << seed041_blade_angle\n                  << \" safe=\" << (safe?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\nfloat maxv(float arr[], std::size_t n) { // NC\n    float m=0; for(size_t i=0;i<n;++i) if(arr[i]>m) m=arr[i]; return m;\n}\n\n} // namespace wind_nc\n\nint main(){\n    using namespace wind_nc;\n    float a[5] = {5.0F,10.0F,12.0F,14.0F,8.0F};\n    assess(a,5);\n    std::cout << \"max=\" << maxv(a,5) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_041_c.cpp)\n// Context: Wind turbine blade angle logic — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nfloat seed041_blade_angle = 12.5F;\nbool seed041_is_safe(float a) { return a < seed041_blade_angle; }\n\nnamespace wind_c {\n\nfloat clamp(float a) { return (a < 0)?0:a; }\n\ntemplate <std::size_t N>\nvoid assess(const float (&angles)[N]) {\n    for (auto a : angles) {\n        float c = clamp(a);\n        bool safe = seed041_is_safe(c);\n        std::cout << \"blade=\" << c\n                  << \" lim=\" << seed041_blade_angle\n                  << \" safe=\" << (safe?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nfloat maxv(const float (&arr)[N]) {\n    float m=0; for(auto v:arr) if(v>m) m=v; return m;\n}\n\n} // namespace wind_c\n\nint main(){\n    using namespace wind_c;\n    const float a[5]={5,10,12,14,8};\n    assess(a);\n    std::cout << \"max=\" << maxv(a) << \"\\n\";\n    return 0;\n}\n"}
{"program":42,"context":"Thermal printer heat pulse configuration","noncompliant":"// -------- Non-Compliant Program (prog_042_nc.cpp)\n// Context: Thermal printer pulse timing\n#include <iostream>\n#include \"seedNC.h\" // seed042_pulse_width_us, seed042_calc_total()\n\nnamespace print_nc {\n\nvoid simulate(int pulses[], std::size_t n) { // NC\n    for (std::size_t i=0;i<n;++i) {\n        int t = seed042_calc_total(pulses[i]);\n        std::cout << \"p=\" << pulses[i]\n                  << \" width=\" << seed042_pulse_width_us\n                  << \" total=\" << t << \"\\n\";\n    }\n}\n\nint sum(int arr[], std::size_t n){int s=0;for(size_t i=0;i<n;++i)s+=arr[i];return s;}\n\n} // namespace print_nc\n\nint main(){\n    using namespace print_nc;\n    int p[4]={1,2,3,5};\n    simulate(p,4);\n    std::cout << \"sum=\" << sum(p,4) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_042_c.cpp)\n// Context: Thermal printer pulse timing — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nint seed042_pulse_width_us = 220;\nint seed042_calc_total(int pulses){ return pulses * seed042_pulse_width_us; }\n\nnamespace print_c {\n\ntemplate <std::size_t N>\nvoid simulate(const int (&pulses)[N]) {\n    for (auto p : pulses) {\n        int t = seed042_calc_total(p);\n        std::cout << \"p=\" << p\n                  << \" width=\" << seed042_pulse_width_us\n                  << \" total=\" << t << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint sum(const int (&arr)[N]) { int s=0; for(auto v:arr) s+=v; return s; }\n\n} // namespace print_c\n\nint main(){\n    using namespace print_c;\n    const int p[4]={1,2,3,5};\n    simulate(p);\n    std::cout << \"sum=\" << sum(p) << \"\\n\";\n    return 0;\n}\n"}
{"program":43,"context":"Biosignal ECG threshold header","noncompliant":"// -------- Non-Compliant Program (prog_043_nc.cpp)\n// Context: ECG peak detection\n#include <iostream>\n#include \"seedNC.h\" // seed043_ecg_threshold, seed043_is_peak()\n\nnamespace ecg_nc {\n\nvoid detect(float s[], std::size_t n) { // NC\n    for(size_t i=0;i<n;++i){\n        bool pk = seed043_is_peak(s[i]);\n        std::cout << \"v=\" << s[i]\n                  << \" thr=\" << seed043_ecg_threshold\n                  << \" peak=\" << (pk?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\nfloat maxv(float arr[], std::size_t n){float m=0;for(size_t i=0;i<n;++i)if(arr[i]>m)m=arr[i];return m;}\n\n} // namespace ecg_nc\n\nint main(){\n    using namespace ecg_nc;\n    float v[6]={0.2F,0.3F,0.5F,0.6F,0.4F,0.1F};\n    detect(v,6);\n    std::cout << \"max=\" << maxv(v,6)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_043_c.cpp)\n// Context: ECG peak detection — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nfloat seed043_ecg_threshold = 0.45F;\nbool seed043_is_peak(float v){ return v > seed043_ecg_threshold; }\n\nnamespace ecg_c {\n\ntemplate <std::size_t N>\nvoid detect(const float (&s)[N]) {\n    for (auto v : s) {\n        bool pk = seed043_is_peak(v);\n        std::cout << \"v=\" << v\n                  << \" thr=\" << seed043_ecg_threshold\n                  << \" peak=\" << (pk?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nfloat maxv(const float (&arr)[N]) {\n    float m=0; for(auto v:arr) if(v>m)m=v; return m;\n}\n\n} // namespace ecg_c\n\nint main(){\n    using namespace ecg_c;\n    const float v[6]={0.2F,0.3F,0.5F,0.6F,0.4F,0.1F};\n    detect(v);\n    std::cout << \"max=\" << maxv(v)<<\"\\n\";\n    return 0;\n}\n"}
{"program":44,"context":"Data compression dictionary size header","noncompliant":"// -------- Non-Compliant Program (prog_044_nc.cpp)\n// Context: Compression dictionary logic\n#include <iostream>\n#include \"seedNC.h\" // seed044_dict_size, seed044_max_code()\n\nnamespace dict_nc {\n\nvoid show(int vals[], std::size_t n) { // NC\n    for(size_t i=0;i<n;++i){\n        int code = seed044_max_code();\n        std::cout << \"in=\" << vals[i]\n                  << \" dict=\" << seed044_dict_size\n                  << \" code=\" << code << \"\\n\";\n    }\n}\n\nint sum(int arr[], std::size_t n){int s=0;for(size_t i=0;i<n;++i)s+=arr[i];return s;}\n\n} // namespace dict_nc\n\nint main(){\n    using namespace dict_nc;\n    int d[4]={1,2,3,4};\n    show(d,4);\n    std::cout << \"sum=\" << sum(d,4) <<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_044_c.cpp)\n// Context: Compression dictionary logic — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nint seed044_dict_size = 4096;\nint seed044_max_code(){ return seed044_dict_size - 1; }\n\nnamespace dict_c {\n\ntemplate <std::size_t N>\nvoid show(const int (&vals)[N]) {\n    for(auto v:vals){\n        int code = seed044_max_code();\n        std::cout << \"in=\" << v\n                  << \" dict=\" << seed044_dict_size\n                  << \" code=\" << code << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint sum(const int (&arr)[N]){int s=0; for(auto v:arr)s+=v; return s;}\n\n} // namespace dict_c\n\nint main(){\n    using namespace dict_c;\n    const int d[4]={1,2,3,4};\n    show(d);\n    std::cout << \"sum=\" << sum(d)<<\"\\n\";\n    return 0;\n}\n"}
{"program":45,"context":"Smartwatch heart rate smoothing window","noncompliant":"// -------- Non-Compliant Program (prog_045_nc.cpp)\n// Context: HR smoothing window\n#include <iostream>\n#include \"seedNC.h\" // seed045_smooth_window, seed045_smooth_hr()\n\nnamespace hr_nc {\n\nvoid smooth(float* v, std::size_t n) { // NC pointer\n    for(size_t i=0;i<n;++i){\n        float out = seed045_smooth_hr(&v[i]);\n        std::cout << \"hr=\" << v[i]\n                  << \" win=\" << seed045_smooth_window\n                  << \" out=\" << out <<\"\\n\";\n    }\n}\n\nfloat avg(float arr[], std::size_t n){float s=0;for(size_t i=0;i<n;++i)s+=arr[i];return n?s/n:0;}\n\n} // namespace hr_nc\n\nint main(){\n    using namespace hr_nc;\n    float h[5]={70,72,78,80,75};\n    smooth(h,5);\n    std::cout << \"avg=\" << avg(h,5)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_045_c.cpp)\n// Context: HR smoothing window — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nint seed045_smooth_window = 6;\nfloat seed045_smooth_hr(float* vals){ return vals[0]; }\n\nnamespace hr_c {\n\ntemplate <std::size_t N>\nvoid smooth(const float (&v)[N]) {\n    for(size_t i=0;i<N;++i){\n        float tmp=v[i];\n        float out = seed045_smooth_hr(&tmp);\n        std::cout << \"hr=\" << v[i]\n                  << \" win=\" << seed045_smooth_window\n                  << \" out=\" << out <<\"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nfloat avg(const float (&arr)[N]){float s=0;for(auto v:arr)s+=v;return N?s/N:0;}\n\n} // namespace hr_c\n\nint main(){\n    using namespace hr_c;\n    const float h[5]={70,72,78,80,75};\n    smooth(h);\n    std::cout << \"avg=\" << avg(h)<<\"\\n\";\n    return 0;\n}\n"}
{"program":46,"context":"Robotic vacuum mapping resolution header","noncompliant":"// -------- Non-Compliant Program (prog_046_nc.cpp)\n// Context: Robotic vacuum map resolution logic\n#include <iostream>\n#include \"seedNC.h\" // seed046_map_resolution, seed046_cells()\n\nnamespace vac_nc {\n\nvoid compute(int meters[], std::size_t n) { // NC: array decay\n    for (std::size_t i = 0; i < n; ++i) {\n        int cells = seed046_cells(meters[i]);\n        std::cout << \"m=\" << meters[i]\n                  << \" resolution=\" << seed046_map_resolution\n                  << \" cells=\" << cells << \"\\n\";\n    }\n}\n\nint sum(int arr[], std::size_t n) {\n    int s=0; for(size_t i=0;i<n;++i)s+=arr[i]; return s;\n}\n\n} // namespace vac_nc\n\nint main(){\n    using namespace vac_nc;\n    int d[5]={1,2,3,4,5};\n    compute(d,5);\n    std::cout << \"sum=\" << sum(d,5) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_046_c.cpp)\n// Context: Robotic vacuum map resolution logic — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nint seed046_map_resolution = 50;\nint seed046_cells(int meters){ return meters * seed046_map_resolution; }\n\nnamespace vac_c {\n\ntemplate <std::size_t N>\nvoid compute(const int (&meters)[N]) {\n    for (auto m : meters) {\n        int cells = seed046_cells(m);\n        std::cout << \"m=\" << m\n                  << \" resolution=\" << seed046_map_resolution\n                  << \" cells=\" << cells << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint sum(const int (&arr)[N]){\n    int s=0; for(auto v:arr)s+=v; return s;\n}\n\n} // namespace vac_c\n\nint main(){\n    using namespace vac_c;\n    const int d[5]={1,2,3,4,5};\n    compute(d);\n    std::cout << \"sum=\" << sum(d) << \"\\n\";\n    return 0;\n}\n"}
{"program":47,"context":"Audio synthesizer detune factor table","noncompliant":"// -------- Non-Compliant Program (prog_047_nc.cpp)\n// Context: Audio synthesizer detune logic\n#include <iostream>\n#include \"seedNC.h\" // seed047_detune, seed047_apply()\n\nnamespace synth_nc {\n\nvoid apply_all(float freqs[], std::size_t n) { // NC\n    for (std::size_t i=0;i<n;++i) {\n        float out = seed047_apply(freqs[i]);\n        std::cout << \"f=\" << freqs[i]\n                  << \" detune=\" << seed047_detune\n                  << \" out=\" << out << \"\\n\";\n    }\n}\n\nfloat maxf(float arr[], std::size_t n){ float m=0; for(size_t i=0;i<n;++i) if(arr[i]>m)m=arr[i]; return m; }\n\n} // namespace synth_nc\n\nint main(){\n    using namespace synth_nc;\n    float f[4]={440,880,220,110};\n    apply_all(f,4);\n    std::cout << \"max=\" << maxf(f,4) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_047_c.cpp)\n// Context: Audio synthesizer detune logic — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nfloat seed047_detune = 0.03F;\nfloat seed047_apply(float f){ return f + (f * seed047_detune); }\n\nnamespace synth_c {\n\ntemplate <std::size_t N>\nvoid apply_all(const float (&freqs)[N]) {\n    for (auto f : freqs) {\n        float out = seed047_apply(f);\n        std::cout << \"f=\" << f\n                  << \" detune=\" << seed047_detune\n                  << \" out=\" << out << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nfloat maxf(const float (&arr)[N]){\n    float m=0; for(auto v:arr) if(v>m)m=v; return m;\n}\n\n} // namespace synth_c\n\nint main(){\n    using namespace synth_c;\n    const float f[4]={440,880,220,110};\n    apply_all(f);\n    std::cout << \"max=\" << maxf(f) << \"\\n\";\n    return 0;\n}\n"}
{"program":48,"context":"Machine tool spindle RPM limiter header","noncompliant":"// -------- Non-Compliant Program (prog_048_nc.cpp)\n// Context: Spindle RPM limiter\n#include <iostream>\n#include \"seedNC.h\" // seed048_rpm_limit, seed048_is_allowed()\n\nnamespace rpm_nc {\n\nvoid test(int vals[], std::size_t n) { // NC\n    for(std::size_t i=0;i<n;++i){\n        bool ok = seed048_is_allowed(vals[i]);\n        std::cout << \"rpm=\" << vals[i]\n                  << \" lim=\" << seed048_rpm_limit\n                  << \" ok=\" << (ok?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\nint maxv(int arr[], std::size_t n){int m=0; for(size_t i=0;i<n;++i) if(arr[i]>m)m=arr[i]; return m;}\n\n} // namespace rpm_nc\n\nint main(){\n    using namespace rpm_nc;\n    int r[5]={8000,9000,12000,15000,7000};\n    test(r,5);\n    std::cout << \"max=\" << maxv(r,5) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_048_c.cpp)\n// Context: Spindle RPM limiter — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nint seed048_rpm_limit = 12000;\nbool seed048_is_allowed(int rpm){ return rpm <= seed048_rpm_limit; }\n\nnamespace rpm_c {\n\ntemplate <std::size_t N>\nvoid test(const int (&vals)[N]) {\n    for (auto v : vals) {\n        bool ok = seed048_is_allowed(v);\n        std::cout << \"rpm=\" << v\n                  << \" lim=\" << seed048_rpm_limit\n                  << \" ok=\" << (ok?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint maxv(const int (&arr)[N]){int m=0; for(auto v:arr) if(v>m)m=v; return m;}\n\n} // namespace rpm_c\n\nint main(){\n    using namespace rpm_c;\n    const int r[5]={8000,9000,12000,15000,7000};\n    test(r);\n    std::cout << \"max=\" << maxv(r) << \"\\n\";\n    return 0;\n}\n"}
{"program":49,"context":"Network routing retry limit header","noncompliant":"// -------- Non-Compliant Program (prog_049_nc.cpp)\n// Context: Routing retry control\n#include <iostream>\n#include \"seedNC.h\" // seed049_retry_limit, seed049_should_retry()\n\nnamespace route_nc {\n\nvoid simulate(int hops[], std::size_t n) { // NC\n    for(size_t i=0;i<n;++i){\n        bool retry = seed049_should_retry(hops[i]);\n        std::cout << \"hop=\" << hops[i]\n                  << \" lim=\" << seed049_retry_limit\n                  << \" retry=\" << (retry?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\nint sum(int a[], std::size_t n){int s=0;for(size_t i=0;i<n;++i)s+=a[i];return s;}\n\n} // namespace route_nc\n\nint main(){\n    using namespace route_nc;\n    int h[5]={0,1,2,5,7};\n    simulate(h,5);\n    std::cout << \"sum=\" << sum(h,5)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_049_c.cpp)\n// Context: Routing retry control — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nint seed049_retry_limit = 5;\nbool seed049_should_retry(int n){ return n < seed049_retry_limit; }\n\nnamespace route_c {\n\ntemplate <std::size_t N>\nvoid simulate(const int (&hops)[N]) {\n    for (auto h : hops) {\n        bool retry = seed049_should_retry(h);\n        std::cout << \"hop=\" << h\n                  << \" lim=\" << seed049_retry_limit\n                  << \" retry=\" << (retry?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint sum(const int (&arr)[N]){int s=0; for(auto v:arr)s+=v; return s;}\n\n} // namespace route_c\n\nint main(){\n    using namespace route_c;\n    const int h[5]={0,1,2,5,7};\n    simulate(h);\n    std::cout << \"sum=\" << sum(h)<<\"\\n\";\n    return 0;\n}\n"}
{"program":50,"context":"Display color temperature adjustment header","noncompliant":"// -------- Non-Compliant Program (prog_050_nc.cpp)\n// Context: Display color temperature\n#include <iostream>\n#include \"seedNC.h\" // seed050_color_temp, seed050_adjust()\n\nnamespace temp_nc {\n\nvoid test(int amb[], std::size_t n) { // NC\n    for(size_t i=0;i<n;++i){\n        int out = seed050_adjust(amb[i]);\n        std::cout << \"amb=\" << amb[i]\n                  << \" base=\" << seed050_color_temp\n                  << \" out=\" << out <<\"\\n\";\n    }\n}\n\nint maxv(int a[], std::size_t n){int m=0;for(size_t i=0;i<n;++i)if(a[i]>m)m=a[i];return m;}\n\n} // namespace temp_nc\n\nint main(){\n    using namespace temp_nc;\n    int a[5]={100,200,300,150,50};\n    test(a,5);\n    std::cout << \"max=\" << maxv(a,5)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_050_c.cpp)\n// Context: Display color temperature — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nint seed050_color_temp = 6500;\nint seed050_adjust(int ambient){ return seed050_color_temp + ambient; }\n\nnamespace temp_c {\n\ntemplate <std::size_t N>\nvoid test(const int (&amb)[N]) {\n    for (auto v : amb) {\n        int out = seed050_adjust(v);\n        std::cout << \"amb=\" << v\n                  << \" base=\" << seed050_color_temp\n                  << \" out=\" << out <<\"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint maxv(const int (&arr)[N]){int m=0;for(auto v:arr)if(v>m)m=v;return m;}\n\n} // namespace temp_c\n\nint main(){\n    using namespace temp_c;\n    const int a[5]={100,200,300,150,50};\n    test(a);\n    std::cout << \"max=\" << maxv(a)<<\"\\n\";\n    return 0;\n}\n"}
{"program":51,"context":"Battery charge cycle counters header","noncompliant":"// -------- Non-Compliant Program (prog_051_nc.cpp)\n// Context: Battery cycle counting logic\n#include <iostream>\n#include \"seedNC.h\" // seed051_cycle_count, seed051_increment()\n\nnamespace batt_nc {\n\nvoid simulate(unsigned int ops[], std::size_t n) { // NC decay\n    for (std::size_t i=0;i<n;++i) {\n        for (unsigned int j=0;j<ops[i];++j) {\n            seed051_increment(); // defined in header (NC)\n        }\n        std::cout << \"ops=\" << ops[i] << \" total=\" << seed051_cycle_count << \"\\n\";\n    }\n}\n\nunsigned int sum(unsigned int arr[], std::size_t n){ unsigned int s=0; for(size_t i=0;i<n;++i)s+=arr[i]; return s; }\n\n} // namespace batt_nc\n\nint main(){\n    using namespace batt_nc;\n    unsigned int a[4]={1,2,3,1};\n    simulate(a,4);\n    std::cout << \"sum=\" << sum(a,4) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_051_c.cpp)\n// Context: Battery cycle counting — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nunsigned int seed051_cycle_count = 0;\nvoid seed051_increment(){ ++seed051_cycle_count; }\n\nnamespace batt_c {\n\ntemplate <std::size_t N>\nvoid simulate(const unsigned int (&ops)[N]) {\n    for (auto v : ops) {\n        for (unsigned int j=0;j<v;++j) seed051_increment();\n        std::cout << \"ops=\" << v << \" total=\" << seed051_cycle_count << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nunsigned int sum(const unsigned int (&arr)[N]){\n    unsigned int s=0; for(auto v:arr)s+=v; return s;\n}\n\n} // namespace batt_c\n\nint main(){\n    using namespace batt_c;\n    const unsigned int a[4]={1,2,3,1};\n    simulate(a);\n    std::cout << \"sum=\" << sum(a) << \"\\n\";\n    return 0;\n}\n"}
{"program":52,"context":"Satellite telemetry beacon interval header","noncompliant":"// -------- Non-Compliant Program (prog_052_nc.cpp)\n// Context: Satellite beacon interval logic\n#include <iostream>\n#include \"seedNC.h\" // seed052_beacon_interval_s, seed052_next_interval()\n\nnamespace sat_nc {\n\nvoid schedule(int times[], std::size_t n) { // NC\n    for(size_t i=0;i<n;++i){\n        int nxt = seed052_next_interval();\n        std::cout << \"t=\" << times[i]\n                  << \" int=\" << seed052_beacon_interval_s\n                  << \" next=\" << nxt << \"\\n\";\n    }\n}\n\nint sum(int arr[], std::size_t n){int s=0;for(size_t i=0;i<n;++i)s+=arr[i];return s;}\n\n} // namespace sat_nc\n\nint main(){\n    using namespace sat_nc;\n    int t[4]={0,60,120,180};\n    schedule(t,4);\n    std::cout << \"sum=\" << sum(t,4)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_052_c.cpp)\n// Context: Satellite beacon interval — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nint seed052_beacon_interval_s = 60;\nint seed052_next_interval(){ return seed052_beacon_interval_s; }\n\nnamespace sat_c {\n\ntemplate <std::size_t N>\nvoid schedule(const int (&times)[N]){\n    for(auto t:times){\n        int nxt = seed052_next_interval();\n        std::cout << \"t=\" << t\n                  << \" int=\" << seed052_beacon_interval_s\n                  << \" next=\" << nxt << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint sum(const int (&arr)[N]){int s=0;for(auto v:arr)s+=v;return s;}\n\n} // namespace sat_c\n\nint main(){\n    using namespace sat_c;\n    const int t[4]={0,60,120,180};\n    schedule(t);\n    std::cout << \"sum=\" << sum(t)<<\"\\n\";\n    return 0;\n}\n"}
{"program":53,"context":"Industrial sensor hysteresis header","noncompliant":"// -------- Non-Compliant Program (prog_053_nc.cpp)\n// Context: Hysteresis switching logic\n#include <iostream>\n#include \"seedNC.h\" // seed053_hysteresis, seed053_should_switch()\n\nnamespace hyst_nc {\n\nvoid detect(float vals[], std::size_t n) { // NC\n    for (size_t i=1;i<n;++i) {\n        bool sw = seed053_should_switch(vals[i-1], vals[i]);\n        std::cout << \"old=\" << vals[i-1]\n                  << \" new=\" << vals[i]\n                  << \" hyst=\" << seed053_hysteresis\n                  << \" sw=\" << (sw?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\nfloat maxv(float arr[], std::size_t n){float m=0;for(size_t i=0;i<n;++i)if(arr[i]>m)m=arr[i];return m;}\n\n} // namespace hyst_nc\n\nint main(){\n    using namespace hyst_nc;\n    float v[5]={1.0F,1.3F,1.9F,2.0F,2.2F};\n    detect(v,5);\n    std::cout << \"max=\" << maxv(v,5)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_053_c.cpp)\n// Context: Hysteresis logic — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nfloat seed053_hysteresis = 0.5F;\nbool seed053_should_switch(float oldv, float newv){ return (newv - oldv) > seed053_hysteresis; }\n\nnamespace hyst_c {\n\ntemplate <std::size_t N>\nvoid detect(const float (&vals)[N]){\n    for(size_t i=1;i<N;++i){\n        bool sw = seed053_should_switch(vals[i-1], vals[i]);\n        std::cout << \"old=\" << vals[i-1]\n                  << \" new=\" << vals[i]\n                  << \" hyst=\" << seed053_hysteresis\n                  << \" sw=\" << (sw?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nfloat maxv(const float (&arr)[N]){float m=0;for(auto v:arr)if(v>m)m=v;return m;}\n\n} // namespace hyst_c\n\nint main(){\n    using namespace hyst_c;\n    const float v[5]={1.0F,1.3F,1.9F,2.0F,2.2F};\n    detect(v);\n    std::cout << \"max=\" << maxv(v)<<\"\\n\";\n    return 0;\n}\n"}
{"program":54,"context":"Smart grid load shedding threshold header","noncompliant":"// -------- Non-Compliant Program (prog_054_nc.cpp)\n// Context: Load shedding margin logic\n#include <iostream>\n#include \"seedNC.h\" // seed054_shed_threshold_kw, seed054_margin()\n\nnamespace grid_nc {\n\nvoid margin_test(double loads[], std::size_t n) { // NC\n    for (size_t i=0;i<n;++i) {\n        double m = seed054_margin(loads[i]);\n        std::cout << \"load=\" << loads[i]\n                  << \" thr=\" << seed054_shed_threshold_kw\n                  << \" margin=\" << m << \"\\n\";\n    }\n}\n\ndouble maxd(double arr[], std::size_t n){double m=0;for(size_t i=0;i<n;++i)if(arr[i]>m)m=arr[i];return m;}\n\n} // namespace grid_nc\n\nint main(){\n    using namespace grid_nc;\n    double l[5]={100,200,350,450,500};\n    margin_test(l,5);\n    std::cout << \"max=\" << maxd(l,5)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_054_c.cpp)\n// Context: Load shedding — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\ndouble seed054_shed_threshold_kw = 500.0;\ndouble seed054_margin(double load){ return seed054_shed_threshold_kw - load; }\n\nnamespace grid_c {\n\ntemplate <std::size_t N>\nvoid margin_test(const double (&loads)[N]){\n    for(auto v:loads){\n        double m = seed054_margin(v);\n        std::cout << \"load=\" << v\n                  << \" thr=\" << seed054_shed_threshold_kw\n                  << \" margin=\" << m << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\ndouble maxd(const double (&arr)[N]){double m=0;for(auto v:arr)if(v>m)m=v;return m;}\n\n} // namespace grid_c\n\nint main(){\n    using namespace grid_c;\n    const double l[5]={100,200,350,450,500};\n    margin_test(l);\n    std::cout << \"max=\" << maxd(l)<<\"\\n\";\n    return 0;\n}\n"}
{"program":55,"context":"3D printer extrusion multiplier header","noncompliant":"// -------- Non-Compliant Program (prog_055_nc.cpp)\n// Context: Extrusion multiplier logic\n#include <iostream>\n#include \"seedNC.h\" // seed055_extrude_mult, seed055_scaled()\n\nnamespace extr_nc {\n\nvoid adjust(float v[], std::size_t n) { // NC\n    for(size_t i=0;i<n;++i){\n        float out = seed055_scaled(v[i]);\n        std::cout << \"in=\" << v[i]\n                  << \" mult=\" << seed055_extrude_mult\n                  << \" out=\" << out << \"\\n\";\n    }\n}\n\nfloat avg(float arr[], std::size_t n){float s=0;for(size_t i=0;i<n;++i)s+=arr[i];return n?s/n:0;}\n\n} // namespace extr_nc\n\nint main(){\n    using namespace extr_nc;\n    float vals[4]={1.0F,1.2F,0.9F,1.1F};\n    adjust(vals,4);\n    std::cout << \"avg=\" << avg(vals,4)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_055_c.cpp)\n// Context: Extrusion multiplier — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nfloat seed055_extrude_mult = 1.0F;\nfloat seed055_scaled(float v){ return v * seed055_extrude_mult; }\n\nnamespace extr_c {\n\ntemplate <std::size_t N>\nvoid adjust(const float (&vals)[N]) {\n    for(auto v:vals){\n        float out = seed055_scaled(v);\n        std::cout << \"in=\" << v\n                  << \" mult=\" << seed055_extrude_mult\n                  << \" out=\" << out << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nfloat avg(const float (&arr)[N]){float s=0;for(auto v:arr)s+=v;return N?s/N:0;}\n\n} // namespace extr_c\n\nint main(){\n    using namespace extr_c;\n    const float vals[4]={1.0F,1.2F,0.9F,1.1F};\n    adjust(vals);\n    std::cout << \"avg=\" << avg(vals)<<\"\\n\";\n    return 0;\n}\n"}
{"program":56,"context":"Smart lock retry timeout header","noncompliant":"// -------- Non-Compliant Program (prog_056_nc.cpp)\n// Context: Smart lock timeout system\n#include <iostream>\n#include \"seedNC.h\" // seed056_retry_timeout_ms, seed056_timeout_expired()\n\nnamespace lock_nc {\n\nvoid test(int arr[], std::size_t n){ // NC\n    for(size_t i=0;i<n;++i){\n        bool ex = seed056_timeout_expired(arr[i]);\n        std::cout << \"elapsed=\" << arr[i]\n                  << \" limit=\" << seed056_retry_timeout_ms\n                  << \" expired=\" << (ex?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\nint maxv(int a[], std::size_t n){int m=0;for(size_t i=0;i<n;++i)if(a[i]>m)m=a[i];return m;}\n\n} // namespace lock_nc\n\nint main(){\n    using namespace lock_nc;\n    int e[5]={500,2000,3500,1000,3000};\n    test(e,5);\n    std::cout << \"max=\" << maxv(e,5)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_056_c.cpp)\n// Context: Smart lock timeout — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nint seed056_retry_timeout_ms = 3000;\nbool seed056_timeout_expired(int elapsed){ return elapsed > seed056_retry_timeout_ms; }\n\nnamespace lock_c {\n\ntemplate <std::size_t N>\nvoid test(const int (&arr)[N]){\n    for(auto e:arr){\n        bool ex = seed056_timeout_expired(e);\n        std::cout << \"elapsed=\" << e\n                  << \" limit=\" << seed056_retry_timeout_ms\n                  << \" expired=\" << (ex?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint maxv(const int (&arr)[N]){int m=0;for(auto v:arr)if(v>m)m=v;return m;}\n\n} // namespace lock_c\n\nint main(){\n    using namespace lock_c;\n    const int e[5]={500,2000,3500,1000,3000};\n    test(e);\n    std::cout << \"max=\" << maxv(e)<<\"\\n\";\n    return 0;\n}\n"}
{"program":57,"context":"Environmental sensor sampling interval header","noncompliant":"// -------- Non-Compliant Program (prog_057_nc.cpp)\n// Context: Sensor sampling interval\n#include <iostream>\n#include \"seedNC.h\" // seed057_sample_interval_ms, seed057_half_interval()\n\nnamespace env_nc {\n\nvoid show(unsigned long v[], std::size_t n){ // NC\n    for(size_t i=0;i<n;++i){\n        auto half = seed057_half_interval();\n        std::cout << \"val=\" << v[i]\n                  << \" interval=\" << seed057_sample_interval_ms\n                  << \" half=\" << half << \"\\n\";\n    }\n}\n\nunsigned long maxv(unsigned long a[], std::size_t n){unsigned long m=0;for(size_t i=0;i<n;++i)if(a[i]>m)m=a[i];return m;}\n\n} // namespace env_nc\n\nint main(){\n    using namespace env_nc;\n    unsigned long v[4]={1000,1500,2000,500};\n    show(v,4);\n    std::cout << \"max=\" << maxv(v,4)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_057_c.cpp)\n// Context: Sensor sampling — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nunsigned long seed057_sample_interval_ms = 1000UL;\nunsigned long seed057_half_interval(){ return seed057_sample_interval_ms/2UL; }\n\nnamespace env_c {\n\ntemplate <std::size_t N>\nvoid show(const unsigned long (&v)[N]){\n    for(auto x:v){\n        auto half = seed057_half_interval();\n        std::cout << \"val=\" << x\n                  << \" interval=\" << seed057_sample_interval_ms\n                  << \" half=\" << half << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nunsigned long maxv(const unsigned long (&arr)[N]){unsigned long m=0;for(auto v:arr)if(v>m)m=v;return m;}\n\n} // namespace env_c\n\nint main(){\n    using namespace env_c;\n    const unsigned long v[4]={1000,1500,2000,500};\n    show(v);\n    std::cout << \"max=\" << maxv(v)<<\"\\n\";\n    return 0;\n}\n"}
{"program":58,"context":"Autopilot yaw control gain header","noncompliant":"// -------- Non-Compliant Program (prog_058_nc.cpp)\n// Context: Yaw control gain computation\n#include <iostream>\n#include \"seedNC.h\" // seed058_yaw_gain, seed058_compute()\n\nnamespace yaw_nc {\n\nvoid apply(double e[], std::size_t n){ // NC decay\n    for(size_t i=0;i<n;++i){\n        double out = seed058_compute(e[i]);\n        std::cout << \"err=\" << e[i]\n                  << \" gain=\" << seed058_yaw_gain\n                  << \" out=\" << out <<\"\\n\";\n    }\n}\n\ndouble maxd(double arr[], std::size_t n){double m=0;for(size_t i=0;i<n;++i)if(arr[i]>m)m=arr[i];return m;}\n\n} // namespace yaw_nc\n\nint main(){\n    using namespace yaw_nc;\n    double e[5]={0.1,0.2,0.4,0.3,0.5};\n    apply(e,5);\n    std::cout << \"max=\" << maxd(e,5)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_058_c.cpp)\n// Context: Yaw control — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\ndouble seed058_yaw_gain = 0.75;\ndouble seed058_compute(double err){ return err * seed058_yaw_gain; }\n\nnamespace yaw_c {\n\ntemplate <std::size_t N>\nvoid apply(const double (&e)[N]){\n    for(auto v:e){\n        double out = seed058_compute(v);\n        std::cout << \"err=\" << v\n                  << \" gain=\" << seed058_yaw_gain\n                  << \" out=\" << out <<\"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\ndouble maxd(const double (&arr)[N]){double m=0;for(auto v:arr)if(v>m)m=v;return m;}\n\n} // namespace yaw_c\n\nint main(){\n    using namespace yaw_c;\n    const double e[5]={0.1,0.2,0.4,0.3,0.5};\n    apply(e);\n    std::cout << \"max=\" << maxd(e)<<\"\\n\";\n    return 0;\n}\n"}
{"program":59,"context":"Camera white-balance preset header","noncompliant":"// -------- Non-Compliant Program (prog_059_nc.cpp)\n// Context: White balance temperature logic\n#include <iostream>\n#include \"seedNC.h\" // seed059_wb_temp, seed059_adjust()\n\nnamespace wb_nc {\n\nvoid test(int a[], std::size_t n){ // NC\n    for(size_t i=0;i<n;++i){\n        int out = seed059_adjust(a[i]);\n        std::cout << \"amb=\" << a[i]\n                  << \" base=\" << seed059_wb_temp\n                  << \" out=\" << out <<\"\\n\";\n    }\n}\n\nint maxv(int a[], std::size_t n){int m=0;for(size_t i=0;i<n;++i)if(a[i]>m)m=a[i];return m;}\n\n} // namespace wb_nc\n\nint main(){\n    using namespace wb_nc;\n    int a[5]={100,200,300,150,50};\n    test(a,5);\n    std::cout << \"max=\" << maxv(a,5)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_059_c.cpp)\n// Context: WB preset — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nint seed059_wb_temp = 5500;\nint seed059_adjust(int ambient){ return seed059_wb_temp + (ambient/100); }\n\nnamespace wb_c {\n\ntemplate <std::size_t N>\nvoid test(const int (&a)[N]){\n    for(auto v:a){\n        int out = seed059_adjust(v);\n        std::cout << \"amb=\" << v\n                  << \" base=\" << seed059_wb_temp\n                  << \" out=\" << out <<\"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint maxv(const int (&arr)[N]){int m=0;for(auto v:arr)if(v>m)m=v;return m;}\n\n} // namespace wb_c\n\nint main(){\n    using namespace wb_c;\n    const int a[5]={100,200,300,150,50};\n    test(a);\n    std::cout << \"max=\" << maxv(a)<<\"\\n\";\n    return 0;\n}\n"}
{"program":60,"context":"Bluetooth advertising interval header","noncompliant":"// -------- Non-Compliant Program (prog_060_nc.cpp)\n// Context: BT advertising interval logic\n#include <iostream>\n#include \"seedNC.h\" // seed060_adv_ms, seed060_get_adv()\n\nnamespace bt_nc {\n\nvoid test(unsigned short a[], std::size_t n){ // NC\n    for(size_t i=0;i<n;++i){\n        unsigned short out = seed060_get_adv();\n        std::cout << \"check=\" << a[i]\n                  << \" adv=\" << seed060_adv_ms\n                  << \" out=\" << out << \"\\n\";\n    }\n}\n\nunsigned short maxv(unsigned short a[], std::size_t n){unsigned short m=0;for(size_t i=0;i<n;++i)if(a[i]>m)m=a[i];return m;}\n\n} // namespace bt_nc\n\nint main(){\n    using namespace bt_nc;\n    unsigned short a[4]={50,100,150,200};\n    test(a,4);\n    std::cout << \"max=\" << maxv(a,4)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_060_c.cpp)\n// Context: BT advertising — COMPLIANT\n#include <iostream>\n#include \"seedC.h\"\n\n// Definitions\nunsigned short seed060_adv_ms = 100;\nunsigned short seed060_get_adv(){ return seed060_adv_ms; }\n\nnamespace bt_c {\n\ntemplate <std::size_t N>\nvoid test(const unsigned short (&a)[N]){\n    for(auto v:a){\n        unsigned short out = seed060_get_adv();\n        std::cout << \"check=\" << v\n                  << \" adv=\" << seed060_adv_ms\n                  << \" out=\" << out << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nunsigned short maxv(const unsigned short (&arr)[N]){\n    unsigned short m=0; for(auto v:arr)if(v>m)m=v; return m;\n}\n\n} // namespace bt_c\n\nint main(){\n    using namespace bt_c;\n    const unsigned short a[4]={50,100,150,200};\n    test(a);\n    std::cout << \"max=\" << maxv(a)<<\"\\n\";\n    return 0;\n}\n"}
{"program":61,"context":"Car infotainment audio buffer size header","noncompliant":"// -------- Non-Compliant Program (prog_061_nc.cpp)\n#include <iostream>\n#include \"seedNC.h\" // seed061_audio_buffer, seed061_half_buffer()\n\nnamespace audio_nc {\n\nvoid process(int samples[], std::size_t n) { // NC decay\n    for (size_t i=0;i<n;++i) {\n        int half = seed061_half_buffer();\n        std::cout << \"sample=\" << samples[i]\n                  << \" buf=\" << seed061_audio_buffer\n                  << \" half=\" << half << \"\\n\";\n    }\n}\n\nint peak(int arr[], std::size_t n) {\n    int m = arr[0];\n    for(size_t i=1;i<n;++i) if(arr[i] > m) m = arr[i];\n    return m;\n}\n\n} // namespace audio_nc\n\nint main(){\n    using namespace audio_nc;\n    int s[5]={12,18,22,30,25};\n    process(s,5);\n    std::cout << \"peak=\" << peak(s,5) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_061_c.cpp)\n#include <iostream>\n#include \"seedC.h\"\n\nint seed061_audio_buffer = 2048;\nint seed061_half_buffer(){ return seed061_audio_buffer/2; }\n\nnamespace audio_c {\n\ntemplate <std::size_t N>\nvoid process(const int (&samples)[N]) {\n    for (auto s : samples) {\n        int half = seed061_half_buffer();\n        std::cout << \"sample=\" << s\n                  << \" buf=\" << seed061_audio_buffer\n                  << \" half=\" << half << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint peak(const int (&arr)[N]){\n    int m = arr[0];\n    for(auto v:arr) if(v>m) m=v;\n    return m;\n}\n\n} // namespace audio_c\n\nint main(){\n    using namespace audio_c;\n    const int s[5]={12,18,22,30,25};\n    process(s);\n    std::cout << \"peak=\" << peak(s) << \"\\n\";\n    return 0;\n}\n"}
{"program":62,"context":"Smart thermostat humidity correction parameters","noncompliant":"// -------- Non-Compliant Program (prog_062_nc.cpp)\n#include <iostream>\n#include \"seedNC.h\"\n\nnamespace hum_nc {\n\nvoid adjust(float h[], std::size_t n) { // NC\n    for (size_t i=0;i<n;++i) {\n        float out = seed062_correct(h[i]);\n        std::cout << \"h=\" << h[i]\n                  << \" off=\" << seed062_humidity_offset\n                  << \" out=\" << out << \"\\n\";\n    }\n}\n\nfloat maxv(float arr[], std::size_t n){ float m=arr[0]; for(size_t i=1;i<n;++i) if(arr[i]>m)m=arr[i]; return m; }\n\n} // namespace hum_nc\n\nint main(){\n    using namespace hum_nc;\n    float h[5]={40.0F,42.5F,43.0F,39.0F,41.0F};\n    adjust(h,5);\n    std::cout << \"max=\" << maxv(h,5)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_062_c.cpp)\n#include <iostream>\n#include \"seedC.h\"\n\nfloat seed062_humidity_offset = 3.5F;\nfloat seed062_correct(float h){ return h + seed062_humidity_offset; }\n\nnamespace hum_c {\n\ntemplate <std::size_t N>\nvoid adjust(const float (&h)[N]){\n    for(auto v:h){\n        float out = seed062_correct(v);\n        std::cout << \"h=\" << v\n                  << \" off=\" << seed062_humidity_offset\n                  << \" out=\" << out << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nfloat maxv(const float (&arr)[N]){ float m=arr[0]; for(auto v:arr) if(v>m)m=v; return m; }\n\n} // namespace hum_c\n\nint main(){\n    using namespace hum_c;\n    const float h[5]={40.0F,42.5F,43.0F,39.0F,41.0F};\n    adjust(h);\n    std::cout << \"max=\" << maxv(h)<<\"\\n\";\n    return 0;\n}\n"}
{"program":63,"context":"Weather station rainfall calibration header","noncompliant":"// -------- Non-Compliant Program (prog_063_nc.cpp)\n#include <iostream>\n#include \"seedNC.h\"\n\nnamespace rain_nc {\n\nvoid read(double mm[], std::size_t n){ // NC\n    for(size_t i=0;i<n;++i){\n        double out = seed063_calibrate(mm[i]);\n        std::cout << \"raw=\" << mm[i]\n                  << \" fac=\" << seed063_rain_factor\n                  << \" cal=\" << out << \"\\n\";\n    }\n}\n\ndouble maxd(double arr[], std::size_t n){ double m=arr[0]; for(size_t i=1;i<n;++i) if(arr[i]>m) m=arr[i]; return m; }\n\n} // namespace rain_nc\n\nint main(){\n    using namespace rain_nc;\n    double d[5]={1.2,2.0,5.5,3.3,4.1};\n    read(d,5);\n    std::cout << \"max=\" << maxd(d,5)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_063_c.cpp)\n#include <iostream>\n#include \"seedC.h\"\n\ndouble seed063_rain_factor = 0.87;\ndouble seed063_calibrate(double mm){ return mm * seed063_rain_factor; }\n\nnamespace rain_c {\n\ntemplate <std::size_t N>\nvoid read(const double (&mm)[N]){\n    for(auto v:mm){\n        double out = seed063_calibrate(v);\n        std::cout << \"raw=\" << v\n                  << \" fac=\" << seed063_rain_factor\n                  << \" cal=\" << out << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\ndouble maxd(const double (&arr)[N]){ double m=arr[0]; for(auto v:arr) if(v>m)m=v; return m; }\n\n} // namespace rain_c\n\nint main(){\n    using namespace rain_c;\n    const double d[5]={1.2,2.0,5.5,3.3,4.1};\n    read(d);\n    std::cout << \"max=\" << maxd(d)<<\"\\n\";\n    return 0;\n}\n"}
{"program":64,"context":"Automated gate motor torque settings","noncompliant":"// -------- Non-Compliant Program (prog_064_nc.cpp)\n#include <iostream>\n#include \"seedNC.h\"\n\nnamespace gate_nc {\n\nvoid check(int vals[], std::size_t n){ // NC\n    for(size_t i=0;i<n;++i){\n        bool o = seed064_overloaded(vals[i]);\n        std::cout << \"t=\" << vals[i]\n                  << \" lim=\" << seed064_torque_limit\n                  << \" ov=\" << (o?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\nint maxv(int a[], std::size_t n){ int m=a[0]; for(size_t i=1;i<n;++i) if(a[i]>m)m=a[i]; return m; }\n\n} // namespace gate_nc\n\nint main(){\n    using namespace gate_nc;\n    int t[5]={80,100,120,130,90};\n    check(t,5);\n    std::cout << \"max=\" << maxv(t,5)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_064_c.cpp)\n#include <iostream>\n#include \"seedC.h\"\n\nint seed064_torque_limit = 120;\nbool seed064_overloaded(int t){ return t > seed064_torque_limit; }\n\nnamespace gate_c {\n\ntemplate <std::size_t N>\nvoid check(const int (&vals)[N]){\n    for(auto v:vals){\n        bool o = seed064_overloaded(v);\n        std::cout << \"t=\" << v\n                  << \" lim=\" << seed064_torque_limit\n                  << \" ov=\" << (o?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint maxv(const int (&arr)[N]){ int m=arr[0]; for(auto v:arr) if(v>m)m=v; return m; }\n\n} // namespace gate_c\n\nint main(){\n    using namespace gate_c;\n    const int t[5]={80,100,120,130,90};\n    check(t);\n    std::cout << \"max=\" << maxv(t)<<\"\\n\";\n    return 0;\n}\n"}
{"program":65,"context":"Warehouse robot battery reserve threshold","noncompliant":"// -------- Non-Compliant Program (prog_065_nc.cpp)\n#include <iostream>\n#include \"seedNC.h\"\n\nnamespace batt_nc {\n\nvoid scan(int pct[], std::size_t n){ // NC\n    for(size_t i=0;i<n;++i){\n        bool low = seed065_low_battery(pct[i]);\n        std::cout << \"pct=\" << pct[i]\n                  << \" lim=\" << seed065_reserve_pct\n                  << \" low=\" << (low?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\nint minv(int a[], std::size_t n){ int m=a[0]; for(size_t i=1;i<n;++i) if(a[i]<m)m=a[i]; return m; }\n\n} // namespace batt_nc\n\nint main(){\n    using namespace batt_nc;\n    int b[5]={10,20,15,30,5};\n    scan(b,5);\n    std::cout << \"min=\" << minv(b,5)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_065_c.cpp)\n#include <iostream>\n#include \"seedC.h\"\n\nint seed065_reserve_pct = 15;\nbool seed065_low_battery(int pct){ return pct < seed065_reserve_pct; }\n\nnamespace batt_c {\n\ntemplate <std::size_t N>\nvoid scan(const int (&pct)[N]){\n    for(auto v:pct){\n        bool low = seed065_low_battery(v);\n        std::cout << \"pct=\" << v\n                  << \" lim=\" << seed065_reserve_pct\n                  << \" low=\" << (low?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint minv(const int (&arr)[N]){ int m=arr[0]; for(auto v:arr) if(v<m)m=v; return m; }\n\n} // namespace batt_c\n\nint main(){\n    using namespace batt_c;\n    const int b[5]={10,20,15,30,5};\n    scan(b);\n    std::cout << \"min=\" << minv(b)<<\"\\n\";\n    return 0;\n}\n"}
{"program":66,"context":"Display refresh compensation factor","noncompliant":"// -------- Non-Compliant Program (prog_066_nc.cpp)\n#include <iostream>\n#include \"seedNC.h\"\n\nnamespace disp_nc {\n\nvoid calc(float hz[], std::size_t n){ // NC\n    for(size_t i=0;i<n;++i){\n        float out = seed066_apply(hz[i]);\n        std::cout << \"hz=\" << hz[i]\n                  << \" fac=\" << seed066_refresh_factor\n                  << \" out=\" << out << \"\\n\";\n    }\n}\n\nfloat maxf(float a[], std::size_t n){ float m=a[0]; for(size_t i=1;i<n;++i) if(a[i]>m)m=a[i]; return m; }\n\n} // namespace disp_nc\n\nint main(){\n    using namespace disp_nc;\n    float f[5]={60,75,90,120,144};\n    calc(f,5);\n    std::cout << \"max=\" << maxf(f,5)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_066_c.cpp)\n#include <iostream>\n#include \"seedC.h\"\n\nfloat seed066_refresh_factor = 1.05F;\nfloat seed066_apply(float hz){ return hz * seed066_refresh_factor; }\n\nnamespace disp_c {\n\ntemplate <std::size_t N>\nvoid calc(const float (&hz)[N]){\n    for(auto v:hz){\n        float out = seed066_apply(v);\n        std::cout << \"hz=\" << v\n                  << \" fac=\" << seed066_refresh_factor\n                  << \" out=\" << out << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nfloat maxf(const float (&arr)[N]){ float m=arr[0]; for(auto v:arr) if(v>m)m=v; return m; }\n\n} // namespace disp_c\n\nint main(){\n    using namespace disp_c;\n    const float f[5]={60,75,90,120,144};\n    calc(f);\n    std::cout << \"max=\" << maxf(f)<<\"\\n\";\n    return 0;\n}\n"}
{"program":67,"context":"Drone propeller pitch lookup header","noncompliant":"// -------- Non-Compliant Program (prog_067_nc.cpp)\n#include <iostream>\n#include \"seedNC.h\"\n\nnamespace pitch_nc {\n\nvoid test(int idx[], std::size_t n){ // NC decay\n    for(size_t i=0;i<n;++i){\n        float p = seed067_get_pitch(idx[i]);\n        std::cout << \"idx=\" << idx[i]\n                  << \" p=\" << p\n                  << \" v0=\" << seed067_pitch_values[0]\n                  << \"\\n\";\n    }\n}\n\nint maxidx(int arr[], std::size_t n){ int m=arr[0]; for(size_t i=1;i<n;++i) if(arr[i]>m)m=arr[i]; return m; }\n\n} // namespace pitch_nc\n\nint main(){\n    using namespace pitch_nc;\n    int i[5]={0,1,2,1,0};\n    test(i,5);\n    std::cout << \"maxidx=\" << maxidx(i,5)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_067_c.cpp)\n#include <iostream>\n#include \"seedC.h\"\n\nfloat seed067_pitch_values[3]={3.1F,4.5F,5.2F};\nfloat seed067_get_pitch(int i){ return seed067_pitch_values[i]; }\n\nnamespace pitch_c {\n\ntemplate <std::size_t N>\nvoid test(const int (&idx)[N]){\n    for(auto v:idx){\n        float p = seed067_get_pitch(v);\n        std::cout << \"idx=\" << v\n                  << \" p=\" << p\n                  << \" v0=\" << seed067_pitch_values[0]\n                  << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint maxidx(const int (&arr)[N]){ int m=arr[0]; for(auto v:arr) if(v>m)m=v; return m; }\n\n} // namespace pitch_c\n\nint main(){\n    using namespace pitch_c;\n    const int i[5]={0,1,2,1,0};\n    test(i);\n    std::cout << \"maxidx=\" << maxidx(i)<<\"\\n\";\n    return 0;\n}\n"}
{"program":68,"context":"Smart irrigation moisture threshold header","noncompliant":"// -------- Non-Compliant Program (prog_068_nc.cpp)\n#include <iostream>\n#include \"seedNC.h\"\n\nnamespace irr_nc {\n\nvoid eval(int v[], std::size_t n){ // NC\n    for(size_t i=0;i<n;++i){\n        bool w = seed068_needs_water(v[i]);\n        std::cout << \"m=\" << v[i]\n                  << \" thr=\" << seed068_moisture_threshold\n                  << \" need=\" << (w?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\nint minv(int a[], std::size_t n){ int m=a[0]; for(size_t i=1;i<n;++i) if(a[i]<m)m=a[i]; return m; }\n\n} // namespace irr_nc\n\nint main(){\n    using namespace irr_nc;\n    int m[5]={30,40,55,60,20};\n    eval(m,5);\n    std::cout << \"min=\" << minv(m,5)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_068_c.cpp)\n#include <iostream>\n#include \"seedC.h\"\n\nint seed068_moisture_threshold = 45;\nbool seed068_needs_water(int v){ return v < seed068_moisture_threshold; }\n\nnamespace irr_c {\n\ntemplate <std::size_t N>\nvoid eval(const int (&v)[N]){\n    for(auto x:v){\n        bool w = seed068_needs_water(x);\n        std::cout << \"m=\" << x\n                  << \" thr=\" << seed068_moisture_threshold\n                  << \" need=\" << (w?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint minv(const int (&arr)[N]){ int m=arr[0]; for(auto v:arr) if(v<m)m=v; return m; }\n\n} // namespace irr_c\n\nint main(){\n    using namespace irr_c;\n    const int m[5]={30,40,55,60,20};\n    eval(m);\n    std::cout << \"min=\" << minv(m)<<\"\\n\";\n    return 0;\n}\n"}
{"program":69,"context":"HVAC fan speed parameters header","noncompliant":"// -------- Non-Compliant Program (prog_069_nc.cpp)\n#include <iostream>\n#include \"seedNC.h\"\n\nnamespace fan_nc {\n\nvoid check(int s[], std::size_t n){ // NC\n    for(size_t i=0;i<n;++i){\n        int out = seed069_limit(s[i]);\n        std::cout << \"in=\" << s[i]\n                  << \" max=\" << seed069_max_fan_speed\n                  << \" out=\" << out << \"\\n\";\n    }\n}\n\nint maxv(int a[], std::size_t n){ int m=a[0]; for(size_t i=1;i<n;++i) if(a[i]>m)m=a[i]; return m; }\n\n} // namespace fan_nc\n\nint main(){\n    using namespace fan_nc;\n    int s[5]={1,4,5,7,2};\n    check(s,5);\n    std::cout << \"maxv=\" << maxv(s,5)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_069_c.cpp)\n#include <iostream>\n#include \"seedC.h\"\n\nint seed069_max_fan_speed = 5;\nint seed069_limit(int s){ return (s > seed069_max_fan_speed) ? seed069_max_fan_speed : s; }\n\nnamespace fan_c {\n\ntemplate <std::size_t N>\nvoid check(const int (&s)[N]){\n    for(auto v:s){\n        int out = seed069_limit(v);\n        std::cout << \"in=\" << v\n                  << \" max=\" << seed069_max_fan_speed\n                  << \" out=\" << out << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint maxv(const int (&arr)[N]){ int m=arr[0]; for(auto v:arr) if(v>m)m=v; return m; }\n\n} // namespace fan_c\n\nint main(){\n    using namespace fan_c;\n    const int s[5]={1,4,5,7,2};\n    check(s);\n    std::cout << \"maxv=\" << maxv(s)<<\"\\n\";\n    return 0;\n}\n"}
{"program":70,"context":"Factory conveyor belt speed configuration","noncompliant":"// -------- Non-Compliant Program (prog_070_nc.cpp)\n#include <iostream>\n#include \"seedNC.h\"\n\nnamespace belt_nc {\n\nvoid check(float s[], std::size_t n){ // NC decay\n    for(size_t i=0;i<n;++i){\n        bool ok = seed070_within_limit(s[i]);\n        std::cout << \"spd=\" << s[i]\n                  << \" lim=\" << seed070_belt_speed\n                  << \" ok=\" << (ok?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\nfloat maxf(float a[], std::size_t n){ float m=a[0]; for(size_t i=1;i<n;++i) if(a[i]>m)m=a[i]; return m; }\n\n} // namespace belt_nc\n\nint main(){\n    using namespace belt_nc;\n    float s[5]={1.0F,1.2F,1.3F,1.0F,0.9F};\n    check(s,5);\n    std::cout << \"maxf=\" << maxf(s,5)<<\"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_070_c.cpp)\n#include <iostream>\n#include \"seedC.h\"\n\nfloat seed070_belt_speed = 1.25F;\nbool seed070_within_limit(float s){ return s <= seed070_belt_speed; }\n\nnamespace belt_c {\n\ntemplate <std::size_t N>\nvoid check(const float (&s)[N]){\n    for(auto v:s){\n        bool ok = seed070_within_limit(v);\n        std::cout << \"spd=\" << v\n                  << \" lim=\" << seed070_belt_speed\n                  << \" ok=\" << (ok?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nfloat maxf(const float (&arr)[N]){ float m=arr[0]; for(auto v:arr) if(v>m)m=v; return m; }\n\n} // namespace belt_c\n\nint main(){\n    using namespace belt_c;\n    const float s[5]={1.0F,1.2F,1.3F,1.0F,0.9F};\n    check(s);\n    std::cout << \"maxf=\" << maxf(s)<<\"\\n\";\n    return 0;\n}\n"}
{"program":71,"context":"Greenhouse CO₂ regulation threshold header","noncompliant":"// -------- Non-Compliant Program (prog_071_nc.cpp)\n#include <iostream>\n#include \"seedNC.h\" // seed071_co2_limit, seed071_exceeds()\n\nnamespace gh_nc {\n\nvoid monitor(int readings[], std::size_t n) { // NC decay\n    for (std::size_t i = 0; i < n; ++i) {\n        bool ex = seed071_exceeds(readings[i]);\n        std::cout << \"ppm=\" << readings[i]\n                  << \" limit=\" << seed071_co2_limit\n                  << \" exceed=\" << (ex ? \"YES\" : \"NO\") << \"\\n\";\n    }\n}\n\nint maxv(int arr[], std::size_t n) { int m = arr[0]; for (std::size_t i=1;i<n;++i) if (arr[i]>m) m=arr[i]; return m; }\n\n} // namespace gh_nc\n\nint main() {\n    using namespace gh_nc;\n    int readings[6] = {420, 780, 910, 880, 950, 870};\n    monitor(readings, 6);\n    std::cout << \"peak=\" << maxv(readings,6) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_071_c.cpp)\n#include <iostream>\n#include \"seedC.h\"\n\nint seed071_co2_limit = 900;\nbool seed071_exceeds(int ppm) { return ppm > seed071_co2_limit; }\n\nnamespace gh_c {\n\ntemplate <std::size_t N>\nvoid monitor(const int (&readings)[N]) {\n    for (auto r : readings) {\n        bool ex = seed071_exceeds(r);\n        std::cout << \"ppm=\" << r\n                  << \" limit=\" << seed071_co2_limit\n                  << \" exceed=\" << (ex ? \"YES\" : \"NO\") << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint maxv(const int (&arr)[N]) { int m = arr[0]; for (auto v:arr) if (v>m) m=v; return m; }\n\n} // namespace gh_c\n\nint main() {\n    using namespace gh_c;\n    const int readings[6] = {420, 780, 910, 880, 950, 870};\n    monitor(readings);\n    std::cout << \"peak=\" << maxv(readings) << \"\\n\";\n    return 0;\n}\n"}
{"program":72,"context":"Marine sonar gain compensation header","noncompliant":"// -------- Non-Compliant Program (prog_072_nc.cpp)\n#include <iostream>\n#include \"seedNC.h\" // seed072_gain, seed072_apply_gain()\n\nnamespace sonar_nc {\n\nvoid process(float samples[], std::size_t n) { // NC decay\n    for (std::size_t i=0;i<n;++i) {\n        float out = seed072_apply_gain(samples[i]);\n        std::cout << \"in=\" << samples[i]\n                  << \" gain=\" << seed072_gain\n                  << \" out=\" << out << \"\\n\";\n    }\n}\n\nfloat maxv(float a[], std::size_t n){ float m=a[0]; for(size_t i=1;i<n;++i) if(a[i]>m) m=a[i]; return m; }\n\n} // namespace sonar_nc\n\nint main(){\n    using namespace sonar_nc;\n    float s[5] = {0.5F, 1.2F, 0.8F, 1.5F, 0.9F};\n    process(s,5);\n    std::cout << \"peak=\" << maxv(s,5) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_072_c.cpp)\n#include <iostream>\n#include \"seedC.h\"\n\nfloat seed072_gain = 2.4F;\nfloat seed072_apply_gain(float v) { return v * seed072_gain; }\n\nnamespace sonar_c {\n\ntemplate <std::size_t N>\nvoid process(const float (&samples)[N]) {\n    for (auto v : samples) {\n        float out = seed072_apply_gain(v);\n        std::cout << \"in=\" << v\n                  << \" gain=\" << seed072_gain\n                  << \" out=\" << out << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nfloat maxv(const float (&a)[N]){ float m=a[0]; for(auto v:a) if(v>m) m=v; return m; }\n\n} // namespace sonar_c\n\nint main(){\n    using namespace sonar_c;\n    const float s[5] = {0.5F, 1.2F, 0.8F, 1.5F, 0.9F};\n    process(s);\n    std::cout << \"peak=\" << maxv(s) << \"\\n\";\n    return 0;\n}\n"}
{"program":73,"context":"Wind turbine blade pitch calibration","noncompliant":"// -------- Non-Compliant Program (prog_073_nc.cpp)\n#include <iostream>\n#include \"seedNC.h\" // seed073_pitch_offset, seed073_calibrate()\n\nnamespace wind_nc {\n\nvoid calibrate(double angles[], std::size_t n) { // NC\n    for (std::size_t i=0;i<n;++i) {\n        double out = seed073_calibrate(angles[i]);\n        std::cout << \"angle=\" << angles[i]\n                  << \" off=\" << seed073_pitch_offset\n                  << \" cal=\" << out << \"\\n\";\n    }\n}\n\ndouble maxd(double a[], std::size_t n){ double m=a[0]; for(size_t i=1;i<n;++i) if(a[i]>m) m=a[i]; return m; }\n\n} // namespace wind_nc\n\nint main(){\n    using namespace wind_nc;\n    double ang[4] = {2.0, 3.5, 1.2, 4.0};\n    calibrate(ang,4);\n    std::cout << \"peak=\" << maxd(ang,4) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_073_c.cpp)\n#include <iostream>\n#include \"seedC.h\"\n\ndouble seed073_pitch_offset = 1.8;\ndouble seed073_calibrate(double p) { return p + seed073_pitch_offset; }\n\nnamespace wind_c {\n\ntemplate <std::size_t N>\nvoid calibrate(const double (&angles)[N]){\n    for(auto v:angles){\n        double out = seed073_calibrate(v);\n        std::cout << \"angle=\" << v\n                  << \" off=\" << seed073_pitch_offset\n                  << \" cal=\" << out << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\ndouble maxd(const double (&a)[N]){ double m=a[0]; for(auto v:a) if(v>m)m=v; return m; }\n\n} // namespace wind_c\n\nint main(){\n    using namespace wind_c;\n    const double ang[4] = {2.0, 3.5, 1.2, 4.0};\n    calibrate(ang);\n    std::cout << \"peak=\" << maxd(ang) << \"\\n\";\n    return 0;\n}\n"}
{"program":74,"context":"Electric scooter speed governor","noncompliant":"// -------- Non-Compliant Program (prog_074_nc.cpp)\n#include <iostream>\n#include \"seedNC.h\" // seed074_speed_cap, seed074_limit_speed()\n\nnamespace scooter_nc {\n\nvoid apply(int speeds[], std::size_t n) { // NC\n    for (std::size_t i=0;i<n;++i) {\n        int out = seed074_limit_speed(speeds[i]);\n        std::cout << \"req=\" << speeds[i]\n                  << \" cap=\" << seed074_speed_cap\n                  << \" out=\" << out << \"\\n\";\n    }\n}\n\nint maxv(int a[], std::size_t n){ int m=a[0]; for(size_t i=1;i<n;++i) if(a[i]>m)m=a[i]; return m; }\n\n} // namespace scooter_nc\n\nint main(){\n    using namespace scooter_nc;\n    int requested[5] = {10, 24, 30, 18, 27};\n    apply(requested,5);\n    std::cout << \"peak=\" << maxv(requested,5) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_074_c.cpp)\n#include <iostream>\n#include \"seedC.h\"\n\nint seed074_speed_cap = 25;\nint seed074_limit_speed(int s) { return (s > seed074_speed_cap) ? seed074_speed_cap : s; }\n\nnamespace scooter_c {\n\ntemplate <std::size_t N>\nvoid apply(const int (&speeds)[N]){\n    for(auto v:speeds){\n        int out = seed074_limit_speed(v);\n        std::cout << \"req=\" << v\n                  << \" cap=\" << seed074_speed_cap\n                  << \" out=\" << out << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint maxv(const int (&a)[N]){ int m=a[0]; for(auto v:a) if(v>m)m=v; return m; }\n\n} // namespace scooter_c\n\nint main(){\n    using namespace scooter_c;\n    const int requested[5] = {10, 24, 30, 18, 27};\n    apply(requested);\n    std::cout << \"peak=\" << maxv(requested) << \"\\n\";\n    return 0;\n}\n"}
{"program":75,"context":"Agricultural soil nutrient factor header","noncompliant":"// -------- Non-Compliant Program (prog_075_nc.cpp)\n#include <iostream>\n#include \"seedNC.h\" // seed075_nutrient_factor, seed075_adjust()\n\nnamespace ag_nc {\n\nvoid adjust(float bases[], std::size_t n) { // NC\n    for (std::size_t i=0;i<n;++i) {\n        float out = seed075_adjust(bases[i]);\n        std::cout << \"base=\" << bases[i]\n                  << \" factor=\" << seed075_nutrient_factor\n                  << \" out=\" << out << \"\\n\";\n    }\n}\n\nfloat avg(float a[], std::size_t n){ float s=0; for(size_t i=0;i<n;++i) s+=a[i]; return n? s/n : 0; }\n\n} // namespace ag_nc\n\nint main(){\n    using namespace ag_nc;\n    float base[4] = {10.0F, 12.5F, 8.0F, 15.0F};\n    adjust(base,4);\n    std::cout << \"avg=\" << avg(base,4) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_075_c.cpp)\n#include <iostream>\n#include \"seedC.h\"\n\nfloat seed075_nutrient_factor = 0.72F;\nfloat seed075_adjust(float base) { return base * seed075_nutrient_factor; }\n\nnamespace ag_c {\n\ntemplate <std::size_t N>\nvoid adjust(const float (&bases)[N]){\n    for(auto v:bases){\n        float out = seed075_adjust(v);\n        std::cout << \"base=\" << v\n                  << \" factor=\" << seed075_nutrient_factor\n                  << \" out=\" << out << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nfloat avg(const float (&a)[N]){ float s=0; for(auto v:a) s+=v; return N? s/N : 0; }\n\n} // namespace ag_c\n\nint main(){\n    using namespace ag_c;\n    const float base[4] = {10.0F, 12.5F, 8.0F, 15.0F};\n    adjust(base);\n    std::cout << \"avg=\" << avg(base) << \"\\n\";\n    return 0;\n}\n"}
{"program":76,"context":"Smart grid load balancing threshold header","noncompliant":"// -------- Non-Compliant Program (prog_076_nc.cpp)\n#include <iostream>\n#include \"seedNC.h\" // seed076_load_limit, seed076_overload()\n\nnamespace grid_nc {\n\nvoid inspect(int loads[], std::size_t n) { // NC\n    for (std::size_t i=0;i<n;++i) {\n        bool o = seed076_overload(loads[i]);\n        std::cout << \"load=\" << loads[i]\n                  << \" limit=\" << seed076_load_limit\n                  << \" overload=\" << (o?\"YES\":\"NO\") << \"\\n\";\n    }\n}\n\nint sum(int a[], std::size_t n){ int s=0; for(size_t i=0;i<n;++i) s+=a[i]; return s; }\n\n} // namespace grid_nc\n\nint main(){\n    using namespace grid_nc;\n    int loads[5] = {1200, 3400, 3600, 3000, 2000};\n    inspect(loads,5);\n    std::cout << \"total=\" << sum(loads,5) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_076_c.cpp)\n#include <iostream>\n#include \"seedC.h\"\n\nint seed076_load_limit = 3500;\nbool seed076_overload(int watts) { return watts > seed076_load_limit; }\n\nnamespace grid_c {\n\ntemplate <std::size_t N>\nvoid inspect(const int (&loads)[N]){\n    for(auto v:loads){\n        bool o = seed076_overload(v);\n        std::cout << \"load=\" << v\n                  << \" limit=\" << seed076_load_limit\n                  << \" overload=\" << (o?\"YES\":\"NO\") << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint sum(const int (&a)[N]){ int s=0; for(auto v:a) s+=v; return s; }\n\n} // namespace grid_c\n\nint main(){\n    using namespace grid_c;\n    const int loads[5] = {1200, 3400, 3600, 3000, 2000};\n    inspect(loads);\n    std::cout << \"total=\" << sum(loads) << \"\\n\";\n    return 0;\n}\n"}
{"program":77,"context":"Medical heart rate smoothing factor","noncompliant":"// -------- Non-Compliant Program (prog_077_nc.cpp)\n#include <iostream>\n#include \"seedNC.h\" // seed077_smooth_factor, seed077_smooth()\n\nnamespace hr_nc {\n\nvoid smooth(float data[], std::size_t n) { // NC\n    for (std::size_t i=0;i<n;++i) {\n        float out = seed077_smooth(data[i]);\n        std::cout << \"raw=\" << data[i]\n                  << \" fact=\" << seed077_smooth_factor\n                  << \" sm=\" << out << \"\\n\";\n    }\n}\n\nfloat avg(float a[], std::size_t n){ float s=0; for(size_t i=0;i<n;++i) s+=a[i]; return n? s/n : 0; }\n\n} // namespace hr_nc\n\nint main(){\n    using namespace hr_nc;\n    float beats[6] = {72.0F, 75.0F, 80.0F, 77.0F, 74.0F, 76.0F};\n    smooth(beats,6);\n    std::cout << \"avg=\" << avg(beats,6) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_077_c.cpp)\n#include <iostream>\n#include \"seedC.h\"\n\nfloat seed077_smooth_factor = 0.85F;\nfloat seed077_smooth(float hr) { return hr * seed077_smooth_factor; }\n\nnamespace hr_c {\n\ntemplate <std::size_t N>\nvoid smooth(const float (&data)[N]){\n    for(auto v:data){\n        float out = seed077_smooth(v);\n        std::cout << \"raw=\" << v\n                  << \" fact=\" << seed077_smooth_factor\n                  << \" sm=\" << out << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nfloat avg(const float (&a)[N]){ float s=0; for(auto v:a) s+=v; return N? s/N : 0; }\n\n} // namespace hr_c\n\nint main(){\n    using namespace hr_c;\n    const float beats[6] = {72.0F, 75.0F, 80.0F, 77.0F, 74.0F, 76.0F};\n    smooth(beats);\n    std::cout << \"avg=\" << avg(beats) << \"\\n\";\n    return 0;\n}\n"}
{"program":78,"context":"Warehouse barcode scanner retry config","noncompliant":"// -------- Non-Compliant Program (prog_078_nc.cpp)\n#include <iostream>\n#include \"seedNC.h\" // seed078_max_retries, seed078_allow_retry()\n\nnamespace scan_nc {\n\nvoid attempt(int counts[], std::size_t n) { // NC\n    for (std::size_t i=0;i<n;++i) {\n        bool ok = seed078_allow_retry(counts[i]);\n        std::cout << \"attempts=\" << counts[i]\n                  << \" max=\" << seed078_max_retries\n                  << \" allow_retry=\" << (ok?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\nint maxv(int a[], std::size_t n){ int m=a[0]; for(size_t i=1;i<n;++i) if(a[i]>m)m=a[i]; return m; }\n\n} // namespace scan_nc\n\nint main(){\n    using namespace scan_nc;\n    int tries[5] = {0,1,2,3,4};\n    attempt(tries,5);\n    std::cout << \"max=\" << maxv(tries,5) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_078_c.cpp)\n#include <iostream>\n#include \"seedC.h\"\n\nint seed078_max_retries = 4;\nbool seed078_allow_retry(int c) { return c < seed078_max_retries; }\n\nnamespace scan_c {\n\ntemplate <std::size_t N>\nvoid attempt(const int (&counts)[N]){\n    for(auto v:counts){\n        bool ok = seed078_allow_retry(v);\n        std::cout << \"attempts=\" << v\n                  << \" max=\" << seed078_max_retries\n                  << \" allow_retry=\" << (ok?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint maxv(const int (&a)[N]){ int m=a[0]; for(auto v:a) if(v>m)m=v; return m; }\n\n} // namespace scan_c\n\nint main(){\n    using namespace scan_c;\n    const int tries[5] = {0,1,2,3,4};\n    attempt(tries);\n    std::cout << \"max=\" << maxv(tries) << \"\\n\";\n    return 0;\n}\n"}
{"program":79,"context":"Satellite power budget margin header","noncompliant":"// -------- Non-Compliant Program (prog_079_nc.cpp)\n#include <iostream>\n#include \"seedNC.h\" // seed079_margin, seed079_within_margin()\n\nnamespace sat_nc {\n\nvoid check(double usages[], std::size_t n) { // NC\n    for (std::size_t i=0;i<n;++i) {\n        bool ok = seed079_within_margin(usages[i]);\n        std::cout << \"usage=\" << usages[i]\n                  << \" margin=\" << seed079_margin\n                  << \" within=\" << (ok?\"YES\":\"NO\") << \"\\n\";\n    }\n}\n\ndouble sumd(double a[], std::size_t n){ double s=0; for(size_t i=0;i<n;++i) s+=a[i]; return s; }\n\n} // namespace sat_nc\n\nint main(){\n    using namespace sat_nc;\n    double u[4] = {10.0, 11.5, 12.0, 13.0};\n    check(u,4);\n    std::cout << \"total=\" << sumd(u,4) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_079_c.cpp)\n#include <iostream>\n#include \"seedC.h\"\n\ndouble seed079_margin = 12.5;\nbool seed079_within_margin(double usage) { return usage < seed079_margin; }\n\nnamespace sat_c {\n\ntemplate <std::size_t N>\nvoid check(const double (&usages)[N]){\n    for(auto v:usages){\n        bool ok = seed079_within_margin(v);\n        std::cout << \"usage=\" << v\n                  << \" margin=\" << seed079_margin\n                  << \" within=\" << (ok?\"YES\":\"NO\") << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\ndouble sumd(const double (&a)[N]){ double s=0; for(auto v:a) s+=v; return s; }\n\n} // namespace sat_c\n\nint main(){\n    using namespace sat_c;\n    const double u[4] = {10.0, 11.5, 12.0, 13.0};\n    check(u);\n    std::cout << \"total=\" << sumd(u) << \"\\n\";\n    return 0;\n}\n"}
{"program":80,"context":"Biometric fingerprint scanner quality threshold","noncompliant":"// -------- Non-Compliant Program (prog_080_nc.cpp)\n#include <iostream>\n#include \"seedNC.h\" // seed080_quality_threshold, seed080_is_valid()\n\nnamespace fp_nc {\n\nvoid evaluate(int quals[], std::size_t n) { // NC\n    for (std::size_t i=0;i<n;++i) {\n        bool v = seed080_is_valid(quals[i]);\n        std::cout << \"q=\" << quals[i]\n                  << \" thresh=\" << seed080_quality_threshold\n                  << \" valid=\" << (v?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\nint maxv(int a[], std::size_t n){ int m=a[0]; for(size_t i=1;i<n;++i) if(a[i]>m)m=a[i]; return m; }\n\n} // namespace fp_nc\n\nint main(){\n    using namespace fp_nc;\n    int q[6] = {60, 70, 72, 85, 71, 73};\n    evaluate(q,6);\n    std::cout << \"best=\" << maxv(q,6) << \"\\n\";\n    return 0;\n}\n","compliant":"// -------- Compliant Program (prog_080_c.cpp)\n#include <iostream>\n#include \"seedC.h\"\n\nint seed080_quality_threshold = 72;\nbool seed080_is_valid(int q) { return q >= seed080_quality_threshold; }\n\nnamespace fp_c {\n\ntemplate <std::size_t N>\nvoid evaluate(const int (&quals)[N]){\n    for(auto v:quals){\n        bool ok = seed080_is_valid(v);\n        std::cout << \"q=\" << v\n                  << \" thresh=\" << seed080_quality_threshold\n                  << \" valid=\" << (ok?\"Y\":\"N\") << \"\\n\";\n    }\n}\n\ntemplate <std::size_t N>\nint maxv(const int (&a)[N]){ int m=a[0]; for(auto v:a) if(v>m)m=v; return m; }\n\n} // namespace fp_c\n\nint main(){\n    using namespace fp_c;\n    const int q[6] = {60, 70, 72, 85, 71, 73};\n    evaluate(q);\n    std::cout << \"best=\" << maxv(q) << \"\\n\";\n    return 0;\n}\n"}
{"program":81,"context":"Drone altitude safety floor header","noncompliant":"#include <iostream>\\n#include \"seedNC.h\"\\nnamespace drone_nc {\\nvoid check_altitudes(float alts[], std::size_t n) { for (std::size_t i=0;i<n;++i){ bool low = seed081_is_too_low(alts[i]); std::cout<<\"alt=\"<<alts[i]<<\" min=\"<<seed081_min_altitude<<\" too_low=\"<<(low?\"YES\":\"NO\")<<\"\\n\";} }\\nfloat average(float a[], std::size_t n){ float s=0.0F; for(size_t i=0;i<n;++i) s+=a[i]; return n? s/static_cast<float>(n) : 0.0F; }\\n} // ns\\nint main(){ float alt[6]={20.0F,18.5F,14.0F,16.5F,22.0F,13.0F}; drone_nc::check_altitudes(alt,6); std::cout<<\"avg=\"<<drone_nc::average(alt,6)<<\"\\n\"; return 0; }","compliant":"#include <iostream>\\n#include \"seedC.h\"\\nfloat seed081_min_altitude = 15.0F;\\nbool seed081_is_too_low(float alt){ return alt < seed081_min_altitude; }\\nnamespace drone_c{\\ntemplate<std::size_t N> void check_altitudes(const float(&alts)[N]){ for(auto a:alts){ bool low = seed081_is_too_low(a); std::cout<<\"alt=\"<<a<<\" min=\"<<seed081_min_altitude<<\" too_low=\"<<(low?\"YES\":\"NO\")<<\"\\n\";} }\\ntemplate<std::size_t N> float average(const float(&a)[N]){ float s=0; for(auto v:a) s+=v; return N? s/static_cast<float>(N) : 0.0F; }\\n}\\nint main(){ const float alt[6]={20.0F,18.5F,14.0F,16.5F,22.0F,13.0F}; drone_c::check_altitudes(alt); std::cout<<\"avg=\"<<drone_c::average(alt)<<\"\\n\"; return 0; }"}
{"program":82,"context":"Substation transformer thermal threshold header","noncompliant":"#include <iostream>\\n#include \"seedNC.h\"\\nnamespace xfmr_nc{\\nvoid monitor(double t[], std::size_t n){ for(std::size_t i=0;i<n;++i){ bool ot = seed082_overtemp(t[i]); std::cout<<\"temp=\"<<t[i]<<\" thr=\"<<seed082_temp_threshold<<\" overtemp=\"<<(ot?\"YES\":\"NO\")<<\"\\n\"; } }\\ndouble mean(double a[], std::size_t n){ double s=0; for(size_t i=0;i<n;++i) s+=a[i]; return n? s/n:0; }\\n}\\nint main(){ double t[5]={72.0,80.5,86.2,84.0,79.5}; xfmr_nc::monitor(t,5); std::cout<<\"avg=\"<<xfmr_nc::mean(t,5)<<\"\\n\"; return 0; }","compliant":"#include <iostream>\\n#include \"seedC.h\"\\ndouble seed082_temp_threshold = 85.0;\\nbool seed082_overtemp(double t){ return t > seed082_temp_threshold; }\\nnamespace xfmr_c{\\ntemplate<std::size_t N> void monitor(const double(&t)[N]){ for(auto v:t){ bool ot=seed082_overtemp(v); std::cout<<\"temp=\"<<v<<\" thr=\"<<seed082_temp_threshold<<\" overtemp=\"<<(ot?\"YES\":\"NO\")<<\"\\n\"; } }\\ntemplate<std::size_t N> double mean(const double(&a)[N]){ double s=0; for(auto v:a) s+=v; return N? s/N:0; }\\n}\\nint main(){ const double t[5]={72.0,80.5,86.2,84.0,79.5}; xfmr_c::monitor(t); std::cout<<\"avg=\"<<xfmr_c::mean(t)<<\"\\n\"; return 0; }"}
{"program":83,"context":"Factory conveyor jam detection header","noncompliant":"#include <iostream>\\n#include \"seedNC.h\"\\nnamespace conv_nc{\\nvoid inspect(int s[], std::size_t n){ for(std::size_t i=0;i<n;++i){ bool jam = seed083_is_jammed(s[i]); std::cout<<\"stalls=\"<<s[i]<<\" limit=\"<<seed083_jam_limit<<\" jammed=\"<<(jam?\"YES\":\"NO\")<<\"\\n\"; } }\\nint total(int a[], std::size_t n){ int S=0; for(size_t i=0;i<n;++i) S+=a[i]; return S; }\\n}\\nint main(){ int stalls[4]={0,1,3,2}; conv_nc::inspect(stalls,4); std::cout<<\"total_stalls=\"<<conv_nc::total(stalls,4)<<\"\\n\"; return 0; }","compliant":"#include <iostream>\\n#include \"seedC.h\"\\nint seed083_jam_limit = 3; bool seed083_is_jammed(int s){ return s >= seed083_jam_limit; }\\nnamespace conv_c{\\ntemplate<std::size_t N> void inspect(const int(&s)[N]){ for(auto v:s){ bool jam=seed083_is_jammed(v); std::cout<<\"stalls=\"<<v<<\" limit=\"<<seed083_jam_limit<<\" jammed=\"<<(jam?\"YES\":\"NO\")<<\"\\n\"; } }\\ntemplate<std::size_t N> int total(const int(&a)[N]){ int S=0; for(auto v:a) S+=v; return S; }\\n}\\nint main(){ const int stalls[4]={0,1,3,2}; conv_c::inspect(stalls); std::cout<<\"total_stalls=\"<<conv_c::total(stalls)<<\"\\n\"; return 0; }"}
{"program":84,"context":"Weather buoy wave-height calibration header","noncompliant":"#include <iostream>\\n#include \"seedNC.h\"\\nnamespace buoy_nc{\\nvoid process(float h[], std::size_t n){ for(std::size_t i=0;i<n;++i){ float out = seed084_adjust_height(h[i]); std::cout<<\"raw=\"<<h[i]<<\" scale=\"<<seed084_wave_scale<<\" adj=\"<<out<<\"\\n\"; } }\\nfloat maxh(float a[], std::size_t n){ float m=a[0]; for(size_t i=1;i<n;++i) if(a[i]>m) m=a[i]; return m; }\\n}\\nint main(){ float h[5]={0.8F,1.2F,0.6F,1.5F,0.9F}; buoy_nc::process(h,5); std::cout<<\"peak=\"<<buoy_nc::maxh(h,5)<<\"\\n\"; return 0; }","compliant":"#include <iostream>\\n#include \"seedC.h\"\\nfloat seed084_wave_scale = 1.12F; float seed084_adjust_height(float h){ return h * seed084_wave_scale; }\\nnamespace buoy_c{\\ntemplate<std::size_t N> void process(const float(&h)[N]){ for(auto v:h){ float out=seed084_adjust_height(v); std::cout<<\"raw=\"<<v<<\" scale=\"<<seed084_wave_scale<<\" adj=\"<<out<<\"\\n\"; } }\\ntemplate<std::size_t N> float maxh(const float(&a)[N]){ float m=a[0]; for(auto v:a) if(v>m) m=v; return m; }\\n}\\nint main(){ const float h[5]={0.8F,1.2F,0.6F,1.5F,0.9F}; buoy_c::process(h); std::cout<<\"peak=\"<<buoy_c::maxh(h)<<\"\\n\"; return 0; }"}
{"program":85,"context":"Biomedical oxygen saturation smoothing header","noncompliant":"#include <iostream>\\n#include \"seedNC.h\"\\nnamespace bio_nc{\\nvoid filter(float v[], std::size_t n){ for(std::size_t i=0;i<n;++i){ float out = seed085_filter(v[i]); std::cout<<\"raw=\"<<v[i]<<\" alpha=\"<<seed085_spo2_alpha<<\" filt=\"<<out<<\"\\n\"; } }\\nfloat avg(float a[], std::size_t n){ float s=0; for(size_t i=0;i<n;++i) s+=a[i]; return n? s/n:0; }\\n}\\nint main(){ float r[6]={96.0F,95.5F,97.0F,94.0F,96.5F,95.0F}; bio_nc::filter(r,6); std::cout<<\"avg_raw=\"<<bio_nc::avg(r,6)<<\"\\n\"; return 0; }","compliant":"#include <iostream>\\n#include \"seedC.h\"\\nfloat seed085_spo2_alpha = 0.6F; float seed085_filter(float v){ return v * seed085_spo2_alpha; }\\nnamespace bio_c{\\ntemplate<std::size_t N> void filter(const float(&vals)[N]){ for(auto v:vals){ float out=seed085_filter(v); std::cout<<\"raw=\"<<v<<\" alpha=\"<<seed085_spo2_alpha<<\" filt=\"<<out<<\"\\n\"; } }\\ntemplate<std::size_t N> float avg(const float(&a)[N]){ float s=0; for(auto v:a) s+=v; return N? s/N:0; }\\n}\\nint main(){ const float r[6]={96.0F,95.5F,97.0F,94.0F,96.5F,95.0F}; bio_c::filter(r); std::cout<<\"avg_raw=\"<<bio_c::avg(r)<<\"\\n\"; return 0; }"}
{"program":86,"context":"Autonomous car lane deviation threshold header","noncompliant":"#include <iostream>\\n#include \"seedNC.h\"\\nnamespace av_nc{\\nvoid evaluate(double d[], std::size_t n){ for(std::size_t i=0;i<n;++i){ bool out = seed086_is_out(d[i]); std::cout<<\"dev=\"<<d[i]<<\" thr=\"<<seed086_lane_threshold<<\" outside=\"<<(out?\"YES\":\"NO\")<<\"\\n\"; } }\\ndouble maxd(double a[], std::size_t n){ double m=a[0]; for(size_t i=1;i<n;++i) if(a[i]>m) m=a[i]; return m; }\\n}\\nint main(){ double dev[5]={0.12,0.4,0.2,0.36,0.05}; av_nc::evaluate(dev,5); std::cout<<\"max_dev=\"<<av_nc::maxd(dev,5)<<\"\\n\"; return 0; }","compliant":"#include <iostream>\\n#include \"seedC.h\"\\ndouble seed086_lane_threshold = 0.35; bool seed086_is_out(double d){ return d > seed086_lane_threshold; }\\nnamespace av_c{\\ntemplate<std::size_t N> void evaluate(const double(&devs)[N]){ for(auto v:devs){ bool out=seed086_is_out(v); std::cout<<\"dev=\"<<v<<\" thr=\"<<seed086_lane_threshold<<\" outside=\"<<(out?\"YES\":\"NO\")<<\"\\n\"; } }\\ntemplate<std::size_t N> double maxd(const double(&a)[N]){ double m=a[0]; for(auto v:a) if(v>m) m=v; return m; }\\n}\\nint main(){ const double dev[5]={0.12,0.4,0.2,0.36,0.05}; av_c::evaluate(dev); std::cout<<\"max_dev=\"<<av_c::maxd(dev)<<\"\\n\"; return 0; }"}
{"program":87,"context":"Data center thermal zone alarm header","noncompliant":"#include <iostream>\\n#include \"seedNC.h\"\\nnamespace dc_nc{\\nvoid scan(int t[], std::size_t n){ for(std::size_t i=0;i<n;++i){ bool trig = seed087_trigger(t[i]); std::cout<<\"zone_temp=\"<<t[i]<<\" alarm_th=\"<<seed087_alarm_temp<<\" alarm=\"<<(trig?\"TRG\":\"OK\")<<\"\\n\"; } }\\nint peak(int a[], std::size_t n){ int m=a[0]; for(size_t i=1;i<n;++i) if(a[i]>m) m=a[i]; return m; }\\n}\\nint main(){ int t[4]={39,41,42,40}; dc_nc::scan(t,4); std::cout<<\"peak=\"<<dc_nc::peak(t,4)<<\"\\n\"; return 0; }","compliant":"#include <iostream>\\n#include \"seedC.h\"\\nint seed087_alarm_temp = 42; bool seed087_trigger(int t){ return t >= seed087_alarm_temp; }\\nnamespace dc_c{\\ntemplate<std::size_t N> void scan(const int(&temps)[N]){ for(auto v:temps){ bool trig=seed087_trigger(v); std::cout<<\"zone_temp=\"<<v<<\" alarm_th=\"<<seed087_alarm_temp<<\" alarm=\"<<(trig?\"TRG\":\"OK\")<<\"\\n\"; } }\\ntemplate<std::size_t N> int peak(const int(&a)[N]){ int m=a[0]; for(auto v:a) if(v>m) m=v; return m; }\\n}\\nint main(){ const int t[4]={39,41,42,40}; dc_c::scan(t); std::cout<<\"peak=\"<<dc_c::peak(t)<<\"\\n\"; return 0; }"}
{"program":88,"context":"Nuclear reactor coolant flow limit header","noncompliant":"#include <iostream>\\n#include \"seedNC.h\"\\nnamespace reactor_nc{\\nvoid validate(double f[], std::size_t n){ for(std::size_t i=0;i<n;++i){ bool low = seed088_low_flow(f[i]); std::cout<<\"flow=\"<<f[i]<<\" min=\"<<seed088_flow_min<<\" low=\"<<(low?\"YES\":\"NO\")<<\"\\n\"; } }\\ndouble minv(double a[], std::size_t n){ double m=a[0]; for(size_t i=1;i<n;++i) if(a[i]<m) m=a[i]; return m; }\\n}\\nint main(){ double f[4]={150.0,125.0,118.0,130.0}; reactor_nc::validate(f,4); std::cout<<\"min_flow=\"<<reactor_nc::minv(f,4)<<\"\\n\"; return 0; }","compliant":"#include <iostream>\\n#include \"seedC.h\"\\ndouble seed088_flow_min = 120.0; bool seed088_low_flow(double f){ return f < seed088_flow_min; }\\nnamespace reactor_c{\\ntemplate<std::size_t N> void validate(const double(&flows)[N]){ for(auto v:flows){ bool low = seed088_low_flow(v); std::cout<<\"flow=\"<<v<<\" min=\"<<seed088_flow_min<<\" low=\"<<(low?\"YES\":\"NO\")<<\"\\n\"; } }\\ntemplate<std::size_t N> double minv(const double(&a)[N]){ double m=a[0]; for(auto v:a) if(v<m) m=v; return m; }\\n}\\nint main(){ const double f[4]={150.0,125.0,118.0,130.0}; reactor_c::validate(f); std::cout<<\"min_flow=\"<<reactor_c::minv(f)<<\"\\n\"; return 0; }"}
{"program":89,"context":"Pipeline pressure regulation header","noncompliant":"#include <iostream>\\n#include \"seedNC.h\"\\nnamespace pipe_nc{\\nvoid check(float p[], std::size_t n){ for(std::size_t i=0;i<n;++i){ bool ok = seed089_pressure_ok(p[i]); std::cout<<\"p=\"<<p[i]<<\" max=\"<<seed089_max_pressure<<\" ok=\"<<(ok?\"YES\":\"NO\")<<\"\\n\"; } }\\nfloat avg(float a[], std::size_t n){ float s=0; for(size_t i=0;i<n;++i) s+=a[i]; return n? s/n:0; }\\n}\\nint main(){ float p[5]={260.0F,275.0F,280.0F,270.0F,265.0F}; pipe_nc::check(p,5); std::cout<<\"avg=\"<<pipe_nc::avg(p,5)<<\"\\n\"; return 0; }","compliant":"#include <iostream>\\n#include \"seedC.h\"\\nfloat seed089_max_pressure = 275.0F; bool seed089_pressure_ok(float p){ return p <= seed089_max_pressure; }\\nnamespace pipe_c{\\ntemplate<std::size_t N> void check(const float(&p)[N]){ for(auto v:p){ bool ok=seed089_pressure_ok(v); std::cout<<\"p=\"<<v<<\" max=\"<<seed089_max_pressure<<\" ok=\"<<(ok?\"YES\":\"NO\")<<\"\\n\"; } }\\ntemplate<std::size_t N> float avg(const float(&a)[N]){ float s=0; for(auto v:a) s+=v; return N? s/N:0; }\\n}\\nint main(){ const float p[5]={260.0F,275.0F,280.0F,270.0F,265.0F}; pipe_c::check(p); std::cout<<\"avg=\"<<pipe_c::avg(p)<<\"\\n\"; return 0; }"}
{"program":90,"context":"Spacecraft fuel consumption estimator header","noncompliant":"#include <iostream>\\n#include \"seedNC.h\"\\nnamespace fuel_nc{\\nvoid estimate_seconds(float s[], std::size_t n){ for(std::size_t i=0;i<n;++i){ float used = seed090_estimate(s[i]); std::cout<<\"sec=\"<<s[i]<<\" rate=\"<<seed090_fuel_rate<<\" used=\"<<used<<\"\\n\"; } }\\nfloat total(float a[], std::size_t n){ float S=0; for(size_t i=0;i<n;++i) S+=seed090_estimate(a[i]); return S; }\\n}\\nint main(){ float t[4]={1000.0F,2000.0F,500.0F,3600.0F}; fuel_nc::estimate_seconds(t,4); std::cout<<\"total_used=\"<<fuel_nc::total(t,4)<<\"\\n\"; return 0; }","compliant":"#include <iostream>\\n#include \"seedC.h\"\\nfloat seed090_fuel_rate = 0.0025F; float seed090_estimate(float sec){ return sec * seed090_fuel_rate; }\\nnamespace fuel_c{\\ntemplate<std::size_t N> void estimate_seconds(const float(&secs)[N]){ for(auto s:secs){ float used=seed090_estimate(s); std::cout<<\"sec=\"<<s<<\" rate=\"<<seed090_fuel_rate<<\" used=\"<<used<<\"\\n\"; } }\\ntemplate<std::size_t N> float total(const float(&a)[N]){ float S=0; for(auto v:a) S+=seed090_estimate(v); return S; }\\n}\\nint main(){ const float t[4]={1000.0F,2000.0F,500.0F,3600.0F}; fuel_c::estimate_seconds(t); std::cout<<\"total_used=\"<<fuel_c::total(t)<<\"\\n\"; return 0; }"}
{"program":91,"context":"Smart irrigation soil-moisture cutoff header","noncompliant":"#include <iostream>\n#include <iomanip>\n#include "seedNC.h"\n\nnamespace irrig_nc {\n\n// Non-compliant: relies on definitions inside seedNC.h (mutable globals + function)\nvoid assess_and_report(float moisture_readings[], std::size_t n) {\n    std::cout << "Smart irrigation (NC) report:\n";\n    for (std::size_t i = 0; i < n; ++i) {\n        bool irrig = seed091_should_irrigate(moisture_readings[i]); // NC: function from header\n        std::cout << "  sensor[" << i << "]=" << std::fixed << std::setprecision(1)\n                  << moisture_readings[i] << "% -> irrigate=" << (irrig?"YES":"NO") << "\n";\n    }\n}\n\nfloat compute_mean(float arr[], std::size_t n) {\n    float s = 0.0F;\n    for (std::size_t i = 0; i < n; ++i) s += arr[i];\n    return (n == 0U) ? 0.0F : (s / static_cast<float>(n));\n}\n\n} // namespace irrig_nc\n\nint main() {\n    using namespace irrig_nc;\n    float samples[8] = {20.1F, 17.8F, 22.4F, 16.5F, 19.0F, 18.2F, 15.9F, 21.3F};\n    assess_and_report(samples, 8);\n    std::cout << "mean=" << compute_mean(samples, 8) << "%\n";\n    // Demonstrate mutation (header-defined non-const global)\n    std::cout << "(NC) cutoff currently=" << seed091_moisture_cutoff << "% (modifiable from header)\n";\n    return 0;\n}","compliant":"#include <iostream>\n#include <iomanip>\n#include "seedC.h"\n\n// Compliant program: externs are in seedC.h; define storage here.\nfloat seed091_moisture_cutoff = 18.5F; // definition in .cpp (compliant)\n\nnamespace irrig_c {\n\ntemplate <std::size_t N>\nvoid assess_and_report(const float (&moisture_readings)[N]) {\n    std::cout << "Smart irrigation (C) report:\n";\n    for (std::size_t i = 0; i < N; ++i) {\n        bool irrig = seed091_should_irrigate(moisture_readings[i]); // calls declaration from header\n        std::cout << "  sensor[" << i << "]=" << std::fixed << std::setprecision(1)\n                  << moisture_readings[i] << "% -> irrigate=" << (irrig?"YES":"NO") << "\n";\n    }\n}\n\ntemplate <std::size_t N>\nfloat compute_mean(const float (&arr)[N]) {\n    float s = 0.0F;\n    for (auto v : arr) s += v;\n    return (N == 0U) ? 0.0F : (s / static_cast<float>(N));\n}\n\n} // namespace irrig_c\n\nint main() {\n    using namespace irrig_c;\n    const float samples[8] = {20.1F, 17.8F, 22.4F, 16.5F, 19.0F, 18.2F, 15.9F, 21.3F};\n    assess_and_report(samples);\n    std::cout << "mean=" << compute_mean(samples) << "%\n";\n    // seed091_moisture_cutoff is defined in this translation unit (compliant)\n    std::cout << "(C) cutoff value=" << seed091_moisture_cutoff << "%\n";\n    return 0;\n}"}
{"program":92,"context":"Airport runway visibility threshold header","noncompliant":"#include <iostream>\n#include <vector>\n#include "seedNC.h"\n\nnamespace rwy_nc {\n\n// Uses header-defined non-const value and inline helper from seedNC.h\nvoid evaluate_visibility(double visibilities[], std::size_t n) {\n    std::cout << "Runway visibility (NC) evaluation:\n";\n    for (std::size_t i = 0; i < n; ++i) {\n        bool low = seed092_is_low(visibilities[i]);\n        std::cout << "  reading[" << i << "]=" << visibilities[i] << " km -> low=" << (low?"YES":"NO") << "\n";\n    }\n}\n\ndouble max_vis(double a[], std::size_t n) {\n    double m = (n>0)? a[0] : 0.0;\n    for (std::size_t i=1;i<n;++i) if (a[i] > m) m = a[i];\n    return m;\n}\n\n} // ns\n\nint main(){\n    using namespace rwy_nc;\n    double v[6] = {2.5, 1.0, 0.8, 3.2, 1.5, 0.9};\n    evaluate_visibility(v, 6);\n    std::cout << "max_vis=" << max_vis(v,6) << " km\n";\n    std::cout << "(NC) threshold=" << seed092_visibility_limit << " km\n";\n    return 0;\n}","compliant":"#include <iostream>\n#include <vector>\n#include "seedC.h"\n\n// define storage for externs declared in seedC.h\ndouble seed092_visibility_limit = 1.2;\n\nnamespace rwy_c {\n\ntemplate <std::size_t N>\nvoid evaluate_visibility(const double (&visibilities)[N]) {\n    std::cout << "Runway visibility (C) evaluation:\n";\n    for (std::size_t i = 0; i < N; ++i) {\n        bool low = seed092_is_low(visibilities[i]);\n        std::cout << "  reading[" << i << "]=" << visibilities[i] << " km -> low=" << (low?"YES":"NO") << "\n";\n    }\n}\n\ntemplate <std::size_t N>\ndouble max_vis(const double (&a)[N]) {\n    double m = (N>0)? a[0] : 0.0;\n    for (auto v : a) if (v > m) m = v;\n    return m;\n}\n\n} // ns\n\nint main(){\n    using namespace rwy_c;\n    const double v[6] = {2.5, 1.0, 0.8, 3.2, 1.5, 0.9};\n    evaluate_visibility(v);\n    std::cout << "max_vis=" << max_vis(v) << " km\n";\n    std::cout << "(C) threshold=" << seed092_visibility_limit << " km\n";\n    return 0;\n}"}
{"program":93,"context":"Robotic gripper force calibration header","noncompliant":"#include <iostream>\n#include "seedNC.h"\n\nnamespace grip_nc {\n\n// Non-compliant program uses header-defined offset and inline calibrator\nint apply_calibration(int raw_force) {\n    // directly call header function (defined in header)\n    return seed093_calibrate(raw_force);\n}\n\nvoid analyze_sequence(int measurements[], std::size_t n) {\n    std::cout << "Gripper calibration (NC) sequence:\n";\n    for (std::size_t i=0;i<n;++i) {\n        int cal = apply_calibration(measurements[i]);\n        std::cout << "  raw="<<measurements[i]<<" cal="<<cal<<"\n";\n    }\n}\n\n} // ns\n\nint main(){\n    using namespace grip_nc;\n    int seq[6] = {100,110,95,120,105,99};\n    analyze_sequence(seq,6);\n    std::cout << "(NC) offset in header=" << seed093_force_offset << "\n";\n    return 0;\n}","compliant":"#include <iostream>\n#include "seedC.h"\n\n// define the variable declared in seedC.h\nint seed093_force_offset = 12;\n\nnamespace grip_c {\n\nint apply_calibration(int raw_force) {\n    // implementation in translation unit (compliant)\n    return seed093_calibrate(raw_force); // calls function declared in header\n}\n\ntemplate <std::size_t N>\nvoid analyze_sequence(const int (&measurements)[N]) {\n    std::cout << "Gripper calibration (C) sequence:\n";\n    for (auto m : measurements) {\n        int cal = apply_calibration(m);\n        std::cout << "  raw="<<m<<" cal="<<cal<<"\n";\n    }\n}\n\n} // ns\n\nint main(){\n    using namespace grip_c;\n    const int seq[6] = {100,110,95,120,105,99};\n    analyze_sequence(seq);\n    std::cout << "(C) offset=" << seed093_force_offset << "\n";\n    return 0;\n}"}
{"program":94,"context":"Radiation sensor dose alarm header","noncompliant":"#include <iostream>\n#include <array>\n#include "seedNC.h"\n\nnamespace rad_nc {\n\nvoid report(const float doses[], std::size_t n) {\n    std::cout << "Radiation doses (NC):\n";\n    for (std::size_t i=0;i<n;++i) {\n        bool alarm = seed094_alarm(doses[i]);\n        std::cout << "  sensor["<<i<<"]="<<doses[i]<<" Gy -> alarm="<<(alarm?"YES":"NO")<<"\n";\n    }\n}\n\nfloat highest(const float arr[], std::size_t n) {\n    float h = (n>0)? arr[0] : 0.0F;\n    for (std::size_t i=1;i<n;++i) if (arr[i] > h) h = arr[i];\n    return h;\n}\n\n} // ns\n\nint main(){\n    using namespace rad_nc;\n    float samples[5] = {0.12F, 0.8F, 0.60F, 0.9F, 0.40F};\n    report(samples, 5);\n    std::cout << "peak="<< highest(samples,5) << " Gy\n";\n    std::cout << "(NC) limit=" << seed094_dose_limit << " Gy\n";\n    return 0;\n}","compliant":"#include <iostream>\n#include <array>\n#include "seedC.h"\n\n// define storage for the extern in seedC.h\nfloat seed094_dose_limit = 0.75F;\n\nnamespace rad_c {\n\ntemplate <std::size_t N>\nvoid report(const float (&doses)[N]) {\n    std::cout << "Radiation doses (C):\n";\n    for (std::size_t i=0;i<N;++i) {\n        bool alarm = seed094_alarm(doses[i]);\n        std::cout << "  sensor["<<i<<"]="<<doses[i]<<" Gy -> alarm="<<(alarm?"YES":"NO")<<"\n";\n    }\n}\n\ntemplate <std::size_t N>\nfloat highest(const float (&arr)[N]) {\n    float h = (N>0)? arr[0] : 0.0F;\n    for (auto v : arr) if (v > h) h = v;\n    return h;\n}\n\n} // ns\n\nint main(){\n    using namespace rad_c;\n    const float samples[5] = {0.12F, 0.8F, 0.60F, 0.9F, 0.40F};\n    report(samples);\n    std::cout << "peak="<< highest(samples) << " Gy\n";\n    std::cout << "(C) limit=" << seed094_dose_limit << " Gy\n";\n    return 0;\n}"}
{"program":95,"context":"Cryptomining rig thermal throttle header","noncompliant":"#include <iostream>\n#include "seedNC.h"\n#include <thread>\n#include <chrono>\n\nnamespace miner_nc {\n\n// Agglomerated monitoring loop (NC) — uses header-defined threshold directly\nvoid monitor_and_throttle(int temps[], std::size_t n) {\n    std::cout << "Miner thermal monitor (NC):\n";\n    for (std::size_t i=0;i<n;++i) {\n        bool thr = seed095_should_throttle(temps[i]);\n        std::cout << "  core["<<i<<"]="<<temps[i]<<" C -> throttle="<<(thr?"YES":"NO")<<"\n";\n    }\n}\n\n} // ns\n\nint main(){\n    using namespace miner_nc;\n    int cores[6] = {85, 90, 92, 88, 91, 87};\n    monitor_and_throttle(cores, 6);\n    std::cout << "(NC) throttle temp (in-header)="<< seed095_throttle_temp << " C\n";\n    return 0;\n}","compliant":"#include <iostream>\n#include "seedC.h"\n\n// define the storage for the extern declared in seedC.h\nint seed095_throttle_temp = 92;\n\nnamespace miner_c {\n\ntemplate <std::size_t N>\nvoid monitor_and_throttle(const int (&temps)[N]) {\n    std::cout << "Miner thermal monitor (C):\n";\n    for (std::size_t i=0;i<N;++i) {\n        bool thr = seed095_should_throttle(temps[i]);\n        std::cout << "  core["<<i<<"]="<<temps[i]<<" C -> throttle="<<(thr?"YES":"NO")<<"\n";\n    }\n}\n\n} // ns\n\nint main(){\n    using namespace miner_c;\n    const int cores[6] = {85, 90, 92, 88, 91, 87};\n    monitor_and_throttle(cores);\n    std::cout << "(C) throttle temp="<< seed095_throttle_temp << " C\n";\n    return 0;\n}"}
{"program":96,"context":"Hydraulic press pressure-limit header","noncompliant":"#include <iostream>\n#include "seedNC.h"\n#include <algorithm>\n\nnamespace press_nc {\n\n// Non-compliant program reads header-defined limit and calls helper defined there.\nbool any_exceed(double readings[], std::size_t n) {\n    for (std::size_t i=0;i<n;++i) { if (seed096_exceeds(readings[i])) return true; }\n    return false;\n}\n\nvoid summary(double vals[], std::size_t n) {\n    std::cout << "Press readings (NC):\n";\n    for (std::size_t i=0;i<n;++i) std::cout << "  "<<vals[i]<<" psi\n";\n    std::cout << "Limit in header="<<seed096_pressure_cap<<" psi\n";\n}\n\n} // ns\n\nint main(){\n    using namespace press_nc;\n    double r[5] = {14500.0, 14900.0, 15100.0, 14800.0, 15050.0};\n    summary(r,5);\n    std::cout << "Any exceed? " << (any_exceed(r,5) ? "YES\n" : "NO\n");\n    return 0;\n}","compliant":"#include <iostream>\n#include "seedC.h"\n#include <algorithm>\n\n// define the extern-storage here (compliant)\ndouble seed096_pressure_cap = 15000.0;\n\nnamespace press_c {\n\ntemplate <std::size_t N>\nbool any_exceed(const double (&readings)[N]) {\n    for (auto v : readings) { if (seed096_exceeds(v)) return true; }\n    return false;\n}\n\ntemplate <std::size_t N>\nvoid summary(const double (&vals)[N]) {\n    std::cout << "Press readings (C):\n";\n    for (auto v : vals) std::cout << "  "<<v<<" psi\n";\n    std::cout << "Limit="<<seed096_pressure_cap<<" psi\n";\n}\n\n} // ns\n\nint main(){\n    using namespace press_c;\n    const double r[5] = {14500.0, 14900.0, 15100.0, 14800.0, 15050.0};\n    summary(r);\n    std::cout << "Any exceed? " << (any_exceed(r) ? "YES\n" : "NO\n");\n    return 0;\n}"}
{"program":97,"context":"Aviation fuel temperature compensation header","noncompliant":"#include <iostream>\n#include "seedNC.h"\n#include <iomanip>\n\nnamespace avfuel_nc {\n\n// Uses seed097_temp_coeff defined in header (NC).\nfloat corrected_density(float temp_c) {\n    return seed097_compensate(temp_c); // function defined in NC header\n}\n\nvoid table(float temps[], std::size_t n) {\n    std::cout << "Fuel compensation table (NC):\n";\n    for (std::size_t i=0;i<n;++i) {\n        float factor = corrected_density(temps[i]);\n        std::cout << "  t="<<temps[i]<<" C -> factor="<< std::fixed << std::setprecision(4) << factor <<"\n";\n    }\n}\n\n} // ns\n\nint main(){\n    using namespace avfuel_nc;\n    float t[6] = {-20.0F, 0.0F, 15.0F, 25.0F, 40.0F, 55.0F};\n    table(t,6);\n    std::cout << "(NC) temp coeff="<<seed097_temp_coeff<<"\n";\n    return 0;\n}","compliant":"#include <iostream>\n#include "seedC.h"\n#include <iomanip>\n\n// define storage for the extern in seedC.h\nfloat seed097_temp_coeff = -0.0021F;\n\nnamespace avfuel_c {\n\nfloat corrected_density_impl(float temp_c) {\n    // implement using the declared function (seed097_compensate)\n    return seed097_compensate(temp_c);\n}\n\ntemplate <std::size_t N>\nvoid table(const float (&temps)[N]) {\n    std::cout << "Fuel compensation table (C):\n";\n    for (auto t : temps) {\n        float factor = corrected_density_impl(t);\n        std::cout << "  t="<<t<<" C -> factor="<< std::fixed << std::setprecision(4) << factor <<"\n";\n    }\n}\n\n} // ns\n\nint main(){\n    using namespace avfuel_c;\n    const float t[6] = {-20.0F, 0.0F, 15.0F, 25.0F, 40.0F, 55.0F};\n    table(t);\n    std::cout << "(C) temp coeff="<<seed097_temp_coeff<<"\n";\n    return 0;\n}"}
{"program":98,"context":"Smartwatch step-counter smoothing header","noncompliant":"#include <iostream>\n#include "seedNC.h"\n#include <numeric>\n\nnamespace watch_nc {\n\n// Non-compliant: header provides non-const alpha and function implementation\nfloat smooth_sequence(int steps[], std::size_t n) {\n    float acc = 0.0F;\n    for (std::size_t i=0;i<n;++i) acc += seed098_smooth(steps[i]);\n    return (n==0)?0.0F: (acc / static_cast<float>(n));\n}\n\n} // ns\n\nint main(){\n    using namespace watch_nc;\n    int seq[7] = {12, 15, 14, 10, 18, 20, 16};\n    float avg = smooth_sequence(seq,7);\n    std::cout << "(NC) smoothed avg steps="<<avg<<" (alpha in-header="<<seed098_smooth_alpha<<")\n";\n    return 0;\n}","compliant":"#include <iostream>\n#include "seedC.h"\n#include <numeric>\n\n// define storage for extern\nfloat seed098_smooth_alpha = 0.35F;\n\nnamespace watch_c {\n\ntemplate <std::size_t N>\nfloat smooth_sequence(const int (&steps)[N]) {\n    float acc = 0.0F;\n    for (auto s : steps) acc += seed098_smooth(s);\n    return (N==0)?0.0F: (acc / static_cast<float>(N));\n}\n\n} // ns\n\nint main(){\n    using namespace watch_c;\n    const int seq[7] = {12, 15, 14, 10, 18, 20, 16};\n    float avg = smooth_sequence(seq);\n    std::cout << "(C) smoothed avg steps="<<avg<<" (alpha="<<seed098_smooth_alpha<<")\n";\n    return 0;\n}"}
{"program":99,"context":"Solar inverter voltage sag detector header","noncompliant":"#include <iostream>\n#include "seedNC.h"\n#include <vector>\n\nnamespace inverter_nc {\n\nbool any_sag(float volts[], std::size_t n) {\n    for (std::size_t i=0;i<n;++i) if (seed099_sag(volts[i])) return true;\n    return false;\n}\n\nvoid diagnostics(float volts[], std::size_t n) {\n    std::cout << "Inverter voltage diagnostics (NC):\n";\n    for (std::size_t i=0;i<n;++i) {\n        std::cout << "  v["<<i<<"]="<<volts[i]<<" V -> sag="<<(seed099_sag(volts[i])?"YES":"NO")<<"\n";\n    }\n}\n\n} // ns\n\nint main(){\n    using namespace inverter_nc;\n    float samples[6] = {230.0F, 218.0F, 212.0F, 221.0F, 209.0F, 215.0F};\n    diagnostics(samples,6);\n    std::cout << "Any sag? " << (any_sag(samples,6) ? "YES\n" : "NO\n");\n    std::cout << "(NC) min voltage="<<seed099_min_voltage<<" V\n";\n    return 0;\n}","compliant":"#include <iostream>\n#include "seedC.h"\n#include <vector>\n\n// define storage for extern declared in seedC.h\nfloat seed099_min_voltage = 210.0F;\n\nnamespace inverter_c {\n\ntemplate <std::size_t N>\nbool any_sag(const float (&volts)[N]) {\n    for (auto v : volts) if (seed099_sag(v)) return true;\n    return false;\n}\n\ntemplate <std::size_t N>\nvoid diagnostics(const float (&volts)[N]) {\n    std::cout << "Inverter voltage diagnostics (C):\n";\n    for (std::size_t i=0;i<N;++i) {\n        std::cout << "  v["<<i<<"]="<<volts[i]<<" V -> sag="<<(seed099_sag(volts[i])?"YES":"NO")<<"\n";\n    }\n}\n\n} // ns\n\nint main(){\n    using namespace inverter_c;\n    const float samples[6] = {230.0F, 218.0F, 212.0F, 221.0F, 209.0F, 215.0F};\n    diagnostics(samples);\n    std::cout << "Any sag? " << (any_sag(samples) ? "YES\n" : "NO\n");\n    std::cout << "(C) min voltage="<<seed099_min_voltage<<" V\n";\n    return 0;\n}"}
{"program":100,"context":"Deep-sea probe hull stress limit header","noncompliant":"#include <iostream>\n#include "seedNC.h"\n#include <cmath>\n\nnamespace deep_nc {\n\n// Non-compliant: uses header-defined stress limit directly and header function\nbool any_alert(double stresses[], std::size_t n) {\n    for (std::size_t i=0;i<n;++i) if (seed100_warn(stresses[i])) return true;\n    return false;\n}\n\nvoid detailed_report(double s[], std::size_t n) {\n    std::cout << "Hull stress report (NC):\n";\n    for (std::size_t i=0;i<n;++i) {\n        double depth_eq = std::sqrt(std::fabs(s[i]));\n        std::cout << "  panel["<<i<<"] stress="<<s[i]<<" -> depth_eq="<<depth_eq<<" warn="<<(seed100_warn(s[i])?"YES":"NO")<<"\n";\n    }\n}\n\n} // ns\n\nint main(){\n    using namespace deep_nc;\n    double vals[5] = {12.0, 19.0, 18.2, 17.5, 20.1};\n    detailed_report(vals,5);\n    std::cout << "Any warning? " << (any_alert(vals,5) ? "YES\n" : "NO\n");\n    std::cout << "(NC) stress limit="<<seed100_stress_limit<<"\n";\n    return 0;\n}","compliant":"#include <iostream>\n#include "seedC.h"\n#include <cmath>\n\n// define the storage for extern in seedC.h\ndouble seed100_stress_limit = 18.7;\n\nnamespace deep_c {\n\ntemplate <std::size_t N>\nbool any_alert(const double (&stresses)[N]) {\n    for (auto v : stresses) if (seed100_warn(v)) return true;\n    return false;\n}\n\ntemplate <std::size_t N>\nvoid detailed_report(const double (&s)[N]) {\n    std::cout << "Hull stress report (C):\n";\n    for (std::size_t i=0;i<N;++i) {\n        double depth_eq = std::sqrt(std::fabs(s[i]));\n        std::cout << "  panel["<<i<<"] stress="<<s[i]<<" -> depth_eq="<<depth_eq<<" warn="<<(seed100_warn(s[i])?"YES":"NO")<<"\n";\n    }\n}\n\n} // ns\n\nint main(){\n    using namespace deep_c;\n    const double vals[5] = {12.0, 19.0, 18.2, 17.5, 20.1};\n    detailed_report(vals);\n    std::cout << "Any warning? " << (any_alert(vals) ? "YES\n" : "NO\n");\n    std::cout << "(C) stress limit="<<seed100_stress_limit<<"\n";\n    return 0;\n}"}
