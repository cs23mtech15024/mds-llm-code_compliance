// Context: Previous error state tracker

// Seed 082 — C: variable without underscore prefix
#include <iostream>
int main(){
    float prev_error = 0.0F; // C
    std::cout << "prev_err=" << prev_error << "\n";
    return 0;
}