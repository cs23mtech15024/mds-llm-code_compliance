// Context: Median filter window size

// Seed 051 — C: variable without underscore prefix
#include <iostream>
int main(){
    int window_size = 5; // C
    std::cout << "window=" << window_size << "\n";
    return 0;
}