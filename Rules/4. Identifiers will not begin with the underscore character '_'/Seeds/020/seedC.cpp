// Context: Timer interval milliseconds

// Seed 020 — C: variable without underscore prefix
#include <iostream>
int main(){
    long interval_ms = 1000; // C
    std::cout << "timer=" << interval_ms << "ms\n";
    return 0;
}