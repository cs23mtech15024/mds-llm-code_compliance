// Context: Loop iteration counter

// Seed 086 — C: variable without underscore prefix
#include <iostream>
int main(){
    int iteration = 0; // C
    for(iteration = 0; iteration < 10; iteration++){
        // process
    }
    std::cout << "iters=" << iteration << "\n";
    return 0;
}