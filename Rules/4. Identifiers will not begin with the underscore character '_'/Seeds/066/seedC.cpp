// Context: Smoothing filter alpha value

// Seed 066 — C: variable without underscore prefix
#include <iostream>
int main(){
    float alpha = 0.3F; // C
    std::cout << "alpha=" << alpha << "\n";
    return 0;
}